# 业务线 10 · 工具确认 UI(R2 复测 · DEF-07 回归口径收严)

- 被测:写工具确认卡(批准/拒绝)、只读直通、SCOPE_RESOLVED CustomEvent、scope chip 渲染边界
- 用例文件:`e2e/specs/11-tool-confirm.spec.ts`(4 用例)
- 结论:**4 通过,0 失败,0 REGRESSED**。**DEF-07 关闭,且回归口径较 R1 收严**:R1 需网关剔除 `enabledMcpTools:[]` 才能触达注册工具;R2 在 **demo-host 默认接入、零网关补偿**下,注册目录工具直接可达(服务端全程 0 条「不在 toolkit」WARN),确认卡/SCOPE_RESOLVED 行为与 R1 补偿态一致。

## 用例明细

| # | 用例 | R2 实际 | 判定 | 证据(assets/) |
| --- | --- | --- | --- | --- |
| 1 | 写工具确认·批准 | 确认卡(工具名+等待确认+允许/拒绝)→ 允许 → 续流 DONE + 全文;宿主简介更新、版本 +1、写工具恰执行一次;审计恰 1 条 allowed(live-confirm) | ✅ | `L10-02-写工具确认卡(工具名+等待确认+允许拒绝).png`、`L10-05-批准后续流至DONE(工具行已完成).png`、`L10-01-宿主状态-后.json`、`L10-06-psql-审计行.txt` |
| 2 | 写工具确认·拒绝 | 拒绝 → 终态已完成(模型收尾);工具行「已拒绝」;**宿主零执行**(模板数不变、调用计数 +0);审计恰 1 条 denied(live-confirm) | ✅ | `L10-09-拒绝后终态(工具行已拒绝).png`、`L10-07-宿主状态-后.json`、`L10-10-psql-审计行.txt` |
| 3 | 两轮脚本(读直通+写确认)与 scope chip 边界 | 只读工具直通(0 次询问);确认卡仅属写工具;单工具行内确认无 scope chip(≥2 批量条才渲染,F13 维持);SCOPE_RESOLVED 恰派发一次 | ✅ | `L10-11-只读直通观察.json`、`L10-12-scope-chip两轮观察.json`、`L10-11-SCOPE_RESOLVED-两轮.json` |
| 4 | 只读工具直通对照(U5) | 全程无确认卡;DONE;宿主恰执行一次;无确认审计行;无 SCOPE_RESOLVED | ✅ | `L10-15-只读工具直通至DONE(无确认卡).png`、`L10-14-宿主状态-后.json`、`L10-16-*.json/txt` |

## SCOPE_RESOLVED 复测证据(宿主元素监听)

- detail 完整:conversationId / runId / replyId / tools[].{toolCallId, toolName, scope};
- scope 恒降级形态 `{resolved:false, degraded:true, summary:"宿主未实现约束范围反查(resolve_scope)…"}`(服务端安全缺省,fail-closed,与 R1 一致);
- 同确认批恰派发一次(runId:replyId 去重)。
- 证据:`L10-03-SCOPE_RESOLVED-detail.json`。

## DEF-07 闭环

- R1 发现:SDK 空引用默认下发 `enabledMcpTools:[]` → 服务端按显式空白名单过滤 → UI 会话中注册工具全部不可达(P1)。
- 修复提交:**d0e569a**(服务端 selectTools 三分法:null/[]=未指定→默认可见性)+ **f6c84dd**(SDK 空引用不再下发该字段)。
- R2 复测:L10 全线在 demo-host 默认接入(无 `/runs` 请求体剔除补偿)通过;journal 全量请求中 **0 处 enabledMcpTools 出现**;服务端日志 0 条「不在 toolkit」。**判定:关闭**。
