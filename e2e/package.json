{
  "name": "inneragent-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "InnerAgent E2E 测试基建(Playwright;只测不改产品代码)",
  "scripts": {
    "test": "playwright test",
    "test:ui-alive": "playwright test --project=chromium",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
