---
name: inneragent-implementation-state
description: InnerAgent 无人值守实现的目标协议、构建命令与阶段进度锚点
metadata:
  type: project
---

InnerAgent(/Users/justin/codes/funcommons/InnerAgent)正在无人值守实现四份文档(01-PRD V1.3 / 02-技术方案 V1.1 / 03-开发计划 V1.1 / 04-对标调研)。参考代码只读:/Users/justin/codes/funcommons/mmagix-minicuts-backup/(前端 frontend/,后端 ai-fusion-video/)。

**Why:** 目标是 /goal 持久指令(实现全部文档,多子agent分工,每步≤1000行,写测试→写代码→测试→修复循环,单任务测试仅测对应代码且由子agent异步执行,>10%或>1000行或>10任务触发全量测试);会话压缩后需凭此恢复。

**How to apply:**
- 构建命令:`export JAVA_HOME=$(/usr/libexec/java_home -v 21) && cd inneragent-server && mvn -q compile`(系统 mvn 默认 JDK17,必须覆盖)。
- 移植:scripts/port-kernel.sh(机械复制+改名,244 文件);审计:scripts/audit-port-diff.sh(依据 scripts/port-report.txt);规范:移植改动 `[port]`、定向改造 `[adapt]`。
- 融光 service/ai/agentscope 子包映射到 com.inneragent.agent.{kernel,state,workspace,mcp,skill,context,runtime,message,permission,tool};run→agent.run;provider→model.provider;未移植依赖一律 com.inneragent.platform.*(从融光同路径复制改名)。
- 开发库:docker/dev-compose.yml(PG 15432 / Redis 16379);迁移规范见 inneragent-server/src/main/resources/db/migration/README.md(ia_ 前缀、app_id NOT NULL DEFAULT 1、event.schema_version)。
- 阶段:P0 移植基座(6 任务)→ P1 工具中枢/MCP/starter → P2 管理站/MVP → P3 融光灰度 → P4 增强。进度以任务列表(TaskList)为准。
- P0 全部完成(2026-09-20,T1–T6):275 主文件+44 兼容类;审计 PASS(222 clean/0 diff/8 [adapt] 白名单,whitelist=scripts/audit-whitelist.txt);ia_ schema V1–V4(19 表+demo seed);真机启动+smoke 真跑通过(CONTENT/DONE、event seq 1..20 零缺口、三表落库);测试套件 82 类移植:286/286 单测绿、52/55 IT(3 项 P1 登记:①PG 子运行幂等 DuplicateKeyException 后同事务再查 25P02→REQUIRES_NEW/预检 ②子运行取消被同步终态 ③租户拦截器→P1-T1 落地后撤销测试适配)。
- P1 在途(5 路并行,2026-09-20):①**T1 已完成**(839a693:AppContext/UserContext 双层上下文、EmbedTokenVerifier RS256 验签+fail-closed、ActTokenIssuer 60s RFC8693+JWKS /.well-known/jwks.json+72h 轮换宽限、AdminTokenFilter(X-IA-Admin-Key vs IA_ADMIN_KEY env)+apps CRUD、AppTenantLineInnerInterceptor 双列组合、@EnableMethodSecurity、surefire 306/306)②T3a sdk-js+demo-host**已完成**(pnpm monorepo:core=API 重映射/tokenGetter 401 懒换单飞/timeline reducer 1:1;components=inneragent-chat WC,element-plus 全拆除换 Ia* 自写件;Vitest 187 全绿+strict typecheck;10 提交;**遗留:服务端仍是融光旧路径 /api/ai/pipeline/*,SDK 打的是 /ia/api/v1/* 新契约→T3b/c 收口任务=API 面重映射+附件端点+偏差对齐,排在合并窗口后**;iframe 仅占位 W15)③遗留台账修复**已闭环**(8f2214c 合并+适配撤销 f339333+合并态全量回归 surefire 377/377、failsafe 62/62 全绿;P0 交付的 3 项 P1 IT 工单全部关闭);④spike 完成;T2b-starter 完成(08422ca,44/44,独立工程 inneragent-starter/,对齐遗留:aud 占位 ia-mcp、resolve_scope REST 占位 /ia/api/v1/tools/resolve-scope 待收口裁决);T2b-invoker **已合并**(28d6562/9e15992/0a9bd52 干净并入;**SDK 钉 0.17.0 勿升 2.0.1**——agentscope-core 2.0.0 传递依赖即 0.17.0,聚合绑 mcp-json-jackson2 双栈消解,0.17.0 对 session-not-found 内建隐式重初始化故恢复形态=单飞重建客户端;客户端须关 resumableStreams/openConnectionOnStartup 兼容 stateless 宿主;遗留:act claims 的 runId/appKey 需内核管线透传、§4.7 限流属目录/策略层);终态回归**全绿定型**(surefire 411/411+failsafe 71/71+starter 44/44;途中抓到并修复 McpToolCatalog 双构造器缺 @Autowired 的装配回归 d58c4a9——合并窗口全量回归的价值实证);P1-T2 关闭;**P1 出口验收已过半**(76dd76f 活体测试 8/8 真机全通过:M0 对话基座+JWKS+admin 缺省封闭+附件矩阵+demo-host 构建;SSE 序号口径修正入开发计划——journal 连续、SSE 可空洞;缺口=U1 写确认真机:demo-spring-host 宿主已建成(1b71684,5/5 冒烟)+旅程脚本(3a9e01d),真机暴露 **D1/D2/D3 三服务端缺陷**(D1 内核工具面不含 catalog 注册宿主工具——内核 MCP 唯一来源是 AgentScopeMcpRegistry 非 McpToolCatalog,invoker 唯一生产调用点是 ResolveScopeService;D2 MockAiProvider 硬编码只调 get_current_time;D3 确认 allowed/denied 审计无生产写入点),修复子agent 在跑(任务 #16,验收门=旅程 exit 0+411/71 不破);缺陷②SCOPE_RESOLVED 前端契约缺失已登记任务;接入成本=1 依赖+每工具 1 注解方法+1 行 yaml);**T3b/c 收口已完成**(8525b21/b799ef0/e7bf95b/b68bfaf:旧路径→/ia/api/v1/* 一次性切换且旧路径 404、SDK 8 偏差全对齐零拒绝、附件端点 V7+三后端落盘、resolve-scope 薄 REST 保留、smoke 新契约真跑 SMOKE DONE、surefire 393/393+failsafe 62/62;遗留入任务 #14:附件 GET 端点+媒体地址双前缀、/api/ai/model/list 无守卫下发 config 安全项、me 语义归位)。**P1 剩余:T2b-invoker worktree 分支合并→端到端演示验收(demo-host 连真服务)→P1 出口核对**。④MCP 桥接 spike(spikes/mcp-bridge-spike 独立 pom,产 FINDINGS.md)⑤管理站 web/ 脚手架**已完成**(8 提交,Vitest 102 全绿+typecheck/build 干净;七域 admin 客户端+六视图 msw mock;**8 项契约自拟字段登记于 web/src/api/types.ts 头注释,T2a 完成后须对齐真实端点→工单 #13**)⑥T2a 工具中枢服务端(注册表+指纹分诊 V14+grant/resolve_scope+McpToolCatalog+McpToolInvoker 端口,迁移只用 V6,V5 已被③预留)。
- **Spike 结论已定(551333e,19/19 绿,详见 spikes/mcp-bridge-spike/FINDINGS.md)**:Q1 确认,官方 `io.modelcontextprotocol.sdk:mcp:2.0.1`(servlet 传输在 core 内,无需 Spring AI);stateless 必须取 `HttpServletStatelessServerTransport`(sessionful 端点拒无会话请求);X-IA-Act 三段挂点=客户端 httpRequestCustomizer+transportContextProvider→Jakarta Filter 401 fail-closed→contextExtractor→McpTransportContext;**SDK 无自动重连**(session terminated 后对同实例再 initialize(),重连编排 McpToolCatalog 自建);**stateless 无推送通道**(tools/list_changed 仅会话态,活刷新走指纹轮询);schema 无 POJO 生成器(宿主 SPI 供 Map);R2 Jackson2/3 双栈可切 mcp-json-jackson2;R6 maxRequestSize 413 大参数需调高。
- **T2b 接管点**:ActTokenSupplier.supply()+ActTokenIssuer.ActTokenRequest(audience=宿主 MCP URI,待 ia_tool_registry 提供);McpToolInvoker 端口已有 UnavailableMcpToolInvoker 占位,T2b-invoker(真实 McpClientToolInvoker,落 server,加 mcp SDK 依赖)排队等 T2a 释放主树 mvn 并给出端口签名;T2b-starter(独立工程 inneragent-starter/,act 验签走 JWKS HTTP 零 server 类依赖)已启动。**合并义务**:worktree 分支在 T1 后由主线合并+全量回归;T1 遗留=两处租户测试适配撤销(合并窗口做,先确认 AgentMessageAllocatorIT 是否走拦截器)+AdminTokenFilter 静态密钥 P2 换 client_credentials+本地旧 tenant_id=0 会话行拦截器后不可见(dev-only);V5/V6 迁移号合并时核对。worktree 落地后先 `git reset --hard main` 对齐(默认基于落后 origin/main)。
- P1 遗留台账(排序后落地):ia_storage_config 表缺;GlobalExceptionHandler 缺(BusinessException→500);reconnect 跨 run 应 4xx;模型用量结算缺 afv.modelCallId 注入(AgentScope GA 限制);netty macOS 告警。
- 启动方式:docker compose -f docker/dev-compose.yml up -d(15432/16379 被占时用 35432/36379 + IA_DB_PORT/IA_REDIS_PORT),`mvn spring-boot:run`,演示认证头 X-IA-Demo-User(默认 12993,仅 local profile)。dashscope starter 自动配置已在 yml 排除;swagger-annotations pinned 2.2.43。
