# 业务线 9 · 附件上传(R2 复测)

- 被测:Composer 附件入口 → 上传 → chip → 消息携带 → resourceUrl 读回/越权;超限与类型拒绝
- 用例文件:`e2e/specs/10-attachments.spec.ts`(3 用例)
- 结论:**3 通过,0 失败**。本线 R2 全部在 **demo-host 默认接入**下执行(DEF-05 修复后从补偿代理页切回);F14(消息区附件不渲染)/F15(25MB 500)/F16(url 回退静默)发现项维持,见 99-优化建议.md。

## 用例明细

| # | 用例 | R2 实际 | 判定 | 证据(assets/) |
| --- | --- | --- | --- | --- |
| 1 | 附件上传 → chip(缩略图+文件名)→ 消息携带 → resourceUrl 读回 | chip 缩略图 naturalWidth>0;附件登记行落库;运行已完成 + 全文收尾;`GET /attachments/{id}` 200 + image/png;他人读回 **404 不泄露存在性** | ✅ | `L9-02-附件chip(缩略图+文件名).png`、`L9-03-psql-附件登记行.txt`、`L9-05-resourceUrl读回-headers.txt`、`L9-06-越权读回-响应.txt`、`L9-04-消息完成后-消息区附件渲染观察.png` |
| 2 | 超限文件:>10MB url 回退(设计内);>20MB 拒收 → UI 反馈 | 15MB chip 正常(url 回退,无错误属设计内,F16 维持);25MB 服务端拒收 → composer 错误反馈可见、附件未加入 | ✅ | `L9-08-15MB传输回退.json`、`L9-09-25MB超限-UI错误反馈.png`、`L9-09-25MB错误反馈文案.txt` |
| 3 | 不支持媒体类型(.exe)→ UI 错误反馈 | 反馈文案含「不是支持的…」;附件未加入 | ✅ | `L9-10-不支持类型-UI错误反馈.png` |

## R2 校准说明(测试基建调整,断言不变)

- demo-host 默认 `agentType='ai_media'`(内置工具面无 get_current_time)→ legacy mock 走 FALLBACK_DELTAS,无收尾脚注。L9-01 以 `setMockScript` 驱动一个只读 MCP 目录工具(demo-spring-host `get_product_brief`)保证「工具执行 → 全文收尾」与 R1 断言语义一致;FOOTER/终态断言一字未改,finally 复位脚本。
