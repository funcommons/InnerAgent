# InnerAgent 未完成任务计划(续作手册)

> 目的:**上下文清空后,读完本文件即可继续干活**。记录时间:2026-09-22 17:10 前后。
> **2026-09-23 更新:§1/§2 全部完成;§7-1 provision 复核挖出真缺陷(Skill 注入静默失效)已修复+守卫 IT;agentscope 2.0.3 评估已定(暂不升)**。详见 §7/§5。
> 当前分支 main,HEAD 见 git log;所有会话成果已提交推送。
> 今日已完成 commits:`d00c521`(P0 依赖升级批次)→ `f4a7738`(provision.sh 公钥对齐修复)→ `907d2fb`(自建 Agent 管理页 + 服务端两处真修复)。

---

## 0. 快速恢复:环境与服务(当前全部在跑)

| 服务 | 端口 | 启动方式 | 账密/凭据 |
|---|---|---|---|
| InnerAgent 主服务 | 18090 | `java -jar inneragent-server/target/inneragent-server-0.1.0-SNAPSHOT.jar`(需 env,见下) | 管理头 `X-IA-Admin-Key: test-key`;管理站引导账号 `admin / Admin#12345` |
| InnerAgent 管理站(网关) | 18081 | `node e2e/gateway.mjs`(服务 web/dist + /ia 反代 18090) | 站内登录 admin / Admin#12345 |
| acme-demo 前端 | 9203 | `cd examples/acme-demo/frontend && pnpm dev`(后台日志 /tmp/acme-demo-frontend.log) | 控制台任意用户名轻登录,建议 alice |
| acme-demo 后端 | 9300 | backend 目录 `mvn spring-boot:run`(env 见下) | 演示会话制 |
| MCP echo 三方演示 | 9401 | `node examples/acme-demo/seeds/mcp-third-party/server.mjs`(已在跑,pid 14363) | 静态头 |
| PG / Redis | 35432 / 36379 | `source e2e/env.sh up` 起(容器 inneragent-postgres/inneragent-redis) | inneragent/inneragent |

**主服务启动 env(缺一不可,尤其 CORS)**:
```bash
cd inneragent-server && export JAVA_HOME=$(/usr/libexec/java_home -v 21) && \
IA_ADMIN_KEY=test-key IA_ADMIN_BOOTSTRAP_USERNAME=admin IA_ADMIN_BOOTSTRAP_PASSWORD='Admin#12345' \
IA_DB_PORT=35432 IA_REDIS_PORT=36379 \
IA_CORS_ALLOWED_ORIGINS=http://localhost:9203,http://localhost:18081 \
nohup java -jar target/inneragent-server-0.1.0-SNAPSHOT.jar > /tmp/ia-e2e-logs/server.log 2>&1 &
```
**demo 后端启动 env**:
```bash
cd examples/acme-demo/backend && export JAVA_HOME=$(/usr/libexec/java_home -v 21) && \
IA_SERVER_BASE=http://localhost:18090 IA_APP_KEY=acme-demo \
IA_SIGN_PRIVATE_KEY="$(cat ../seeds/.local/host.key)" \
IA_WEBHOOK_SECRET=whsec-9f2c1ab77d IA_ADMIN_KEY=test-key \
nohup mvn spring-boot:run > /tmp/acme-demo-backend.log 2>&1 &
```
- `env.sh` 已加固:CORS 缺省放行 `http://localhost:9203,http://localhost:18081`(9203=WC 跨源;**18081=管理台 iframe 的 origin,缺它管理台 iframe 内登录 403「Invalid CORS request」**)。
- 健康探针:`curl http://localhost:18090/.well-known/jwks.json` → 200;demo:`/api/demo/config` → 200。

---

## 1. 正在做(最高优先):管理台嵌入重构——收尾清单

### 1.1 背景与用户拍板(2026-09-22 下午,设计讨论结论)
- 用户口径:**「Skill/KB/MCP 是 InnerAgent 域的能力,不是 APP 端的;APP 只提供 APP 内部 MCP」**(业务工具经 @IaTool → /ia-mcp 桥,已在)。
- 用户三条拍板:① 管理台 iframe 内独立管理员登录,**不做 SSO**;② 自建的 Agent 管理页 + demo 后端代理**彻底删除**(git 历史留档);③ 入口与 总览/画廊/嵌入/工具 **侧栏并列**。
- 形态:**iframe 整站嵌入 18081,默认落 `/definitions`**(管理台自带侧栏覆盖 定义/MCP/Skill/KB/用量/反馈/审计/模型/熔断/webhooks/apps/tools)。
- 用户追加要求(见 §2):**DEMO 应演示中台的所有能力,没有的要补上**。

### 1.2 已完成(工作区未提交改动,均为本任务)
- **删除**(已 `git rm` 暂存):`examples/acme-demo/backend/src/main/java/fun/commons/acmedemo/web/AgentAdminController.java`、`.../ia/IaAgentAdminService.java`、两个对应测试、`frontend/src/views/ia/AgentAdmin.vue`、`frontend/src/views/ia/__tests__/AgentAdmin.test.ts`、`frontend/src/api/__tests__/iaAgentAdmin.test.ts`。
- **还原**:`backend/.../config/WebConfig.java`(去掉 agent-admin 拦截路径)、`backend/.../ia/InnerAgentAdminClient.java`(去掉 getEnvelope/getEnvelopeAsUser/postEnvelope 与 JsonNode import)。
- **新增**:`frontend/src/views/ia/AdminEmbed.vue`(iframe 整站,bar 含提示文案+新窗口打开,常量 `IA_ADMIN_CONSOLE_URL='http://localhost:18081'` 在 `frontend/src/api/ia.ts` 尾部)。
- **修改**:`frontend/src/api/ia.ts`(删自建代理死代码段)、`router/index.ts`(`/ia/admin` → AdminEmbed.vue, name=IaAdminEmbed, icon Setting)、`composables/useSidebarNavItems.ts`(`/ia/admin` 并列入口)、`locales/zh-CN.ts`+`en-US.ts`(router 键 `ia-admin`=「InnerAgent 管理台」;删 ia.agentAdmin.* 整块;新增 ia.adminEmbed.{title,hint,new-tab,frame-title})、`composables/__tests__/useSidebarNavItems.test.ts`(路径断言更新)。
- **修改**:`e2e/env.sh`(CORS 缺省两来源,见 §0)。
- **验证已过**:前端 `npx vitest run` → **473/473 绿**;demo 后端 `mvn test` → **35/35 绿** 且已重启(代理端点已不在)。
- **截图**:`test-report/2026-09-22-02/` 01-02(旧自建页,可删)、03(嵌入初始=iframe 内管理站登录页,✅ 姿态正确)、04(登录尝试中间态,**未拿到登录成功版**)。

### 1.3 剩余步骤(按序执行)——✅ 全部完成(2026-09-22 晚)
1. ✅ 主服务已带两来源 CORS 重启(预检 18081/9203 均 200)。
2. ✅ 04 截图已补(`04-admin-embed-definitions.png`,iframe 内登录后定义页)。
3. ✅ 已提交 `5f8a66d`。
4. ✅ 01/02 旧图已删。
5. ✅ 记忆文件已更新(嵌入定稿 + iframe origin=18081 CORS 教训)。

### 1.4 保留不动(勿回滚)
- `inneragent-server/.../server/admin/AgentDefinitionAdminService.java` 的 `AppContext.runAsSystem` 包裹(907d2fb):管理面定义读写显式 appId 提供范围,**修的是无上下文线程上 updateById 被注入 app_id=1 致 overwrite 更新静默落空**(provision 此前靠泄漏上下文碰巧成功)。
- `inneragent-server/.../model/provider/OpenAiResponsesAgentScopeModel.java` 的 `toolCallNameFallback`(907d2fb):openai-java 4.67 移除 arguments.done 事件 name 字段的兼容。
- **smoke-agent**(ia_agent_definition id=27, app 34, enabled=false「冒烟测试 Agent」):演示产物,留着无害;是否删除待用户表态。

---

## 2. 下一任务(用户追加硬要求):中台能力全覆盖盘点与补缺

> 用户原话:**「加一条,DEMO 应演示中台的所有能力,没有的要补上」**。
> 架构前提(已定):能力管理 UI 归 InnerAgent 管理台(嵌入即覆盖);宿主侧演示接入模式。覆盖 = demo 里每项中台能力都有「可体验的入口」。

### 2.1 中台能力面(管理台 12 页,嵌入即覆盖)
`web/src/router/index.ts` 路由:/apps(应用/公钥/webhook 配置)、/tools(工具注册表)、/definitions(Agent 定义+提示词+导入导出)、/mcp-servers(三方 MCP)、/skills(Skill zip/激活)、/kb(知识库文档/分块)、/usage(用量)、/feedbacks(反馈)、/audit(审计)、/models(模型/连通性)、/circuit(熔断/紧急停用/limits)、/webhooks(投递队列)。
**这些由 /ia/admin 嵌入页整站覆盖——盘点时逐页打开确认有数据、不空白即可。**

### 2.2 宿主侧已演示能力(demo 各页)
- 对话运行(SSE 流式、多会话、MiniMax-M3 真模型):`/ia/embed?agentType=X`(深链选场景;WC 里 Enter=换行要点「发送」)
- 确认流(WRITE 工具):ticket-assistant 说「帮我建一张工单…」→ 确认卡 → /ia/tools 出 channel=agent 记录
- 宿主桥 MCP(4 工具)+ 三方 MCP(acme-echo)+ webhook 事件流:/ia/tools
- Skill(report-style)/ KB 引用([KB:184] 标记):report-writer / knowledge-qa 对话
- 子 Agent(2 层):ops-analyst、master-demo
- Embed 双模式(WC 直挂 / iframe postMessage)+ tokenGetter:/ia/embed
- embed token 签发/claims 可视化:/playground
- 接入自检五步:/ia/overview

### 2.3 候选缺口(盘点起点,逐项核真)
1. **能力覆盖地图页**:建议在 `/ia/overview`(总览)或 `/docs` 加「中台能力 ↔ demo 体验入口」对照表(约 14-16 行:对话/工具确认流/MCP/Skill/KB/子Agent/模型管理/用量/反馈/审计/熔断/webhook/多应用/嵌入双模式),每行给「在哪体验」链接。这是覆盖要求的**验收工件**。
2. **SSE 断点续传(Last-Event-ID)**:内核有(IaCorsConfiguration 允许头、断线恢复),demo 无可视化演示 → 建议以覆盖表「内核保障(E2E/IT 覆盖)」标注,不做 UI。
3. **用户面 Skill API**(`/ia/api/v1/skills`,K 交接项):管理台/内核已通,宿主演示可选;若盘点发现管理台 Skill 页已覆盖激活语义,标注即可。
4. **工具授权 grants**(`/admin/grants` 端点存在):确认管理台 tools 页是否含授权视图;若管理台没有对应 UI,属「中台自身缺口」记录台账,不是 demo 缺口。
5. **多租户(tenantId claim)**:embed token 已支持(体验台可传);标注即可。
6. **管理台各页空数据风险**:demo app=34 数据齐全(8 定义/11 工具/skill=2/KB 3 篇 21 分块/用量反馈有数据),但 /apps /models /circuit /webhooks /audit 页要在嵌入页里逐页点开验一遍——**/apps 页与 /models 页是全局视角(acme-demo 行在列)**;发现空白页先查数据再查 bug。

### 2.4 验收口径 —— ✅ 全部达成(2026-09-22 晚)
- ✅ **覆盖表已上线** `/ia/overview` 新增「中台能力 ↔ DEMO 体验入口」表(19 行:对话/确认流/工具事件流/MCP/Skill/KB/子Agent/嵌入双模式/playground/管理台整站/多应用/grants/模型/用量/反馈/审计/熔断/webhook/SSE续传),每行要么站内链接要么「内核保障」标注;组件字面量沿用 mapping 表范式,i18n 只加表头/标题键;新测 `Overview.test.ts` 3 用例(行数≥16、链接白名单、核实项在表)。
- ✅ **管理台 12 页 iframe 逐页截图无异常**(web/dist 重建后 playwright 走查,`test-report/2026-09-22-02/assets/admin-01~14`):definitions 18 条(跨应用)/apps 2/default+acme-demo 同指纹/tools 12/mcp-servers acme-echo/skills report-style 已激活/kb 3/usage 1/audit 10/models 2(MiniMax+Mock,密钥脱敏)/circuit 表单齐/webhooks 10/工具授权 Tab 2 条代授;feedbacks 空态但渲染正常。覆盖表整页截图 `15/16-overview-coverage-*`。
- ✅ 前端 vitest **476/476 绿**(+3)+ vue-tsc 干净;产物提交。

### 2.5 盘点中发现的观察(记录,不阻断)
- **管理台数据视角不一致**:定义(18 条)/工具为跨应用聚合;用量/反馈「appId 由服务端按缺省应用行级注入」(AdminFeedbackController 注释)——acme-demo(34)的反馈明细在管理台不可见(放宽时间窗仍 0)。属管理台自身口径,记台账。
- **IAB(浏览器技能)guest 进程会整体僵化**:表现为截图报「capture failed for guest」+ cua.click/keypress 事件不到达页面(插桩 window 事件为空),CDP 层 fill/evaluate 仍通;新开标签页同样复现。本轮改用项目自带 Playwright(headless chromium)完成走查——后续浏览器走查可直接走这条路。
- web/dist 是 gitignore 构建产物,gateway 即时读盘;**走查前先 `cd web && pnpm build`**(本轮 dist 曾落后 src 5 小时)。

### 2.6 下一步(见 §7)

---

## 3. 关键事实(跨任务有效)

- **应用**:appKey=`acme-demo`,app_id=34;embed token:RS256,iss=acme-demo,sub=用户数字 ID,exp 12h,无 aud;演示私钥 `examples/acme-demo/seeds/.local/host.key`(gitignore,**指纹 39780d49…,与 DB ia_app.sign_public_key 已对齐**——provision.sh 现会自动指纹比对+PUT 轮换)。
- **模型**:MiniMax-M3 为默认对话模型(ia_ai_model id=2,api_config id=19 anthropic 协议);model_protocol 必须填 anthropic。
- **种子**:`zsh examples/acme-demo/seeds/provision.sh`(幂等;zsh 不是 bash)。
- **测试基线**:inneragent-server surefire 849 + failsafe 138(`mvn verify`,~13min);web(管理站)473?否——**demo 前端 473**;管理站 web 测试在 `web/`(与 demo 无关);starter 60;demo 后端 35;E2E 65(`cd e2e && npx playwright test`)。
- **依赖现状**:Boot 3.5.16 / MP 3.5.17 / anthropic-java 2.64 / openai-java 4.67 / dashscope 2.23.1 / otel 1.66 / bcprov 1.86 / springdoc 2.8.17(swagger-annotations 钉 2.2.47 对齐);**MCP SDK server=0.17.0 钉死不动,starter=2.0.1**;agentscope 2.0.0(2.0.3 有 subagent-bugfix 待评估,升级需全量回归+golden-diff)。

## 4. 已知坑与教训(必读,全是真机踩出来的)

1. **PageResult 的 JSON 字段是 `list` 不是 `records`**——先核对字段名再断言「接口坏了」(本轮误诊根源)。
2. **iframe 内页面 origin 是 iframe 的 src(18081),不是外层页(9203)**——CORS 按 18081 放行。
3. **`python3 -c` 不预载 sys 名字**(系统 3.9.6 实测):单行脚本用 sys.argv 必须显式 `import sys`。
4. **阿里云 maven 镜像同步滞后数小时**:新版本首轮解析失败会被 ~/.m2 缓存,重试必须 `mvn -U`。
5. **MP 3.5.17**:①列解析提前到 wrapper 构造期,单测要手动 `TableInfoHelper.initTableInfo`;②starter 不再传递 mybatis-plus-extension,`extension.spring.MybatisSqlSessionFactoryBean` 已从所有工件移除(挪到 mybatis-plus-spring,包名改)。
6. **增量编译会掩盖依赖 API 漂移**:改 pom 后必须触发一次全量重编译(改同模块任一文件即可)再验证。
7. **场景切换用深链 `/ia/embed?agentType=X`**(useLocalStorage 是 JSON 序列化,裸字符串无效);WC 发送点「发送」按钮(Enter=换行)。
8. 起 server 前核对 jar 时间戳 vs HEAD;改 server 代码后要重启进程才生效(jar 在 verify 的 package 阶段产出)。
9. Reactor 全局调度器/线程池会丢上下文(U 批修了大半);**管理面写路径已用 runAsSystem+显式 appId 加固**,新增管理面服务照此模式。

## 5. 长期台账(非阻塞,需用户拍板或运营动作)

- 压测回填容量报告(xk6 SSE 需装 Go;tools/k6 13 文件已备)
- mmagix-minicuts 冻结未解:融光删除批次①② + RG-1 指标出口待冻结解除
- P3 灰度切流 W9-W12(5%→25%→50%→100%,升档前 golden-diff)——运营动作
- greeting 承接点、跨租户系统调度(webhook/toolhub)runAsSystem 补齐、内核技能内建工具溢出白名单待产品定夺
- ~~provision.sh 按目标应用 provisioning KB/Skill~~ ✅ **2026-09-23 定稿并修复**(见 §7 记录):Skill/KB 管理面显式 appId 面 + runAsSystem;provision 已按目标应用 provisioning,embed 上下文技能注入活体回归
- ~~agentscope 2.0.3 升级评估~~ ✅ **2026-09-23 定论:暂不升级**(见 §7)
- smoke-agent(ia_agent_definition id=27,app 34,enabled=false)演示遗留,删除与否待用户拍板

## 6. 续作第一步(给清空后的自己)

1. 读本文件 §0 起 服务 → §1.3 按 5 步收尾嵌入重构并提交;
2. 转入 §2:逐页打开管理台 12 页验证 → 写能力覆盖表 → 补缺口 → 全量回归 → 提交;
3. 汇报时附截图(test-report/2026-09-22-02/)。

---

## 7. 续作记录与剩余项(2026-09-23)

### 7.1 provision.sh 按目标应用 provisioning 复核 → 挖出真缺陷并修复 ✅

**缺陷(活体证据链)**:embed 过滤器把运行态 AppContext 设为宿主应用(34),`activatedSkills` 严格按 app_id 过滤;而 Skill/KB 管理面此前无(有效)显式 appId 面——X-IA-Admin-Key 请求无线程上下文,行级拦截器注入 `app_id=1`:①ia_skill 的显式 appId≠1 读写被叠加成 `app_id=34 AND app_id=1` 恒空(admin list appId=34 返回 0 的实测);②KB 控制器干脆只有 currentOrDefault。净效果:**provision 落到缺省应用(1),report-writer 场景的 Skill 注入静默失效**(修复前 embed 上下文 reference-options 实测无 report-style)。

**修复(同 definitions 先例)**:
- `AppSkillCatalogService` 管理面六方法(import/page/get/activate/deactivate/delete)包 `AppContext.runAsSystem`(系统模式跳过注入,显式条件管范围);运行态 activatedSkills/listActive 不包裹,隔离不弱化。
- `AdminKbController` 九端点全部补 `@RequestParam(defaultValue="1") long appId` + runAsSystem 包裹。
- `provision.sh`:Skill 导入/计数与 KB 列表/导入/更新/检索全部传 `appId=${APP_ID}`(顺手修掉编辑中丢失的 overwrite=true);更新过时注释。

**验证**:真库守卫 IT `SkillAdminAppScopeIT` 4 用例(导入落 34/page 跨应用隔离/激活后仅 34 运行态可见/同名覆盖幂等)全绿;全量回归 **surefire 849/849 + failsafe 142/142(138+4)BUILD SUCCESS**;重跑 provision 后 **embed 上下文 reference-options 实测 report-style(import)回归**,DB 终态 app 34 名下 skill id=1 active + KB 三篇。

**遗留观察(记台账,非阻断)**:管理台 Skill/KB 页仍按缺省应用(1)查询,看到的是 app 1 的陈旧副本(app 1 名下 report-style id=2 active)——根因同「管理台无应用切换器」台账项,待产品定夺;app 1 旧副本无害。

### 7.2 agentscope 2.0.3 升级评估 ✅ 定论:暂不升级

方法论:本地 `dependency:get` 2.0.3 五工件(core/harness/openai/anthropic/dashscope),与 2.0.0 逐类 md5 对比,再与本项目消费面(102 个 import 类,server+starter)求交。

- 2.0.3 变更 218 类 + 移除 2 类(harness 内部);新增:周期门控 coordination、ArtifactDelivery、RoutedSandboxFilesystem、SessionTranscriptWriter、TeamsMiddleware、SubagentsMiddleware 快照、ToolResultEviction 等。
- **本项目消费的 102 个类在 2.0.3 逐字节未变,移除的 2 类也与本项目无关**。
- 「2.0.2-subagent-bugfix 是否覆盖我们自研的子 Agent 级联取消」:**不覆盖也无需覆盖**——我们的级联取消是平台层自研(一级子运行行 + `CancellationCoordinator.cancelChildren(parentRunId)`,见 PlatformSubAgentRunService),不经由上游 SubagentsMiddleware/WorkspaceTaskRepository(上游修复区)。
- 升级真实风险:消费类字节未变 ≠ 行为不变——AgentScopeAgent 等内部调用链可能触达 ReActAgent 等变更类,事件形态漂移会影响 AgentScopeEventMapper;另有传递依赖漂移(okhttp/okio 钉版联动)。收益:仅当需要上游新能力(SessionTranscriptWriter/ArtifactDelivery/周期门控)时才有意义。
- **结论:留 2.0.0;需要上游新能力时按「全量回归+golden-diff」流程一次性升 2.0.3。**

### 7.3 管理台应用上下文切换器 ✅(2026-09-23,§7.1 修复的配套收口)

- `web/src/stores/appContext.ts`(新):候选应用列表(localStorage 持久化选中 id,失效回落缺省应用 1)。
- AdminLayout 顶栏 el-select 切换器(路由切换时静默刷新候选);Skill/知识库/用量/反馈四视图 watch 上下文变化重置分页重查;KB 全端点、Skill 导入、用量/北极星/反馈查询显式携带 appId。
- 验证:web vitest 232/232(+4 appContext 用例)+ vue-tsc 净;真机断言切到 acme-demo 后 `/admin/skills|kb/documents|feedbacks|usage/north-star` 全部带 `appId=34`,Skill 目录显示 app 34 的已激活 report-style(截图 17-admin-app-switcher-skills.png)。

### 7.4 剩余待办(均需拍板/运营,非代码)

1. smoke-agent 删留(§5);2. 压测容量回填(需装 Go);3. minicuts 冻结解除后融光删除批次;4. P3 灰度切流(运营)。
