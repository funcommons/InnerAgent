# R2 · 复测分册(第 2 轮 · 修复验证轮)

- 轮次:/loop 测试循环 第 2 轮(R1 七缺陷修复后的全量复测)
- 日期:2026-09-21 · 执行:E2E 复测 agent(只测不改产品代码;e2e/** 测试基建按本轮明确调整点调整)
- 结论:**12 个 spec · 64 用例,两轮全量连跑均 64/64 全绿(2.2m/轮);R1 七缺陷(DEF-01~07)全部关闭,0 REGRESSED,0 新缺陷**;补偿段(SDK_BARE_PREFIX + 宿主页 baseURL:'')已删除并在无补偿态复跑通过
- 收官文档:`00-统一测试报告.md`(十线拆解/全程叙事/缺陷闭环/已知缺口)、`99-优化建议.md`(四维 26 条)
- 明细:01-认证 · 02-应用管理 · 03-工具注册与分诊 · 04-授权管理 · 05-审计检索 · 06-模型配置 · 07-SDK对话链路 · 08-断流重连 · 09-附件上传 · 10-工具确认UI · assets/(206 份证据)

---

## 1. 环境搭建结论(相对 R1 的差异点)

| 项 | 结果 |
| --- | --- |
| 产物新鲜度 | ✅ **全部强制重建**:server jar 05:34(旧 jar 00:37 不含 DEF-02/03/04/07 服务端修复)、web dist 05:43(DEF-01 后)、sdk-js WC dist 05:43(DEF-05/06/07 后)。**若沿用旧 jar 将全军覆没**(见下条) |
| 数据卷 | ⚠️ **全新 volume**:R1 保留的卷停在一个无 V11 的迁移历史(V11__webhook_delivery 为 18b 合并产物),与含 V11/V13 的新 jar 无法通过 Flyway 校验(Detected resolved migration not applied: 11)。`down -v` 重建后 V1..V13 全量应用、V4 演示 seed 重播、`uk_ia_tool_grant_active`(V13,DEF-04 修复面)实证存在 |
| compose 错峰 | ✅ 35432/36379(e2e-ports 覆盖);server 18090(IA_ADMIN_KEY=test-key + bootstrap admin/Admin#12345,Argon2id 引导建号日志在案) |
| web 真服务模式 | ✅ env.sh:pnpm build 产物 + gateway.mjs(18081 静态 + /ia 反代);生产构建无 msw |
| 辅助进程 | ✅ demo-host vite(5180,`node_modules/.bin/vite` 直起)、demo-spring-host(18091,JDK21)、reconnect-host(18085) |
| 六工具注册 | ✅ 经 admin API 注册 demo-spring-host 六工具(get_host_time / create_host_record / get_product_brief / update_product_brief / copy_flow_template / list_login_records,endpointUrl=http://localhost:18091/ia-mcp,annotationsJson readOnlyHint 齐备),200×6,证据 `assets/R2-00-工具注册表.txt` |
| 匿名演示链路 | ✅ `/me/models?type=1` 无凭据(X-IA-Demo-User / 缺省 12993)→ 200 |

## 2. 本轮 e2e 基建调整清单(全在 e2e/**,产品代码零改动)

| # | 调整 | 文件 | 动机 |
| --- | --- | --- | --- |
| A1 | 证据目录 2026-09-21-01 → **2026-09-21-02** | `helpers/support.ts` | R2 证据独立落目录,R1 资产保持原样供回引 |
| A2 | **specs 10/11/12 切换 RECONNECT_HOST → DEMO_HOST 默认接入**(任务口径) | `specs/10-attachments.spec.ts`、`11-tool-confirm.spec.ts`、`12-admin-authz-ui.spec.ts` | DEF-05 修复后默认 baseURL 可用,补偿代理页不再必要;10/11/12 与 spec 08 的 L7-01/07 共同构成「默认接入」回归面 |
| A3 | spec 08 L7-02~06 **维持**可断中转页(非任务切换项) | `specs/08-sdk-chat.spec.ts` | 其断言以 `agentType:'demo'` 工具链为基准(get_current_time chip/脚注);demo-host 示例未声明 agentType(默认 ai_media)属**环境差异**而非产品缺陷 → 中转页保留 demo 口径 |
| A4 | 宿主页 `baseURL:''` 补偿删除 → SDK 默认 baseURL | `e2e/sdk-reconnect-page.html` | DEF-05 修复后请求自带 /ia 前缀,不再依赖网关改写 |
| A5 | **SDK_BARE_PREFIX 裸路径重写段删除** | `e2e/reconnect-host.mjs` | 同上;删除后中转为纯透传(断流开关保留) |
| A6 | **新增 DEF-03 回归用例**(L3-13 注销→重注册+审计 tool_revived) | `specs/03-tools.spec.ts` | R1 管理站分册 §3 修复意见与 R2 交接「全查点」要求的回归用例,R1 未建;物理清理降级为保险 |
| A7 | L9-01 增加 mockScript 驱动(R2 校准,断言不变) | `specs/10-attachments.spec.ts` | DEMO_HOST 的 ai_media 工具面无 get_current_time,legacy mock 无收尾脚注;以只读 MCP 工具驱动「工具执行→全文」保持 FOOTER 断言语义 |
| A8 | DEF-07 网关补偿核验 | `e2e/reconnect-host.mjs` | 入场核验:修复批次已移除 `[rewrite] enabledMcpTools` 段(本轮跑前跑后日志均 0 条),仅剩 A5 所删裸路径重写段 |

## 3. 用例统计(64 用例,两轮全绿)

| 业务线 | spec | 用例 | 通过 | 复测对象 |
| --- | --- | --- | --- | --- |
| 1 认证 | 01-auth.spec.ts | 9 | 9 | DEF-01 回归(L1-08) |
| 2 应用管理 | 02-apps.spec.ts | 5 | 5 | — |
| 3 工具注册与分诊 | 03-tools.spec.ts | **11**(+1) | 11 | DEF-02(L3-05b)、**DEF-03 新增回归(L3-13)** |
| 4 授权管理 | 04-grants.spec.ts | 5 | 5 | — |
| 5 审计检索 | 05-audit.spec.ts | 7 | 7 | DEF-04 回归(L5-07) |
| 6 模型配置 | 06-models.spec.ts | 6 | 6 | — |
| 已知缺口记录 | 07-mockdomains.spec.ts | 2 | 2 | 熔断/Webhook 页维持 404(服务端未排期,不定级) |
| 7 SDK 对话链路 | 08-sdk-chat.spec.ts | 7 | 7 | DEF-05 回归(L7-07,demo-host 直测) |
| 8 断流重连 | 09-stream-reconnect.spec.ts | 2 | 2 | DEF-06 回归(L8-06)、L8-01 无条件全文断言 |
| 9 附件上传 | 10-attachments.spec.ts | 3 | 3 | DEF-05 默认接入(A2/A7) |
| 10 工具确认 UI | 11-tool-confirm.spec.ts | 4 | 4 | **DEF-07 无补偿回归**(默认接入 + 0 网关改写) |
| 11 管理鉴权 UI(轻量线) | 12-admin-authz-ui.spec.ts | 3 | 3 | L11-03 匿名链路切默认接入 |
| **合计** | | **64** | **64** | 连跑两轮 2.2m/轮 |

- 跑法:`npx playwright test`(workers=1 串行,`test-report/2026-09-21-02/assets` 证据落盘;失败留 trace/video/console 于 `e2e/test-results/`,本轮终态无失败)。
- **DEF-07 服务端侧证**:全轮 server 日志「不在 toolkit」WARN 计数 = **0**(R1 同场景为必现 WARN)。

## 4. 补偿段清理结论(任务核心口径)

1. **DEF-07 网关补偿**:修复批次已移除;本轮入场核验 + 全程日志 0 条 `[rewrite] enabledMcpTools` ✅
2. **specs 10/11/12 → DEMO_HOST 默认接入**:切换后两轮全绿 ✅(未触发「保留该段」的兜底条件)
3. **SDK_BARE_PREFIX 裸路径重写段**:**已删除**(连同居于宿主页的 `baseURL:''`)。删除前跑 1:日志含 46 条 `[rewrite]`(仅 spec 08 L7-02~06 与 09 的中转页在用);删除宿主页补偿并重启中转后跑 2(全量 12 spec):**`[rewrite]` 0 条,`enabledMcpTools` 0 条**,断流线 `[cut]`×6、`[reconn]`×2 照常 —— **中转已是纯透传,断流开关是其唯一「非透传」行为(测试仪器,非补偿)**。
4. 遗留说明:spec 08 L7-02~06 仍以中转页为宿主(A3,agentType 口径差异),属**环境差异待后续清理** —— 待 demo-host 示例声明 `agentType`(产品侧一行,examples 只读)或 SDK 默认 agentType 调整后即可全量切 demo-host;届时 08 的 L7-02~06 与 09 共用的中转页仅剩断流线的存在必要。
5. 附带收获:10/11 两线在默认接入下复测,使 **DEF-05 + DEF-07 的回归口径较 R1 收严一档**(R1 是「补偿后可用」,R2 是「零补偿可用」)。

## 5. R1 交接验证点全查结果

| 交接点(R1 分册 §7) | R2 结果 |
| --- | --- |
| DEF-03 复活路径:注销→重注册生效 + 审计 tool_revived,物理清理仅保险 | ✅ L3-13(新增回归):复活同 id、详情/分诊可达、audit decision=tool_revived(source=forced-policy)恰 1 条 |
| DEF-02:指纹不空写 | ✅ L3-05b:unchanged(schema_not_resent),effectiveSchemaSha256 保持现库值,历史留痕 silent_refresh |
| DEF-04 三态(撤销 200/重授 200/重复 409) | ✅ L5-07 + L4-03 校验矩阵;V13 `uk_ia_tool_grant_active` 在全新卷实证存在 |
| L8-01 重放游标对齐断言(修 DEF-06 时同步复核) | ✅ 路径 A:重放起点=游标+1 连续、覆盖游标后全部可见事件、直达 DONE(`L8-ui-sse-id与库对齐.json`) |
| 断流用例 run 存活期 > SDK 5s 重试延迟 | ✅ 两用例以 8s 断流窗 + legacy mock(5 deltas×800ms+工具轮)构造,均确定性收敛 |
| 失败后 worker 模块重求值 / 固定标识符+每用例自建前置 / 物理清理 | ✅ 沿用;两轮连跑零污染复跑通过 |
| Playwright 流式响应体不可读 → 页内 fetch 包装 | ✅ captureSse 沿用,SSE 证据完整 |

## 6. 清理确认(证据落 `assets/R2-99-*.txt`)

- 进程/端口:`source e2e/env.sh down` + 辅助进程(reconnect-host 18085 / demo-host vite 5180 / demo-spring-host 18091)全部停止;**18090/18081/18085/5180/18091/35432/36379 七端口全部无监听**,inneragent 容器 0,辅助进程 0。
- 数据(物理清理终检,`assets/R2-99-数据清理终检.txt`):ia_tool_registry **0**、ia_tool_grant **0**、ia_agent_conversation/run/message/event/attachment **0**、ia_ai_model.config 非空 **0**(mockScript 全复位)、管理账号锁定态复位(failed_attempts=0);管理线留痕(登录审计/授权审计)与 R1 同口径保留于 dev 卷。
- compose:down(容器与网络移除,**volume 保留** —— 本轮重建的卷为 V1..V13 完整迁移态,含 V4 演示 seed,后续轮次可直接复用;R1 保留卷的惯例不变)。
- git:仅 e2e/** 测试基建调整与 test-report/2026-09-21-02/**;建议提交前缀 `test(e2e):`;**未 push**。

## 7. 循环判定

**R2 复测轮通过,循环可结束**:64/64 全绿 ×2、七缺陷全部关闭、0 REGRESSED、0 新缺陷(P0~P3 均无)、补偿段清理完成且复跑通过、环境清理取证完毕。余量工作全部为**产品优化建议**(见 99-优化建议.md)与**环境差异待清理**(§4-4),均不构成缺陷。
