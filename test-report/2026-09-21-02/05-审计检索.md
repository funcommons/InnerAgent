# 业务线 5 · 管理站-审计检索(R2 复测)

- 被测:`AdminAuditController`(分页/过滤/脱敏)+ `ToolGrantService` 生命周期(DEF-04 修复面)
- 用例文件:`e2e/specs/05-audit.spec.ts`(7 用例,其中 1 例为 DEF-04 修复反转回归)
- 结论:**7 通过,0 失败,0 REGRESSED**。DEF-04 关闭:撤销→同键重授 200;活跃重复授予 409(不再裸 500)。

## 用例明细

| # | 用例 | R2 实际 | 判定 | 证据(assets/) |
| --- | --- | --- | --- | --- |
| 1 | 分页:total 正确,翻页 | 一致 | ✅ | `L5-01-审计分页-第1页10行.png`、`L5-01-分页-服务端.json` |
| 2 | decision / decisionSource 过滤 | 一致(服务端与 UI 一致) | ✅ | `L5-02-decision过滤-服务端.json` |
| 3 | 时间过滤:from 排除历史行;UI 触发 ISO 查询 | 一致(R1 F9「UI 键入不触发查询」在 R2 同样未复现为阻断,该发现项维持,见 99-优化建议.md) | ✅ | `L5-03-时间过滤-服务端.json`、`L5-03-时间过滤-UI未触发请求(发现项).png` |
| 4 | 敏感参数脱敏:库内明文,出参/UI 一律 *** | 一致 | ✅ | `L5-04-脱敏-出参.json` |
| 5 | **DEF-04 回归**:撤销后同键重授 → 200;活跃重复 → 409 | revoke=200 → reGrant=**200(code=0,新授权行落库)**;活跃行重复授予 → **409**(业务冲突语义,非 500) | ✅ **缺陷关闭** | `L5-07-DEF04-撤销后重授200.json`、重复 409 断言于用例内通过(409 形态另见 `L4-03-授予校验矩阵.json`) |
| 6 | decision_source 值域:字典含 expired,web 下拉缺失 | 服务端 expired 在值域;web 下拉仍缺该档(F5 维持,发现项不定级) | ✅ | `L5-05-值域对比.json` |
| 7 | 真实写路径审计:granted/revoked 落库可检索 | 一致 | ✅ | `L5-06-granted审计行.json` |

## DEF-04 闭环

- R1 发现:撤销(逻辑删)后同键重授 500 —— `uk_ia_tool_grant` 不含 deleted 谓词,死行占键(P1)。
- 修复提交:**fea46de**(V13 部分唯一索引 `uk_ia_tool_grant_active WHERE deleted=false` + GlobalExceptionHandler 约束冲突映射 409)。
- R2 复测:L5-07 反转为「200/409 三态」回归后通过(全新 volume 上 V13 索引实证存在);SDK live-confirm 再确认同键域在 L10 线同样无 500 暴露。**判定:关闭**。
