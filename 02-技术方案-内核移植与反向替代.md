# InnerAgent 技术方案(内核移植与反向替代)

> **版本 V1.1 · 2026-09-20** · 依据《01-PRD V1.3》与《04-开源与大厂实现对标调研》修订:工具策略字段与 schema 活刷新分诊、授权表 ia_tool_grant、SSE 事件补强(自动决策标记/拒绝终态/delta 自标识)、/ia-mcp 无状态化、act token claims 按 RFC 8693 规范化、Q1 关闭(官方 MCP Java SDK)、确认幂等三条不变式、附件端点、管理站认证、事件 schema 版本化。

## 目录

- 1. 定位与目标
- 2. 现状资产盘点(mmagix-minicuts 侧)
- 3. 总体架构
- 4. 后端内核移植设计
- 5. 数据模型
- 6. 凭据与认证
- 7. 接口设计
- 8. 前端与 SDK
- 9. 反向替代融光内置 Agent(核心)
- 10. 安全设计清单
- 11. 代码改造清单(文件级)
- 12. 分阶段落地计划
- 13. 关键设计决策记录(ADR)
- 14. 开放问题

---

## 1. 定位与目标

### 1.1 本方案要解决的问题

1. **移植**:把 mmagix-minicuts(下称「融光」,仓库 `ai-fusion-video`)中已经过工程验证的 Agent 内核(持久化运行、事件日志、确认流、Skill、子 Agent、模型接入)抽取为 InnerAgent 独立微服务的前后端代码基座,不重写内核。
2. **去业务化**:剥离融光业务工具(剧本/分镜/素材/生成)与业务数据模型,业务能力一律改为经 MCP 从宿主应用接入。
3. **反向替代**:InnerAgent 成熟后,融光自身成为第一个宿主应用——融光后端把既有 30+ `ToolExecutor` 经 MCP Server 桥接暴露,融光前端弃用内置 Agent 工作区、改嵌 InnerAgent SDK,最终**删除融光仓库中的内置 Agent 引擎代码**。

### 1.2 与 PRD 的关系

本方案是《01-产品需求文档-PRD》的技术实现方案;PRD 中标注「默认值见技术方案」的参数在 §4.7 给出;PRD 的 M0–M3 里程碑与本方案 §12 的阶段映射。

### 1.3 三条总原则

| 原则 | 含义 |
| --- | --- |
| 移植优先 | 内核代码以「复制 + 定向改造」迁移,保持类名与结构可追溯,禁止借移植之名重写 |
| 行为等价 | 双跑期间 InnerAgent 与融光内置引擎对同一输入产生等价的运行行为(SSE 事件序列、确认语义、消息投影),以对照测试验证 |
| 可回滚 | 反向替代全程灰度,前端开关一键切回内置引擎;删除融光代码是最后一步且在 100% 切流稳定后执行 |

---

## 2. 现状资产盘点(mmagix-minicuts 侧)

### 2.1 后端资产(`ai-fusion-video/src/main/java/com/stonewu/fusion/`)

`service/ai/` 共 **248 个 Java 文件**:

| 包 | 文件数 | 内容 | 移植性 |
| --- | --- | --- | --- |
| `service/ai/run/` | 75 | 持久化运行引擎:RunCoordinator、lease/epoch、事件日志与 Outbox、回放、消息投影、确认/取消协调、用量结算适配 | ✅ 整包移植 |
| `service/ai/agentscope/`(含 `kernel/state/workspace/mcp/skill/context` 子包) | 71 | AgentScope Harness 工厂、内核缓存与指纹、工具适配器、子 Agent 适配器、状态存储(fail-closed)、工作区三后端、MCP 注册表、Skill 注册表、上下文对象 | ✅ 整包移植,身份对象改造(§4.2) |
| `service/ai/provider/` | 14 | OpenAI 兼容/Anthropic/Gemini/DashScope/Ollama/Vertex Provider | ✅ 移植(GoogleFlowReverseApiProvider 除外) |
| `service/ai/` 根 | 14 | AgentConversationService、AgentMessageService、AiAgentService、AiModelService、ApiConfigService、AiToolConfigService、ChatModelFactory、ModelPresetService、AiStreamRedisService、ToolExecutor SPI 及注册表 | ✅ 移植;ToolExecutor SPI 仅保留给内置工具(§4.3) |
| `service/ai/tool/` | 43 | 融光业务工具执行器(项目/剧本/分镜/素材/生成)+ `ToolResourceAccessGuard` | ❌ 不移植,留融光(经 MCP 桥继续服务) |
| `service/ai/comfyui、googleflow、dashscope、model、plan、proxy、onetoken` | 29 | 图片/视频生成与计费相关 | ❌ 不移植(融光业务;onetoken 结算为 InnerAgent P2) |
| `entity/ai/` | 15 | Agent 系列 11 张 + AiModel + ApiConfig + ComfyUI 3 张 | ✅ 移植 Agent 系列 11 张与 AiModel/ApiConfig;ComfyUI 不移植 |
| `controller/` | 5 | AiPipelineController、AiAssistantController、AgentConfigurationController、AiModelController、ApiConfigController | ✅ 移植并改造鉴权(§7) |
| `config/ai/` | 4 | AiAgentDefinition、AiAgentRegistry(代码定义 17 个 Agent)、Onetoken 配置 | ⚠️ 定义改为数据驱动;Onetoken 不移植(P2) |
| `resources/prompts/agents/` | 22 | 17 个 Agent 的系统提示词/指令模板 | ⚠️ 作为融光应用的种子配置导出(§9.2) |

关键依赖:`agentscope-harness 2.0.0` + 5 个 model 扩展、Spring Boot 3.5.14、Java 21、MyBatis-Plus、victools(schema)、PostgreSQL 17、Redis、Flyway。

### 2.2 前端资产(新前端 `frontend/src/`,Vue)

| 路径 | 内容 | 移植性 |
| --- | --- | --- |
| `components/assistant/*.vue`(13 个)+ ts 辅助 | 对话窗、消息列表、时间线、推理折叠、工具确认条、会话导航、多模态 | ✅ 作为 SDK 渲染层基底(§8.2) |
| `store/assistant*.ts`(3 个) | 会话/时间线/页面上下文状态 | ✅ 移植进 SDK |
| `api/ai-pipeline.ts、assistant.ts、agent-config.ts` | SSE 客户端与 REST 封装 | ✅ 移植进 SDK 核心客户端 |
| `views/assistant/index.vue` | 独立对话页 | ✅ 改造为 SDK 整页视图 |
| `views/settings/agents.vue` + skill 对话框 | Agent 设置页(Skill/MCP/状态保留/工作区) | ✅ 作为 InnerAgent 管理站与 SDK 配置页的蓝本 |

旧前端 `ai-fusion-video-web/`(Next.js)的 assistant 实现仅作行为参考,不移植。

### 2.3 数据资产

Agent 相关表 12 张(`afv_agent_conversation/message/run/event/state/state_cleanup_policy/model_call_usage/mcp_server/workspace_config/workspace_entry/workspace_migration/workspace_migration_item`——11 张实体表 + `afv_agent_state`,迁移文件 `V1.0.6.1.5`、`V1.1.0.0.3/7/8`)。注意 `afv_api_config`/`afv_ai_model` **同时服务图文生成**,归属融光,不整体迁移(§5.3)。

---

## 3. 总体架构

### 3.1 目标架构

```text
┌─────────────── 融光前端(Vue)───────────────┐   ┌──────── 其他宿主应用 ────────┐
│ 业务页面 + 页面上下文 store                    │   │ 业务页面                      │
│ Assistant入口: feature flag inner | builtin  │   │ <inner-agent-chat> (WC/iframe)│
└──────┬──────────────────────┬───────────────┘   └──────┬───────────────────────┘
       │ builtin: /api/ai/**  │ inner: InnerAgent SDK(JWKS 验签内嵌视图)
       │(过渡期保留)          │
       ▼                      ▼
┌──────────────────┐  embed token(宿主签)  ┌──────────────────────────────────┐
│ 融光后端          │◄──────────────────────│ InnerAgent 微服务                 │
│ Spring Boot      │                        │ inneragent-server                │
│ ┌──────────────┐ │  MCP tools/call        │  run/agentscope 内核(移植)      │
│ │ia-starter    │◄┼────────────────────────┤  工具中枢: MCP客户端+内置工具    │
│ │MCP Server 桥 │ │  X-IA-Act(IA签名)     │  会话/审计/熔断/模型接入/管理API  │
│ │→ToolExecutor │ │                        │ inneragent-web(管理站)          │
│ └──────────────┘ │                        │ Postgres(ia_*) + Redis          │
│ 业务ToolExecutor │                        └──────────────────────────────────┘
└──────────────────┘
```

### 3.2 仓库与模块划分

```text
InnerAgent/
├─ inneragent-server/              Spring Boot 3.5 / Java 21 / Maven(内核移植落点)
├─ inneragent-web/                 管理站前端(Vue,基于融光 settings/agents 蓝本)
├─ sdk-js/                         JS SDK(monorepo)
│  ├─ packages/core/               API/SSE 客户端(移植 api/ai-*.ts)
│  ├─ packages/components/         Web Component 渲染层(移植 Vue assistant 组件)
│  └─ packages/iframe/             iframe 模式壳(postMessage 协议)
├─ inneragent-spring-boot-starter/ 宿主 Java 接入件(MCP Server 桥 + token 签发 + JWKS 验签)
├─ docker/                         compose 部署(server/web/pg/redis)
└─ docs/                           PRD、本方案、API 契约
```

包名约定:`com.inneragent.{agent,model,tool,platform,server}`;Maven `com.inneragent:*`。

---

## 4. 后端内核移植设计

### 4.1 包映射(融光 → InnerAgent)

| 融光 `com.stonewu.fusion.*` | InnerAgent `com.inneragent.*` | 改造点 |
| --- | --- | --- |
| `service.ai.run` | `agent.run` | 改 import 与身份对象;P0 差异审计须核实「等待确认是否占用运行 lease」——PRD 规则为不占用(《04-调研》V3:业界收敛语义),若融光实现不同则列为 P1 定向改造点 |
| `service.ai.agentscope`(71) | `agent.kernel / agent.state / agent.workspace / agent.mcp / agent.skill / agent.context` | 见 4.2/4.3 |
| `service.ai.provider` | `model.provider` | 删 GoogleFlowReverseApiProvider |
| `service.ai` 根 Service | `agent.*(会话/消息)/model.*(模型)` | AiToolConfigService 改读 MCP 工具注册表 |
| `entity.ai.Agent*、AiModel、ApiConfig` | `agent.entity / model.entity` | 基类换 `AppTenantBaseEntity`(§4.2);表前缀 `afv_→ia_` |
| `controller.Ai*、Agent*` | `server.controller` | 鉴权换 embed token(§6) |
| `config.ai.AiAgentRegistry` | 删除 | Agent 定义改 `ia_agent_definition` 数据驱动,支持导入导出 |

估算:整包复制约 160 文件,需定向改造约 45 文件,新增(MCP 客户端整合、App 隔离、审计、熔断、Webhook、管理 API)约 60 文件。

### 4.2 身份与隔离改造

融光:`TenantContext`(ThreadLocal)+ `TenantBaseEntity(tenant_id)` 行级隔离 + `SecurityUtils.requireCurrentUserId()`。

InnerAgent 引入双层上下文,贯穿同一批 ThreadLocal 设施(直接复用融光的 Filter/Handler 模式):

| 上下文 | 来源 | 落库 |
| --- | --- | --- |
| `AppContext(appKey)` | 路由/令牌 | 所有 `ia_*` 表 `app_id` 列(单应用部署也强制携带,为 P2 多应用预留) |
| `UserContext(userId, tenantId)` | embed token claims(tenantId 由宿主声明透传) | 会话/运行/审计表;工具调用透传给宿主 |

- `AgentScopeToolAdapter` 中 `TenantContext.runInTenant` 调用点改为注入 `ToolExecutionContext{appId,userId,tenantId,runId}` 并携带到 MCP 调用头(§6.2)。
- MyBatis-Plus 行级拦截器改为 `app_id + tenant_id` 双列(单应用部署不弱化,防误配)。

### 4.3 工具层改造(本方案最关键的一处)

融光工具是进程内 Spring Bean(`ToolExecutor` SPI,`ToolExecutorRegistry` 自动收集)。InnerAgent 独立部署后进程不通,改造为:

1. **内核删除对业务 ToolExecutor 的进程内依赖**:`AgentScopeHarnessFactory` 组装 Toolkit 时,工具清单来源改为 `McpToolCatalog`(聚合:应用 MCP(宿主)+ 三方 MCP(用户/应用级)+ 内置工具)。
2. **`ToolExecutor` SPI 保留但只服务内置工具**(时间、文件解析、`resolve_scope` 代理),沿用既有注册与 schema 机制。
3. **`AgentScopeToolAdapter` 改为双实现**:`LocalToolAdapter`(内置工具,原逻辑)与 `McpToolAdapter`(远程):封装 MCP `tools/call`,注入身份头,超时/重试按 §4.7 配置,JSON `status:error` 映射沿用。
4. **注册与指纹(双层策略)**:管理后台注册宿主 MCP 端点时 `list_tools` 拉取清单(含 MCP 注解,存 `ia_tool_registry`)。① **运行级 pinning 保持绝对**:内核快照 sha256 清单锁定照搬,运行中工具集不漂移;② **注册表级活刷新分诊**:订阅 `tools/list_changed`,纯增量差异自动接受并审计,安全相关差异(工具名集合变化、新增必填参数、`readOnlyHint` true→false)强制重新确认后生效——不再全量拒绝新运行。注解仅对可信宿主服务器采信作策略软输入(`readOnlyHint`→自动执行、`idempotentHint`→自动重试门槛、`openWorldHint`→确认卡出口警示),三方工具一律确认。模型可见名用确定性 FQN(`mcp__<serverKey>__<tool>`,serverKey 避用下划线),原始 server+tool 对存注册表。
5. **`resolve_scope` 协议约定**(保留工具名):宿主 MCP 实现该工具,入参 `{pageId, objectId, objectType, custom:{...}}`,出参 `{visibleDomains[], writableFields[], forbidden[], hints[]}`;未实现时走 PRD §6.1.4 降级。

MCP 技术选型:服务端用官方 `io.modelcontextprotocol.sdk:mcp`(Streamable HTTP 传输);客户端复用 AgentScope `McpClientBuilder` 路径(融光 `AgentScopeMcpRegistry` 已验证)。

### 4.4 模型接入、Skill、子 Agent、知识库

- **模型**:`provider/` 整包移植;`ia_model_api_config`/`ia_ai_model` 表结构照搬(去掉图像/视频字段,保留文本协议、上下文窗口、多模态输入、推理档位、并发);配置指纹→内核缓存失效逻辑原样。
- **Skill**:`AgentScopeSkillRepository` 体系移植;用户级 Skill(zip 导入预览)照搬;`SKILL.md` 规范与 128KB/8 个上限沿用。
- **子 Agent**:`AgentScopeSubAgentToolAdapter` + `PlatformSubAgentRunService` 整体移植;子 Agent 定义进 `ia_agent_definition(kind=sub)`。
- **mini 知识库(P1,新增开发)**:倒排索引建议直接用 PostgreSQL `tsvector`(中文配 `zhparser` 或 pg_jieba 扩展;Compose 镜像内置),分段表 `ia_agent_kb_chunk`,检索走 SQL 全文 + 元数据过滤——不引入新中间件。

### 4.5 事件流与 SSE

`run/` 的 journal→outbox→投影、`runId:seq`、Last-Event-ID 重连、50ms 合并、4096 事件/8MB ingress 上限、Redis Stream 分发与唤醒/取消通道——**原样移植,零行为变更**。

### 4.6 Agent 定义数据化

`AiAgentRegistry` 的 17 个代码定义 → 导出器(融光侧一次性脚本)生成 JSON(含提示词、`toolNames`、`subAgentTools`、`instructionTemplate` 变量),经 InnerAgent 管理 API 导入 `ia_agent_definition`。`instructionTemplate` 的 `{projectId}` 类变量统一改为上下文变量注入(前端上下文 + `resolve_scope` 结果)。

### 4.7 运行资源上限与熔断(PRD 承诺的默认值)

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| 单运行最大工具调用次数 | 32 | 超限终止,事件流告知原因 |
| 单运行最大 token(输入+输出累计) | 300,000 | 按模型上下文窗口自适应下修 |
| 单运行最长时长 | 30 分钟 | 与 deadline 取小 |
| 工具失败重试上限 | 2 次(指数退避 1s/4s) | 仅对幂等只读工具自动重试 |
| 单宿主 MCP 并发/QPS | 并发 8 / QPS 20 | 应用级可配(PRD 护栏指标落点) |
| 确认等待超时 | 24h | PRD 已定 |
| 内核缓存 | 64 个 / 30 分钟过期 | 沿用融光配置 |
| 紧急停用 | 应用级总开关 + 单运行终止 | 管理站一键操作,生效延迟 ≤ 5s(经 Redis 取消通道) |

---

## 5. 数据模型

### 5.1 表清单(`ia_` 前缀,Flyway 从 V1 起链)

移植改造自融光 5 个迁移文件(重命名前缀 + 增列):

| 新表 | 来源 | 改造 |
| --- | --- | --- |
| `ia_app` | 新增 | appKey、名称、签名公钥、webhook 配置、保留期等应用配置 |
| `ia_agent_definition` | 新增(替代代码 AiAgentRegistry) | kind(main/sub)、提示词、工具白名单、上下文模板 |
| `ia_agent_conversation/message/run/event` | `afv_agent_*` 四表 | +`app_id`;`ia_agent_event` 增 `schema_version` 列(持久事件载荷版本化,fail-fast 兼容策略);其余列结构不变 |
| `ia_agent_state(+cleanup_policy)` | `afv_agent_state*` | +`app_id` |
| `ia_agent_mcp_server` | `afv_agent_mcp_server` | +`scope(app/user)`、`app_id` |
| `ia_agent_workspace_*`(4) | `afv_agent_workspace_*` | +`app_id` |
| `ia_agent_model_call_usage` | `afv_agent_model_call_usage` | +`app_id`(结算 P2) |
| `ia_tool_registry` | 新增 | 应用 MCP 注册:端点、凭证、工具清单快照、风险级(注解生成默认+人工覆盖)、管理员策略(force-ask/allow/deny)、`resume_safe`、注解缓存、FQN 映射、健康状态 |
| `ia_tool_grant` | 新增 | 用户工具授权:tool FQN、作用域(会话/永久)、授予时风险级、schema 指纹、来源;风险升级或安全 schema 变更自动失效,工具注销级联清除 |
| `ia_audit_log` | 新增 | 仅追加;敏感字段脱敏值;`decision_source`(mode-default/user-grant/forced-policy/live-confirm/full-access) |
| `ia_model_api_config / ia_ai_model` | `afv_api_config/afv_ai_model` 文本部分 | 去图像/视频列 |
| `ia_agent_kb / ia_agent_kb_chunk` | 新增(P1) | tsvector 列 |

### 5.2 行级隔离

所有业务表 `app_id + tenant_id` 双列;会话/消息另带 `user_id`。单应用部署不省略 `app_id`(ADR-10 预留)。

### 5.3 历史数据迁移(反向替代时)

**默认策略(ADR-T5):不迁移。** 切流后新会话进 InnerAgent;融光侧 `afv_agent_*` 转归档只读(记录页旧入口保留一个版本周期),之后随引擎代码一起删除。理由:Agent 会话历史回溯价值低、双写/ETL 成本高;若试点数据证明需要,提供一次性 ETL(同构表直拷 + `projectId→context` 映射)作为备选。

`afv_api_config/afv_ai_model` 不迁移:融光图像/视频生成仍依赖;InnerAgent 独立配置文本模型(密钥可指向同一服务商账号)。

---

## 6. 凭据与认证

### 6.1 两级令牌(与 03-租户方案 §5 同构)

| 令牌 | 签发方 | 载荷 | 用途 |
| --- | --- | --- | --- |
| **embed token** | 宿主后端(注册时上传公钥,InnerAgent 验签) | appKey、userId、tenantId、exp(默认 12h) | SDK→InnerAgent 全部 API 与 SSE |
| **act token(X-IA-Act 头)** | InnerAgent(RS256,JWKS 发布于 `/.well-known/jwks.json`) | claims 按 RFC 8693 建模:`sub`=终端用户、`act.sub`=运行身份、`aud`=宿主 MCP URI(audience 绑定);另含 appKey、tenantId、runId、toolName、exp 60s | 每次 MCP `tools/call` 携带,宿主验签后重建用户上下文;保留自定义头(不占用 `Authorization`);仅限宿主桥内环——三方 MCP 服务器一律走规范 OAuth(CIMD/DCR + RFC 8707 resource 参数)或用户配置静态头 |

闭环 PRD R2:前端上报的 page/object 为**建议性上下文**(可伪造),安全决策只依赖两级签名令牌中的身份 + 宿主工具侧校验;审计记录两者。

### 6.2 宿主侧上下文重建(starter 职责)

`inneragent-spring-boot-starter` 提供 Filter:验 `X-IA-Act` → 写入 Spring Security 上下文与 `TenantContext` → 既有 `ToolResourceAccessGuard`、行级租户拦截器**零改动生效**。

### 6.3 管理 API 凭据与管理站认证

宿主后端 ↔ InnerAgent 管理面的 M2M 调用(注册 MCP、导入 Agent 定义、配置 webhook)用 client_credentials 换 token(对齐 03-方案 §5.2)。

独立管理站(inneragent-web)人类管理员认证:首版内置管理员账号(Argon2 口令散列 + 失败锁定 + 登录审计),宿主 SSO 跳转列 P2;embed token(用户级)对管理站 API 无效,两套凭据域隔离。

### 6.4 确认与续跑不变式(《04-调研》V29/V30)

- **确认端点幂等**:`POST /runs/{runId}/confirm` 以 unique(run_id, pending_confirmation_id, decision) 约束 + 运行 epoch 守卫保证双击/双端重复提交安全。
- **确认者绑定**:仅会话属主(embed token 中 userId)可确认;UI 提交的批准不作为授权边界,服务端状态机为唯一裁决点。
- **决策先日志化**:确认/拒绝事件写入 journal 后才恢复运行执行(等价 MS AF response-entry checkpoint,保证 HITL 重放精确)。

---

## 7. 接口设计

### 7.1 API 清单与兼容映射(ADR-T4)

保持融光 pipeline API 形状,降低前端切换成本;基路径 `/ia/api`,鉴权换 embed token:

| 融光旧路由 | InnerAgent 路由 | 说明 |
| --- | --- | --- |
| `POST /api/ai/pipeline/run|continue`(SSE) | `POST /ia/api/v1/runs`(SSE)/`/{runId}/continue` | 请求体增 `context{page,object}`;事件协议不变 |
| `GET /api/ai/pipeline/reconnect` | `GET /ia/api/v1/runs/{runId}/events` | Last-Event-ID 语义不变 |
| `POST /api/ai/pipeline/cancel|confirm|confirm/expire` | `POST /ia/api/v1/runs/{runId}/cancel|confirm|confirm/expire` | 同;confirm 幂等(§6.4) |
| `GET /api/ai/pipeline/status|running` | `GET /ia/api/v1/runs/{runId}|/running` | 同 |
| `GET /api/ai/assistant/conversations*` | `GET /ia/api/v1/conversations*` | 同 |
| `/api/storage/assistant-upload` | `POST /ia/api/v1/attachments` | 对话附件上传,落 Agent 工作区三后端;类型/大小按模型能力校验 |
| `/api/ai/agent-config/*`(skills/mcp/state/workspace) | `/ia/api/v1/me/*` | 用户级配置(SDK 配置页) |
| — | `/ia/api/v1/admin/*` | 应用级管理(定义/工具/模型/审计/熔断/webhook) |
| — | `POST {宿主 webhook}` | 终态通知:HMAC 签名,5 次指数退避重试 |

### 7.2 SSE 事件协议

沿用融光 `outputType` 全集:CONTENT/REASONING/TOOL_CALL_STARTED/CALL/FINISHED、SUB_AGENT_*/…、EXTERNAL_EXECUTION_REQUIRED/RESULT、DONE/ERROR/CANCELLED;事件 ID `runId:sequence`。**新增** 2 个事件:`RESOURCE_LIMIT`(触发 §4.7 上限)、`SCOPE_RESOLVED`(反查结果,供前端展示当前约束范围)。**吸收 Vercel AI SDK 协议三点**(《04-调研》§3.3):①强制策略自动放行/拒绝的决策仍下发并标 `isAutomatic`(审计可见);②拒绝为独立终态(denied 语义,回灌文案陈述后果);③所有 delta 事件按 messageId/toolCallId 自标识,保证重放安全;确认卡四档(本次/本会话/总是/拒绝)由 `EXTERNAL_EXECUTION_REQUIRED` 载荷描述。

### 7.3 版本兼容

SDK 与服务端 N-1 兼容(PRD §8);API 带 `IA-Protocol-Version` 头,服务端兼容读取旧事件序列。

---

## 8. 前端与 SDK

### 8.1 SDK 形态(ADR-8)

- `@inneragent/sdk`:入口 `init({appKey, tokenGetter, theme, mode:'wc'|'iframe'})`;`tokenGetter` 回调支持宿主懒换 token(过期重取,PRD 鉴权生命周期)。
- Web Component:`<inner-agent-chat view="chat|history|config">`,Shadow DOM 隔离 + CSS 变量主题(`--ia-primary` 等 12 个令牌,P0 主色/明暗)。
- iframe 模式:同源策略受限宿主用 `postMessage` 协议桥接(token 不落 iframe URL,经 postMessage 传递)。

### 8.2 组件复用(融光 Vue → SDK)

| 融光 `frontend/src/` | SDK 落点 |
| --- | --- |
| `api/ai-pipeline.ts、assistant.ts、agent-config.ts` | `packages/core`(改 baseURL 与 token 注入) |
| `store/assistant*.ts` | `packages/core`(状态机,时间线 reducer 逻辑不变) |
| `components/assistant/*.vue`(13 组件) | `packages/components`(去业务依赖:reference-picker 的项目/剧本引用改为通用上下文引用) |
| `views/assistant/index.vue` | SDK 整页视图 |
| `views/settings/agents.vue` + skill 对话框 | SDK 配置视图(用户级)+ `inneragent-web` 管理站蓝本 |

### 8.3 管理站(inneragent-web)

Vue 3 + Vite;页面:应用注册、Agent 定义(导入导出/提示词编辑/工具白名单)、工具注册与体检、模型配置、审计检索、用量、紧急停用。以融光旧前端 settings/agents 各 section 的交互为蓝本,用新写 Vue 实现。

### 8.4 宿主接入件(inneragent-spring-boot-starter)

职责:① embed token 签发(`ia.starter.sign-key`);② MCP Server 桥——扫描宿主 `ToolExecutorRegistry`(或注解声明),自动生成 MCP 工具(schema 取自 SPI 的 `getParametersSchema`),Streamable HTTP 暴露于 `/ia-mcp`;③ `X-IA-Act` 验签 Filter + 用户/租户上下文重建;④ `resolve_scope` 实现占位(宿主可覆写)。**这是「接入 ≤ 2 人日」承诺的载体。**

---

## 9. 反向替代融光内置 Agent(核心)

### 9.1 替代范围

| 项 | 处置 |
| --- | --- |
| 引擎(run/agentscope/state/workspace) | 替代 → 删除融光侧代码 |
| 业务 ToolExecutor(43 文件) | **保留**,经 starter 的 MCP 桥继续服务 |
| 模型管理(ApiConfig/AiModel) | 融光保留(图像/视频仍用);Agent 文本模型转 InnerAgent 配置 |
| 17 个 Agent 定义 + 22 个提示词 | 导出 → InnerAgent `ia_agent_definition`(app=rongguang) |
| Skill/MCP 用户配置 | 迁至 SDK 配置视图 |
| 会话历史 | 归档只读一个版本周期后删(ADR-T5) |
| onetoken 用量结算 | 融光图像/视频保留;Agent 侧结算随 InnerAgent P2 |

### 9.2 融光侧改造清单

1. 引入 `inneragent-spring-boot-starter`(依赖 + `application.yaml` 配置 sign-key、工具暴露开关);`generate_image/generate_video` 等 ToolExecutor 经桥自动可用,零改写。
2. 后端新增 embed token 签发端点(登录态内,`GET /api/ia/embed-token`)。
3. Agent 定义导出脚本(读 `AiAgentRegistry` → JSON → 调 InnerAgent admin API 导入)。
4. 前端 feature flag `ASSISTANT_BACKEND=builtin|inner`:inner 分支挂 `<inner-agent-chat>`,上下文由既有 `assistantPageContext` store 喂给 SDK `setPage/setObject`;builtin 分支保持旧链路。
5. 记录页旧会话入口加「历史(归档)」标记。

### 9.3 双跑与灰度

- 环境:融光 compose 网络内新增 inneragent-server/web/pg(独立库)/redis;宿主 MCP 指向融光后端 `/ia-mcp`。
- 灰度:flag 按用户百分比放行(运营在融光管理端配置);双跑期两侧指标并报(工具成功率、确认率、重连成功率、任务完成率代理指标)。
- 等价性验证:对照组测试集(融光既有 agent 测试 + 4 个 PRD 锚点用例的融光化版本)在两侧跑通,事件序列 diff 一致(允许时间戳/ID 差异)。

### 9.4 切流与回滚

100% 切流后观察 ≥ 2 周(无 P0/P1 缺陷、指标不劣于内置),随后分两批删除;任一阶段出问题,flag 一键回内置(引擎代码删除前回滚零成本;删除后回滚 = 重新部署旧版本镜像,保留最后一个含引擎的镜像 tag ≥ 90 天)。

### 9.5 融光侧最终删除清单

| 删除 | 内容 |
| --- | --- |
| 代码 | `service/ai/run`、`service/ai/agentscope`、`service/ai/provider`(若融光其他功能无引用)、根 Service 中 Agent 专属类、`controller/Ai*、AgentConfiguration*`、`config/ai/*`、`entity/ai/Agent*`、`prompts/agents/` |
| 保留 | `service/ai/tool/**`(经桥服务)、`ToolExecutor` SPI 与注册表、`ApiConfig/AiModel`(图像视频)、onetoken、`AiStreamRedisService` 若任务流仍用则保留 |
| 前端 | `components/assistant/*`、`store/assistant*`、`api/ai-*.ts`、`views/assistant`、`views/settings/agents.vue` → 由 SDK 与 InnerAgent 管理站取代 |
| 数据 | `afv_agent_*` 表归档后 drop(单独迁移脚本,保留导出备份) |

---

## 10. 安全设计清单

| # | 项 | 方案 |
| --- | --- | --- |
| S1 | 身份不可伪造 | 双级签名令牌(§6.1);前端上下文仅建议性 |
| S2 | 越权拦截 | 宿主侧最终裁决(ADR-3):starter 重建用户上下文后,既有 Guard/行级拦截零改动生效 |
| S3 | 凭据存储 | MCP/模型密钥加密落库(AES-GCM,主密钥环境变量);审计脱敏 |
| S4 | 审计 | `ia_audit_log` 仅追加,含被拒调用;敏感参数打码(§PRD 6.9) |
| S5 | 传输 | 全链路 HTTPS;SSE 与 MCP 同源策略由部署层 Nginx 收敛 |
| S6 | 资源 | §4.7 全套上限;紧急停用 ≤5s 生效 |
| S7 | 内容安全 | 输入/输出审核接入点(过滤器链,可配外部审核服务或回调宿主) |
| S8 | 状态一致性 | fail-closed 状态存储照搬;Outbox 投递重试 |
| S9 | 供应链 | AgentScope 2.0.0 版本锁定;starter 依赖最小化 |
| S10 | 授权生命周期 | ia_tool_grant 撤销/级联清理;风险升级与安全 schema 差异自动失效;确认端点幂等+确认者绑定(§6.4) |
| S11 | 注解不可信与协议演进 | MCP 注解仅可信宿主采信;三方服务器走规范 OAuth;`/ia-mcp` 按无状态实现(不依赖 session/GET 流),兼容 2025-06-18 与 2026-07-28 两代客户端 |

---

## 11. 代码改造清单(文件级)

### 11.1 InnerAgent 新建(骨架)

| 模块 | 关键内容 |
| --- | --- |
| `inneragent-server/src/main/java/com/inneragent/agent/run/**`(75 文件) | 复制自融光,改 import/身份 |
| `.../agent/kernel|state|workspace|mcp|skill|context/**`(71) | 复制 + §4.2/4.3 改造:`McpToolAdapter`、`McpToolCatalog`、App 隔离 |
| `.../model/provider/**`(13) | 复制(去 GoogleFlow) |
| `.../server/controller/**`(5→8) | 移植 5 个 + 新增 admin/webhook/app |
| `.../platform/**`(新,~40) | ia_app、定义数据化、工具注册表、审计、熔断、Webhook、审核接入点 |
| `resources/db/migration/V1__*.sql`(8 个) | §5.1 表结构 |
| `sdk-js/**` | §8.2 映射的移植 + WC/iframe 壳 |
| `inneragent-spring-boot-starter/**`(新,~15) | §8.4 四职责 |
| `inneragent-web/**` | 管理站(蓝本重写) |

### 11.2 融光侧改造(试点期)

| 文件/位置 | 改造 |
| --- | --- |
| `pom.xml` | + `inneragent-spring-boot-starter` |
| `application*.yaml` | + `ia.starter.*`(sign-key、工具暴露、MCP 路径) |
| 新 `IaEmbedTokenController` | embed token 签发端点 |
| 新脚本 `tools/export-agent-definitions` | AiAgentRegistry → JSON 导出 |
| `frontend/src` flag 与挂载点 | `ASSISTANT_BACKEND` 开关 + `<inner-agent-chat>` 集成 + 上下文对接 |
| `views/settings/agents.vue` | 加「由 InnerAgent 提供」跳转提示(过渡) |

### 11.3 融光侧删除(替代完成后,§9.5)

按 §9.5 清单执行;删除以独立 PR 分批:①引擎与控制器 ②实体与迁移归档 ③前端 assistant 域 ④配置与文档清理。

---

## 12. 分阶段落地计划

| 阶段 | 内容 | 出口条件 | 对应 PRD |
| --- | --- | --- | --- |
| P0 移植基座(1 周) | 仓库/模块初始化;run+agentscope+provider 整包复制编译通过;`ia_` schema;内置工具跑通单轮对话 | 演示会话经 SSE 出流、事件落库 | M0 前半 |
| P1 去业务化 + 工具中枢(3 周) | 身份双层改造;McpToolCatalog/Adapter;starter 四职责;resolve_scope 协议;embed/act 令牌与 JWKS;SDK core+chat WC | 融光 starter 桥接后,U1/U5 全流程(含确认与审计)走 InnerAgent | M0 后半 + M1 主体 |
| P2 管理与运维(2 周) | inneragent-web;审计检索;资源上限与紧急停用;终态 Webhook;工具体检 v1 | PRD M1 验收(U1–U3、U5 + 接入演练 ≤2 人日,融光即演练对象) | M1 |
| P3 反向替代灰度(2 周开发 + 4 周灰度) | §9.2 全部改造;定义导出导入;flag 双跑;等价性对照 | 100% 切流且稳定 ≥2 周 | (PRD 外新增,试点关键路径) |
| P4 增强与删除(4 周) | 三方 MCP、Skill、mini KB、子 Agent、用量;执行 §9.5 删除 | PRD M2 验收 + 融光引擎代码移除 | M2 |

人力估算:P0–P2 约 2 后端 + 1 前端;P3 起 +1 后端(融光侧)。

---

## 13. 关键设计决策记录(ADR)

| # | 决策 | 理由 | 放弃的方案 |
| --- | --- | --- | --- |
| ADR-T1 | 内核整包移植(复制改造),不重写 | 248 文件的运行引擎已被生产验证;重写风险与工期不可控 | 基于 LangGraph 等重造 |
| ADR-T2 | 工具统一走 MCP,进程内 SPI 仅留内置工具 | 独立微服务进程边界使然;也是产品边界(N2)的技术表达 | RPC 直调宿主接口(失去标准生态) |
| ADR-T3 | 融光工具零重写:starter 桥接 ToolExecutorRegistry → MCP Server,身份经 X-IA-Act 重建 Spring Security/TenantContext | 43 个业务工具与权限 Guard 零改动即服务化,试点成本最低 | 逐工具改写为独立 MCP 服务 |
| ADR-T4 | API 协议兼容期:沿用融光 pipeline API 形状与 SSE 事件 | 前端切换成本最小化,双跑对照才有共同度量口径 | 全新 API 设计(切换成本高) |
| ADR-T5 | 历史会话不迁移,归档只读后删除 | 会话回溯价值低;ETL/双写成本高 | 全量 ETL 迁移 |
| ADR-T6 | 非对称双级令牌:宿主签 embed(RSA),InnerAgent 签 act(JWKS) | 双方互不信任前端;密钥轮换友好 | 共享对称密钥(轮换与泄露面差) |
| ADR-T7 | 同栈同源:Spring Boot 3.5/Java 21/PG/Redis/AgentScope 2.0.0 | 移植摩擦最小,运维经验复用 | 换语言/框架重实现 |
| ADR-T8 | SDK 渲染层以融光 Vue assistant 组件为基底,输出框架无关 Web Component | 已验证的交互与流式渲染;WC 形态满足 ADR-8 双形态 | React 重写 / 纯 iframe |
| ADR-T9 | mini KB 用 PG tsvector 全文检索 | 不新增中间件,与「极简接入」一致 | ES/向量库(违反 N4) |

---

## 14. 开放问题

| # | 问题 | 影响 | 当前倾向 |
| --- | --- | --- | --- |
| Q1 | ~~MCP Java SDK 选型~~ | starter 实现复杂度 | **已决(2026-09-20,依《04-调研》§3.4)**:官方 `io.modelcontextprotocol.sdk:mcp` v2.0.x(2026-06 GA,规范 2025-11-25);Spring 传输在 Spring AI 2.0,不引入。P1 首任务即集成验证 |
| Q2 | AgentScope 升级窗口(2.0.0 锁定) | 内核安全补丁跟进 | 移植期锁版本,替代完成后建升级流水线 |
| Q3 | 双跑期模型用量计量归属(融光 onetoken vs InnerAgent 用量表) | 成本核算口径 | 双跑期各自计量,切流后以 InnerAgent 为准 |
| Q4 | zhparser/pg_jieba 在目标 PG 镜像的可获得性 | mini KB 中文检索质量 | Compose 镜像预装 zhparser;不可得则降级 trigram(P4 决策) |
| Q5 | webhook 密钥轮换机制 | 运维 | 支持双密钥并存 72h(P2 实现) |
| Q6 | 内嵌视图与宿主 CSP/cookie 策略冲突面 | iframe 模式覆盖度 | P1 用真实宿主(融光)验证,形成 CSP 白名单清单文档;iframe 需 postMessage 工具/上下文桥,排期时计入真实工程量(业界 iframe 仅用于不可信 UI) |
| Q7 | MCP 2026-07-28 无状态规范(session/GET 流删除)的 SDK 跟进节奏 | `/ia-mcp` 与 MCP 客户端兼容策略 | 桥按无状态形态实现,同时服务 2025-06-18 与 2026-07-28 两代客户端(§4.3/S11);Java SDK 跟进后评估切换 |
