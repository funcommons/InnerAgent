# InnerAgent · 应用内嵌智能体微服务 产品需求文档(PRD)

## 目录

- 0. 文档信息
- 1. 背景与问题
- 2. 产品定位与目标
- 3. 用户与核心场景
- 4. 名词解释
- 5. 总体方案
- 6. 功能需求
- 7. 产品边界
- 8. 非功能需求
- 9. 数据与埋点
- 10. 风险与开放问题
- 11. 里程碑规划
- 12. 关键设计决策记录(ADR)
- 附录 A. 参考实现能力对照
- 附录 B. 术语速查

---

## 0. 文档信息

| 项 | 内容 |
| --- | --- |
| 产品名称 | InnerAgent(应用内嵌智能体微服务) |
| 文档版本 | V1.3(修订稿) |
| 日期 | 2026-09-20 |
| 作者 | justin |
| 目标读者 | 产品、前后端研发、测试、运维 |
| 参考实现 | mmagix-minicuts(融光)中基于 AgentScope 的 Agent 工作区,能力模型已经过工程验证 |
| 关联文档 | 《03-租户体系设计-通用方案》《04-微剧场微中台技术方案》(mmagix-minicuts 仓库) |
| 交互设计稿 | 待补(评审通过后回链) |
| API 契约 | 《02-技术方案-内核移植与反向替代》§7(接口设计) |
| 开发计划 | 《03-开发计划》(阶段验收与测试方案) |
| 对标调研 | 《04-开源与大厂实现对标调研》(源码级证据与裁定) |

### 版本记录

| 版本 | 日期 | 修订人 | 说明 |
| --- | --- | --- | --- |
| V1.0 | 2026-09-20 | justin | 首版,定义产品功能与边界 |
| V1.1 | 2026-09-20 | justin | Q1–Q3 拍板落档:管理双形态(ADR-7)、SDK 双形态(ADR-8)、会话保留期 180 天可配(ADR-9) |
| V1.2 | 2026-09-20 | justin | 评审修订:U4 移至 M2 并补查询锚点 U5;撤销表述降级为「可拦、可停、可追溯」(N11);补反馈度量通道;确认优先级规则;ADR-10 单应用部署;新增资源上限与熔断、终态 Webhook、内容安全接入点(N12)、scope 降级、token 生命周期、N-1 兼容、工具体检;指标口径与 NFR 细化 |
| V1.3 | 2026-09-20 | justin | 按《04-开源与大厂实现对标调研》修订:ALWAYS_ASK 重定义为写操作强制确认、FULL_ACCESS 三护栏、确认卡四档(新增本会话级)、授权存储与撤销、schema 活刷新分诊、注解作策略软输入、子 Agent 权限继承、审计 decision_source、审核 tripwire 建模、OTel GenAI 可观测、上下文可检视;里程碑时长与《03-开发计划》对齐;init 签名统一为 tokenGetter |

---

## 1. 背景与问题

### 1.1 背景

公司内部及交付的多款业务系统(管理后台、SaaS 应用、创作者工具)都在出现同一类需求:用户希望在应用内直接用自然语言完成业务操作——改一条数据、生成一段内容、按模板建一个流程,而不是在菜单里逐级寻找功能入口。

这些需求的能力内核是同构的:LLM 对话 + 工具调用 + 会话管理 + 权限确认 + 模型接入。如果每个应用各建一套,将重复投入,且各应用的 Agent 质量、安全水位、审计能力参差不齐。

### 1.2 问题清单

| # | 问题 | 后果 |
| --- | --- | --- |
| P1 | 每个 App 自建 Agent,重复造轮子 | 成本高、质量不一、无法沉淀通用能力 |
| P2 | 业务操作入口深,用户不会用 | 功能使用率低,培训成本高 |
| P3 | LLM 直连业务接口缺乏权限确认与审计 | 误操作、越权、不可追责 |
| P4 | 模型服务商多变(合规/成本/能力) | 切换模型需要改业务代码 |
| P5 | 跨系统数据孤岛 | 用户需要人工在多个系统间搬运数据 |

### 1.3 解决思路

把 Agent 能力抽成一个**独立微服务**:只服务内嵌它的宿主应用,不做独立 to C 产品。宿主应用把自己的业务能力以 **MCP** 形式开放给 InnerAgent;前端通过 **JS SDK** 以微前端方式嵌入对话能力;Agent 的每一次业务操作都受**约束范围**限制并留审计。

能力模型在 mmagix-minicuts(融光)中已完成工程验证:持久化运行(lease/心跳/断线重连)、事件日志与消息投影、工具权限四模式与确认流、MCP 客户端(系统级 + 用户级)、Skill、子 Agent、多协议模型接入、租户隔离与用量结算。InnerAgent 是把这套内核**从视频创作业务中剥离、通用化**后的独立产品。

---

## 2. 产品定位与目标

### 2.1 一句话定位

> InnerAgent 是一个只服务本应用的**内嵌式 AI Agent 微服务**:宿主应用用 MCP 开放业务能力,前端用 JS SDK 嵌入对话界面,用户用自然语言即可安全地操控应用。

### 2.2 产品价值

| 角色 | 价值 |
| --- | --- |
| 宿主应用研发 | 极简接入(开一个内部 MCP 服务 + 引入 SDK),不改业务代码即可获得 Agent 能力 |
| 宿主应用运营/管理员 | 配置页可视化配置 Agent 人设、工具白名单、Skill、知识库,无需发版 |
| 终端用户 | 用一句话完成深路径业务操作;每一步可见、可控、可确认 |
| 公司/平台方 | 统一模型接入、统一审计与用量口径,能力一次沉淀多应用复用 |

### 2.3 产品目标与指标

**北极星指标:Agent 任务完成率**——用户发起对话后,目标业务操作被成功执行、且会话无负反馈的比例。首版度量口径:运行成功 ∧ 该会话无 👎 或「未解决」标注(反馈通道见 §6.7)。

| 目标 | 度量 | 首版锚点 |
| --- | --- | --- |
| 接入快 | 宿主应用从零到可用对话的接入时长 | ≤ 2 人日 |
| 答得准 | 工具调用成功率(调用成功 / 总调用,排除用户拒绝) | ≥ 95% |
| 做得对 | Agent 任务完成率 | ≥ 80% |
| 管得住 | 高危操作 100% 走确认流;100% 操作可审计 | 硬性指标 |
| 稳得住 | 断网/刷新后对话可无损恢复 | 断线重连成功率 ≥ 99% |

护栏指标(不允许变坏):InnerAgent 对同一宿主 MCP 的调用并发与 QPS 受可配置上限约束(默认值见技术方案),宿主工具侧可二次限流,确保 Agent 流量不冲击宿主服务;Agent 侧越权事件为 0。

口径说明:「工具调用成功率」不含用户拒绝的调用,但用户拒绝后 Agent 未给出可接受替代方案的,计入任务失败;确认拒绝率作为独立健康指标监控(§9.2)。

### 2.4 目标用户(角色定义)

| 角色 | 是谁 | 核心诉求 |
| --- | --- | --- |
| 接入研发 | 宿主应用的后端/前端工程师 | 接入步骤少、契约清晰、排障容易 |
| 应用管理员 | 宿主应用的管理员/实施人员 | 在配置页自助调整 Agent 行为,不依赖研发发版 |
| 终端用户 | 宿主应用的最终用户 | 说一句话把事办成;误操作可拦、可停、可追溯 |
| 平台运营 | InnerAgent 的运维方 | 统一管理接入应用、模型成本可控、用量可结算 |

### 2.5 竞品与替代方案

| 方案 | 定位 | 为何不满足本产品诉求 |
| --- | --- | --- |
| Dify / Coze 等编排平台 | 独立 Agent 应用搭建平台,面向门户式对话与工作流编排 | 形态是「再建一个应用」而非嵌入既有应用;缺少宿主上下文反查、按工具确认流与应用内审计 |
| LangGraph / Agent 框架 | 开发框架与运行库 | 需自建持久化运行、权限、确认流与前端;本产品即这类工程量的产品化封装(内核继承自已验证实现,§1.3) |
| 各产品内置 Copilot | 厂商自有应用的助手 | 能力绑定单一产品,无法跨应用复用,也不开放 MCP 工具模型 |

**差异化**:InnerAgent 的形态是「微服务 + 宿主 MCP 操控 + 微前端嵌入 + 确认/审计合规」,聚焦让既有应用获得可安全操控业务的内嵌 Agent,而非通用 Agent 平台。

---

## 3. 用户与核心场景

### 3.1 核心用例(验收锚点)

以下 5 个用例为首版验收锚点,覆盖"查、改、建、跨"四类操作形态:

| # | 用户输入(示例) | 涉及能力 | 形态 |
| --- | --- | --- | --- |
| U1 | 「把用户 #12993 的密码改为 test123」 | 身份上下文 + 写操作确认流 + 审计 | 改(高危) |
| U2 | 「这个产品的简介优化一下」 | 当前页/当前对象上下文注入 + 内容生成 + 回写 | 改(低危) |
| U3 | 「创建一个新流程,以 #3432 流程为模板,加上用户退款环节」 | 工具组合调用 + 模板复制 + 追加节点 | 建 |
| U4 | 「用我上传的聊天记录创建一份合同,甲方是 XXX 公司,同时创建这个商户」 | 附件上传/文件理解 + mini 知识库 + 多工具编排 + 三方/本应用工具协同 | 建(多步) |
| U5 | 「查一下 #12993 最近 30 天的登录记录」 | 只读工具直通(无确认流)+ 结果结构化呈现 | 查 |

### 3.2 场景故事(摘要)

- **场景 A · 管理后台提效**:运营在用户管理页打开侧边对话,说「把 #12993 的密码重置为 test123」。Agent 基于当前页上下文知道对象域是"用户",发起 `update_user` 工具调用;该工具被标记为写操作,前端弹出确认卡片;运营点击允许后执行(宿主工具侧同时做密码策略校验,弱密码将被拒绝——体现 ADR-3「宿主最终权决」);执行成功,审计日志记录"操作人、Agent、工具、参数、结果"。
- **场景 B · 内容创作**:商家在商品编辑页说「简介优化一下」,Agent 反查宿主应用得到"当前对象 = 商品 #88,可见字段、可写范围",生成 3 版简介供选择,用户选定后回写。
- **场景 C · 跨系统打通**:企业在 InnerAgent 注册了 CRM 与工单系统的 MCP。客服说「把这个客户最近的 3 个工单汇总后写进 CRM 备注」,Agent 串联两个系统的工具完成跨系统操作。
- **场景 D · 文档生成**:用户上传聊天记录文件,Agent 结合 mini 知识库中的合同模板条款,生成合同草稿并调用宿主应用工具创建商户与合同记录。

---

## 4. 名词解释

| 名词 | 定义 |
| --- | --- |
| 宿主应用(Host App) | 内嵌 InnerAgent 的业务系统,后续简称"应用" |
| Agent 实例 | 面向某应用/某场景的一组 Agent 配置(人设、模型、工具白名单、Skill、知识库),一个应用可配多个 |
| 会话(Conversation) | 一次多轮对话及其消息序列,归属用户 + Agent 实例 |
| 运行(Run) | 会话内一次可恢复的 Agent 执行单元,拥有独立状态机与事件流 |
| MCP | Model Context Protocol,工具调用的标准协议。InnerAgent 是 **MCP 客户端** |
| 应用工具 | 宿主应用以内部 MCP 服务形式注册给 InnerAgent 的业务工具(如 `update_user`) |
| 约束范围(Scope) | 本次对话被允许操作的对象域与边界,由"前端上下文 + Agent 反查应用"共同确定 |
| Skill | 以文件夹/包形式分发的能力扩展(提示词 + 资源 + 脚本说明),按需激活,不占常驻上下文 |
| mini 知识库 | **无向量**的小型知识库:文档分段后以关键词/倒排/结构化方式检索,面向千级文档规模 |
| 子 Agent | 由主 Agent 以工具形式调起的下级 Agent,拥有独立定义与受限工具集 |
| 确认流 | 写操作执行前向前端推送确认请求、等待用户批准/拒绝的闭环 |
| 内核快照(Kernel Snapshot) | 一次运行所用的 Agent 定义 + 工具清单(含 schema 哈希)的不可变快照,保证可复现与可回放 |

---

## 5. 总体方案

### 5.1 产品架构

```text
┌────────────────────────────── 宿主应用前端 ──────────────────────────────┐
│  业务页面  ◄── 上下文(用户/当前页/当前对象) ──►  InnerAgent JS SDK        │
│                                        ├─ 对话页(微前端)                 │
│                                        ├─ 记录页(微前端)                 │
│                                        └─ 配置页(微前端)                 │
└──────────────┬───────────────────────────────┬──────────────────────────┘
               │ SSE / HTTPS                   │ 注册/注销
┌──────────────▼───────────────────────────────▼──────────────────────────┐
│                        InnerAgent 微服务                                 │
│  ┌─────────┐ ┌──────────┐ ┌───────────┐ ┌─────────┐ ┌────────────────┐  │
│  │ 会话/运行 │ │ 工具中枢  │ │ Skill 体系 │ │ mini KB │ │ 模型接入层      │  │
│  │ 引擎     │ │ MCP客户端 │ │           │ │         │ │ 多协议 Provider │  │
│  └─────────┘ └──────────┘ └───────────┘ └─────────┘ └────────────────┘  │
│  ── 公共底座:事件日志/消息投影、确认流、审计、用量结算、多应用隔离 ──      │
└──────────────┬───────────────────────────────┬──────────────────────────┘
               │ MCP 调用(受约束范围)          │ MCP 注册
┌──────────────▼──────────────┐  ┌─────────────▼──────────────────────────┐
│  宿主应用 MCP 服务(业务能力) │  │  三方应用 MCP(CRM/工单/…)  │
└─────────────────────────────┘  └────────────────────────────────────────┘
```

### 5.2 核心概念模型

- **应用(App)→ Agent 实例 → 会话 → 运行 → 消息/事件**,五级从属结构,数据全部按应用隔离。
- **工具**有三个来源:应用工具(宿主 MCP)、三方工具(三方 MCP)、内置工具(时间、文件解析等)。统一入"工具中枢",按 Agent 实例白名单下发。
- **约束范围**的确定是双通道的:① 前端 SDK 主动上报(用户、当前页、当前对象);② Agent 通过标准工具 `resolve_scope` **反查**宿主应用,得到该上下文下可见/可操作的对象集合。工具调用必须携带 scope 派生的上下文参数,由**宿主应用做最终权限判定**(见 ADR-3)。
- **运行是持久化的**:每次运行生成内核快照与事件日志(journal),消息表只是事件流的投影;因此支持断线重连、失败续跑与完整回放。

### 5.3 关键流程

#### 5.3.0 极简接入流程(产品承诺的八步)

| 步骤 | 动作 | 承担方 |
| --- | --- | --- |
| 1 | 创建 Agent 实例(配置页) | 应用管理员 |
| 2 | 应用开发内部 MCP 服务,并向 InnerAgent 注册为工具 | 接入研发 |
| 3 | 前端引入 JS SDK | 接入研发 |
| 4 | 前端上报用户、当前页、当前对象 | 接入研发 |
| 5 | Agent 反查应用,得到约束范围 | InnerAgent(自动) |
| 6 | SDK 渲染微前端:对话页/记录页/配置页(可配样式、主题、颜色) | SDK(自动) |
| 7 | 用户对话,经 MCP 操控应用 | InnerAgent + 宿主 |
| 8 | (可选)注册三方 MCP,实现跨应用数据打通 | 应用管理员 |

#### 5.3.1 对话与工具调用主流程

```text
用户输入 → SDK 上报上下文(user/page/object) → 创建 Run(内核快照)
  → 模型推理 ─┬─ 输出内容/思考过程 ──► SSE 流式下发(SDK 渲染)
              ├─ 调用工具 ─► 工具中枢 ─► 权限判定 ─┬─ 只读:直接执行
              │                                    └─ 写操作:推送确认卡片
              │                                         └─ 用户批准 → 执行 → 结果回灌模型
              └─ 调起子 Agent → 子 Run(受父超时约束) → 结果回灌
→ 运行结束(事件落日志,消息投影入库,用量结算)
```

#### 5.3.2 断线恢复流程

前端断网/刷新 → SDK 携带 `Last-Event-ID(runId:seq)` 重连 → 服务端从事件日志回放缺失部分 → 无缝衔接实时流;若运行已结束则直接返回终态。运行进程崩溃时由维护调度器按 lease 接管或标记失败,用户可一键续跑。

---

## 6. 功能需求

优先级定义:**P0** = 首版必须(MVP);**P1** = 第二迭代;**P2** = 规划中、待验证后启动。

### 6.0 功能清单总览

| 模块 | 功能 | 优先级 |
| --- | --- | --- |
| M1 Agent 引擎 | 会话管理、运行管理、流式输出、上下文注入、约束范围反查 | P0 |
| M2 工具中枢 | 应用工具注册、工具白名单、权限模式、确认流、三方 MCP | P0(三方 MCP 为 P1) |
| M3 模型接入 | 多协议 Provider、模型配置、连通性测试 | P0 |
| M4 Skill 体系 | 内置 Skill 库、用户/应用 Skill 导入与激活 | P1 |
| M5 mini 知识库 | 文档导入、分段、无向量检索、Agent 挂载 | P1 |
| M6 子 Agent | 子 Agent 定义、调起、事件归属 | P1 |
| M7 前端 SDK | 初始化、上下文上报、微前端渲染、用户反馈标注、主题定制 | P0(主题定制全量为 P1) |
| M8 管理后台 | Agent 配置、工具/模型/Skill/KB 管理、审计、紧急停用、工具体检、用量 | P0(用量与工具体检为 P1) |
| M9 安全底座 | 多应用隔离、审计日志、附件与工作区、用量结算 | P0(结算对接 P2) |

### 6.1 M1 · Agent 引擎

#### 6.1.1 会话管理(P0)

| 功能点 | 规则 |
| --- | --- |
| 会话创建 | 用户在对话页发起首条消息时自动创建;归属 `应用 + Agent 实例 + 用户` |
| 会话列表/历史 | 记录页按时间倒序展示;支持加载指定会话的完整消息(含工具调用卡片与思考过程) |
| 会话标题 | 首轮对话后自动生成标题,管理员与用户可改名 |
| 会话删除 | 仅本人(或应用管理员)可删;删除为逻辑删除,默认保留 180 天后物理清理,保留期应用级可配(ADR-9) |
| 会话隔离 | 严格按用户隔离;同应用不同用户互不可见 |

#### 6.1.2 运行管理(P0)

| 功能点 | 规则 |
| --- | --- |
| 单会话单运行 | 同一会话同一时刻只允许一个活跃运行;重复提交返回明确错误码 |
| 运行状态机 | `排队 → 运行中 → (等待确认 | 等待工具) → 成功 / 失败 / 已取消` |
| 内核快照 | 每次运行固化 Agent 定义 + 工具清单(schema sha256);配置变更不影响进行中的运行 |
| 运行取消 | 用户可随时取消;进行中的工具调用收到取消信号后中止,已执行成功的写操作不回滚(以审计为准) |
| 失败续跑 | 失败/取消的运行可从断点继续(continue),不重复扣模型调用 |
| 崩溃接管 | 运行带 lease 与心跳、deadline;实例崩溃后由维护调度器接管或置失败 |
| 等确认超时 | 写操作确认等待超时可配(默认 24h),超时自动拒绝并通知;等待确认状态不占用运行 lease,支持跨会话/跨端恢复,超时拒绝后运行可续跑 |
| 资源上限 | 单运行强制限制:最大工具调用次数、最大 token 消耗、最长运行时长、失败重试上限(默认值由技术方案定);超限自动终止并向用户明确说明原因 |
| 终态通知 | 运行到达终态时通过 Webhook 回调宿主应用注册的回调地址(带签名验证与失败重试),由宿主自行提醒用户(P1) |

#### 6.1.3 流式输出(P0)

| 功能点 | 规则 |
| --- | --- |
| 传输协议 | SSE,事件携带持久化 ID(`runId:seq`),支持 `Last-Event-ID` 断线续传 |
| 事件类型 | 内容增量、思考增量、工具开始/参数/结束、子 Agent 开始/结束、需外部确认/结果、完成/出错/取消 |
| 事件整形 | 服务端做合并(约 50ms 窗口)与背压限制,防止刷屏与内存膨胀 |
| 消息持久化 | 事件日志为唯一事实源;消息表为投影,供记录页按普通消息口径读取 |

#### 6.1.4 上下文注入与约束范围反查(P0,本产品核心机制)

| 功能点 | 规则 |
| --- | --- |
| 前端上下文 | SDK 每轮携带:`userId`、`pageId/pageName`、`objectId/objectType`(当前页、当前对象),可扩展自定义键值 |
| 标准反查工具 | InnerAgent 内置 `resolve_scope` 工具约定:宿主应用在 MCP 中实现该方法,返回当前上下文下的可见对象域、可写字段、禁止操作清单 |
| 注入方式 | 上下文写入系统提示词变量 + 运行上下文对象;工具调用自动附带 scope 派生参数 |
| 缺省行为 | 前端未上报上下文时,Agent 必须先调用 `resolve_scope` 澄清,禁止凭空猜测对象 |
| 边界声明 | InnerAgent 传递与呈现 scope,**不做权限判定的最终裁决方**;越权拦截由宿主应用工具侧执行(见 ADR-3) |
| 降级行为 | 宿主未实现 `resolve_scope` 时,退化为「无上下文提示 + 全量白名单工具 + 写操作一律确认」;接入文档将其标注为推荐实现,并在接入体检中提示 |

### 6.2 M2 · 工具中枢

#### 6.2.1 应用工具注册(P0)

| 功能点 | 规则 |
| --- | --- |
| 注册方式 | 管理后台录入宿主应用 MCP 端点(凭证 + 传输方式),自动拉取工具清单(schema) |
| 工具清单 | 展示工具名(模型可见名 = 服务器前缀 FQN,如 `mcp__<server>__<tool>`)、描述、参数 schema、只读/写标记、并发安全性、风险等级 |
| 策略字段 | 每工具含:管理员策略(force-ask / force-allow / deny,优先级高于用户授权)、`resumeSafe`(可被 continue 重执行,默认取 MCP `idempotentHint`)、注解缓存(`readOnlyHint/destructiveHint/idempotentHint/openWorldHint`,仅可信宿主服务器采信,作策略软输入;三方工具一律确认) |
| 风险等级 | 注册时由注解生成默认值,人工确认/覆盖;工具体检可按描述与 schema 建议风险级 |
| schema 变更 | 活刷新+差异分诊:纯增量变更自动接受并留审计;安全相关差异(工具名集合变化、新增必填参数、readOnlyHint true→false)强制重新确认后生效;进行中运行按快照不受影响 |
| 冲突保护 | 工具名应用内唯一(多应用共享部署时以应用为命名空间隔离,见 ADR-10);与内置工具、三方工具重名时拒绝注册 |
| 健康检查 | 支持手动与定时连通性测试,异常时在后台标红并通知;订阅 MCP `tools/list_changed` 通知触发刷新 |
| 注销/停用 | 停用后从所有 Agent 白名单摘除;进行中的运行按快照继续,新运行不可见;**级联清除该工具的全部用户授权** |

#### 6.2.2 工具权限模式与确认流(P0)

| 权限模式 | 行为 | 默认 |
| --- | --- | --- |
| DEFAULT | 只读工具自动执行;写操作需用户确认 | ✅ |
| ALWAYS_ASK | 所有**写**操作必确认,且不可被任何用户授权绕过(= DEFAULT 减去授权通道);不对只读调用逐次询问——《04-调研》V19:业界无逐读确认先例,且与本产品 R3(确认疲劳)矛盾 | 可选 |
| ALWAYS_ALLOW | 全部自动执行(仅限低风险场景,记录审计) | 可选 |
| FULL_ACCESS | 自动执行且放开参数级限制,仅平台管理员可开;开启时审计记录一次性确认;**宿主侧强制校验在本模式下依然生效**;应用/Agent 实例配置不得自激活(对齐 Claude Code bypass 护栏) | 可选 |

确认流规则:

- 确认卡片展示:工具名、参数(人话化摘要)、风险等级、影响对象;授权四档:"本次允许 / 本会话允许 / 总是允许(该工具)/ 拒绝"。
- 用户拒绝时,拒绝原因回灌模型,Agent 可调整方案而非直接终止;**拒绝文案必须陈述后果**(如「记录未被修改」),否则模型会误当作已完成变更的迭代反馈而漂移(Cline 实测教训)。
- 批量确认仅限同轮并行调用:逐项列明(名称/参数/风险),支持 全部允许 / 全部拒绝 / 逐项过;批量内禁用「总是允许」;拒绝按项审计(业界无合并确认卡先例,《04-调研》V21)。
- 风险等级由工具注册方声明 + InnerAgent 校验;删除类、资金类、凭据类工具后台强制标为高危。
- 确认优先级:平台/应用的强制确认策略 > 用户「总是允许」选择;被强制确认的工具不提供「总是允许」按钮;工具升级为高危时,存量「总是允许」授权自动失效并通知用户。

#### 6.2.3 三方 MCP(P1)

| 功能点 | 规则 |
| --- | --- |
| 注册层级 | 应用级(管理员注册,全员可用)与用户级(用户自接,仅本人可用) |
| 能力 | 连通性测试、启用工具子集、超时配置、凭据加密存储 |
| 能力 | 连通性测试、启用工具子集、超时配置、凭据加密存储;接入认证支持规范 OAuth 2.1(CIMD/DCR、audience 绑定)或静态头,act token 不出宿主桥内环 |
| 隔离规则 | 用户级工具不得遮蔽系统工具名;三方工具默认按写操作确认处理 |

#### 6.2.4 用户授权管理(「总是允许」)

| 功能点 | 规则 |
| --- | --- |
| 授权存储 | 独立授权记录:用户、工具 FQN、作用域(会话/永久)、授予时风险等级、工具 schema 指纹、来源、时间 |
| 作用域 | 「本会话允许」随会话结束失效;「总是允许」不过期(业界无 TTL 先例),撤销走配置页授权列表 |
| 失效 | 工具风险等级升级或安全相关 schema 变更时,存量授权自动失效并通知;被 force-ask 策略覆盖的工具不提供授权入口 |
| 撤销与清理 | SDK 配置页提供授权列表(含来源与作用域)与逐条撤销;工具注销级联清除 |

### 6.3 M3 · 模型接入(P0)

| 功能点 | 规则 |
| --- | --- |
| 协议支持 | OpenAI 兼容、Anthropic、Gemini、DashScope、Ollama 五类协议,与参考实现对齐 |
| 配置管理 | API 配置(端点/密钥/代理)与模型(类型、上下文窗口、多模态能力、推理档位、并发上限)分离管理;密钥加密落库,不进代码 |
| 能力声明 | 模型声明支持能力(视觉/推理/多模态输入类型)与上下文窗口;引擎据此做校验与上下文压缩 |
| 连通性测试 | 保存前测试文本连通;模型配置变更通过配置指纹使内核缓存失效 |
| 多模型路由 | Agent 实例可绑定主模型;按 Agent 实例粒度选择,不做全局动态路由(P2 评估) |
| 合规建议 | 国内生产环境推荐 DashScope 或私有化 Ollama;OpenAI/Anthropic/Gemini 协议面向开发调试与海外业务场景(见 R7) |

### 6.4 M4 · Skill 体系(P1)

| 功能点 | 规则 |
| --- | --- |
| Skill 形态 | 目录包:`SKILL.md`(说明)+ 资源文件;兼容参考实现的 Classpath/文件系统仓库 |
| 内置仓库 | 平台内置基础 Skill(随版本发布);应用可挂载自有 Skill 仓库 |
| 导入 | zip 上传,先预览校验(结构、大小、脚本声明)再导入;单个 Skill 内容上限 128KB |
| 激活 | 用户每轮可激活的 Skill 数量有上限(默认 8);激活列表随运行快照固化 |
| 用户隔离 | 用户自定义 Skill 仅本人可见可用 |

### 6.5 M5 · mini 知识库(P1,无向量)

**产品约束:不做向量库、不做 Embedding 服务。** 面向"千级文档、十万级分段"以内的轻量检索。

| 功能点 | 规则 |
| --- | --- |
| 导入 | 支持 txt/md/html/pdf/docx 文本抽取;单文档 ≤ 5MB,单库 ≤ 1000 文档(超限提示拆库) |
| 分段 | 按标题/段落 + 最大长度切分,保留来源(文档名/章节/锚点) |
| 检索 | 关键词 + 倒排索引 + 结构化过滤(标签/元数据);中文分词内置,可配同义词表 |
| 挂载 | 知识库挂载到 Agent 实例;命中片段作为引用注入上下文,回答须可溯源(展示来源) |
| 管理 | 增删改查、重建索引、失效文档标黄 |

### 6.6 M6 · 子 Agent(P1)

| 功能点 | 规则 |
| --- | --- |
| 定义 | 子 Agent = 独立系统提示词 + 受限工具集 + 输出约定;在 Agent 实例中以"子 Agent 工具"形式声明(含参数/输出 schema) |
| 执行 | 调起子 Agent 即创建子运行(parentRunId/parentToolCallId 关联),独立事件流;父取消级联取消子 |
| 约束 | 子运行 deadline 不得超过父运行;子 Agent 不可再调起子 Agent(首版一层) |
| 权限继承 | 子运行经内核快照继承父运行的模式与强制确认策略(不读取实时配置);需确认的调用,确认卡片浮到父会话 UI;子 Agent 工具自身需确认时,由父确认预授或阻止(Goose 子代理硬编码 Auto 的前车之鉴) |
| 展示 | 前端按父子层级渲染事件与结果 |

### 6.7 M7 · 前端 JS SDK(P0)

| 功能点 | 规则 |
| --- | --- |
| 初始化 | `InnerAgent.init({ appKey, tokenGetter, theme?, mode? })`;`tokenGetter` 回调由宿主提供 token(支持过期懒换),SDK 不接触模型密钥 |
| 嵌入形态 | 双形态支持(ADR-8):默认 **Web Component**(样式隔离 + CSS 变量与宿主主题联动);宿主环境受限(老旧框架、严格 CSP)时提供 **iframe** 模式,两种形态 API 一致 |
| 上下文上报 | `setUser / setPage / setObject` 三个标准方法 + 通用 `setContext(k,v)`;每轮对话开始时自动携带(不流中变更);SDK 提供上下文检视面板,可按项停用自动附带 |
| 微前端渲染 | 三个可嵌入视图:对话页(默认)、记录页(会话历史)、配置页(用户级 Skill/MCP 管理);可整页嵌入或侧边抽屉 |
| 流式渲染 | Markdown 渐进渲染、思考过程折叠、工具卡片(参数/结果/确认按钮)、子 Agent 嵌套时间线 |
| 附件 | 图片/文件上传,走宿主应用存储或 InnerAgent 工作区(可配);类型与大小按模型能力校验 |
| 主题定制 | 颜色、圆角、字号、明暗主题通过 theme 参数配置(P0 提供主色与明暗;完整设计令牌 P1) |
| 生命周期 | 会话切换、路由变化不串流;页面卸载自动断开;提供 `destroy` 清理 |
| 用户反馈 | 每条助手消息提供 👍/👎 与「已解决/未解决」轻量标注(P0),作为任务完成率的度量通道(§2.3) |
| 鉴权生命周期 | token 过期时 SDK 暂停输入并提示重新鉴权;进行中的运行由服务端继续执行,重新鉴权后重连无缝恢复 |
| 多标签页 | 同一会话在第二个标签页打开时进入只读跟随模式(实时流 + 历史回放),不重复建立运行 |

### 6.8 M8 · 管理后台(P0,双形态:用户级配置页微前端 + 应用级/平台级独立管理站,ADR-7)

| 功能点 | 优先级 | 规则 |
| --- | --- | --- |
| Agent 实例配置 | P0 | 人设提示词、模型绑定、工具白名单、上下文模板变量、默认开场白 |
| 工具管理 | P0 | MCP 注册/测试/停用、风险等级标记、白名单分配 |
| 模型管理 | P0 | API 配置、模型清单、连通性测试 |
| 审计日志 | P0 | 按用户/会话/工具检索;记录工具名、参数、结果、风险级、确认人与耗时 |
| 紧急停用 | P0 | 应用级 Agent 总开关与单运行终止按钮;出现失控循环、成本异常等熔断场景时一键停用并通知管理员 |
| 工具体检 | P1 | 自动检查工具描述长度、schema 完整性、只读标记、命名规范并评分,不合格项给出整改建议(R1 的应对落档) |
| 用量统计 | P1 | 按 应用/Agent/用户/模型 维度的 token 与调用次数统计;支持导出 |
| 结算对接 | P2 | 与平台计费/onetoken 类结算体系对接 |
| 租户对接 | P1 | 对齐《03-租户体系设计-通用方案》:应用 ↔ 租户映射,行级 tenant 隔离 |

### 6.9 M9 · 安全底座(P0)

| 功能点 | 规则 |
| --- | --- |
| 应用隔离 | 以 App 为一等实体;数据(会话/运行/工具/Skill/KB)全部按 App 隔离;首版为单应用私有化部署,一应用一套(ADR-10),数据模型自始携带应用维度,多应用共享部署为 P2 |
| 身份传递 | SDK token ↔ 用户身份映射由宿主后端签发;工具调用强制携带原始用户身份,杜绝匿名执行 |
| 审计 | 每次工具调用一条审计(含被拒绝的);日志保留 ≥ 180 天,防篡改(仅追加);工具参数中 password/token/secret/key 类敏感字段自动打码,审计侧存脱敏值或哈希;每条记录携带 **decision_source**(mode-default / user-grant / forced-policy / live-confirm / full-access),使「高危 100% 确认」可从日志证明 |
| 数据保留 | 会话/消息/运行数据默认保留 180 天,应用级可配置(ADR-9);到期物理清理,审计日志不受影响 |
| 附件与工作区 | Agent 文件工作区支持 数据库/本地/对象存储 三后端,按用户隔离;可配保留期与清理策略 |
| 密钥安全 | 模型/MCP 凭据加密存储;日志与审计中脱敏 |
| 失败封闭 | 会话状态保存失败时阻断模型调用(fail-closed),宁可失败不可失去一致性 |
| 内容安全 | 不内置审核引擎(N12);审核接入点按 tripwire 三类建模——input(仅首条)/ output(仅终答)/ tool(包裹每次工具调用),可配审核服务或回调宿主审核接口;命中按策略拦截/改写/放行并审计,可选廉价分类模型前置筛查 |

---

## 7. 产品边界

### 7.1 做什么(In Scope)

1. Agent 运行时:会话、持久化运行、流式、重连、续跑、子 Agent、运行终态 Webhook 通知(P1)。
2. 工具中枢:MCP 客户端(应用工具 + 三方工具)、白名单、权限模式、确认流。
3. 配置体系:Agent 实例、Skill、mini 知识库、模型接入。
4. 前端 SDK 与微前端三视图(对话/记录/配置),主题可配。
5. 安全底座:应用隔离、审计、用量统计。

### 7.2 明确不做什么(Out of Scope)

| # | 不做 | 理由 / 归属 |
| --- | --- | --- |
| N1 | 不做独立 to C 对话产品/门户/Agent 商店 | InnerAgent 只以内嵌形态存在,"只服务本应用"是产品红线 |
| N2 | 不做宿主业务功能本身 | 业务能力一律由宿主应用以 MCP 工具提供;InnerAgent 不内嵌任何具体业务逻辑 |
| N3 | 不做权限最终裁决 | 权限模型与越权拦截在宿主应用工具侧;InnerAgent 只负责身份传递、范围注入与审计 |
| N4 | 不做向量 RAG / Embedding / 大规模知识中台 | mini 知识库明确无向量、有规模上限;超规模需求另行立项 |
| N5 | 不做模型训练/微调/私有化模型托管 | 只做接入与路由 |
| N6 | 不做 Agent 长期记忆(跨会话个性化记忆) | 首版关闭;P2 评估按 App 粒度的受控记忆 |
| N7 | 不做 Skill 脚本执行沙箱(首版) | Skill 仅提示词与资源;脚本执行待安全方案落地后单独评审(参考实现亦为待办) |
| N8 | 不对外暴露 InnerAgent 自身为 MCP Server | InnerAgent 是 MCP 消费端;把聚合工具再对外输出不属于本产品边界 |
| N9 | 不做多模态生成(图片/视频/语音合成) | 参考实现中的生成能力属于融光业务,不属于通用底座;如宿主需要,以宿主 MCP 工具形式提供 |
| N10 | 不做通用聊天协议兼容(微信/钉钉机器人等) | 只做 Web 内嵌;其他渠道 P2 评估 |
| N11 | 不做通用撤销/事务回滚 | 已执行的写操作不回滚(§6.1.2);业务级回滚(如「恢复旧值」「重新生成」)由宿主以工具形式提供 |
| N12 | 不内置内容审核引擎 | 仅提供审核接入点(§6.9);内容合规责任由宿主与其选择的模型服务承担(见 R7) |

### 7.3 与参考实现(mmagix-minicuts)的裁剪对照

| 参考实现能力 | InnerAgent 取舍 |
| --- | --- |
| 持久化运行(lease/事件日志/outbox/回放) | ✅ 全量继承,作为内核 |
| 工具权限四模式 + 确认流 + 批量确认 | ✅ 全量继承 |
| MCP 客户端(系统级 + 用户级) | ✅ 继承,系统级更名为"应用工具" |
| Skill(内置 + 用户导入,激活上限) | ✅ 继承;脚本沙箱不做(N7) |
| 子 Agent(子运行、级联取消、一层限制) | ✅ 继承 |
| 五协议模型接入 + 配置指纹缓存失效 | ✅ 继承 |
| 多租户行级隔离 + 用量结算 | ✅ 隔离继承;结算降为 P2 |
| 业务工具(剧本/分镜/素材/生成等 30+ 工具) | ❌ 剥离,归融光业务 |
| 图片/视频生成任务体系、ComfyUI/各厂商适配 | ❌ 剥离(N9) |
| Agent 文件工作区(三后端) | ✅ 继承,作为附件/产物存储 |
| 项目/团队/创作域数据模型 | ❌ 剥离 |

### 7.4 外部依赖

| 依赖 | 说明 | 失败策略 |
| --- | --- | --- |
| LLM 服务商 | 对话与推理的唯一外部强依赖 | 多协议可切换;服务商故障时运行置失败,支持续跑 |
| 宿主应用 MCP | 业务操作执行方 | 工具超时/异常回灌模型,由 Agent 决定重试或告知用户 |
| Redis | 流分发、唤醒/取消信号 | 不可用时降级为实例内直连,放弃跨实例接管 |
| PostgreSQL | 会话/运行/事件/审计持久化 | 主存储,无降级;fail-closed |

---

## 8. 非功能需求

| 维度 | 要求 |
| --- | --- |
| 性能 | 首字延迟(用户发送到首个流式事件)P95 ≤ 3s(不含模型首 token);SSE 事件端到端透传延迟 P95 ≤ 200ms;工具确认推送延迟 P95 ≤ 1s |
| 容量 | 单部署支撑 1000 并发会话、100 并发运行;事件写入吞吐 ≥ 500 events/s(首版容量目标,上线前以压测校准) |
| 可用性 | 服务可用性 ≥ 99.9%;运行引擎崩溃后 30s 内完成 lease 接管或标记失败 |
| 一致性 | 事件日志为唯一事实源,消息投影可随时重建;同会话运行严格串行 |
| 安全 | 见 6.9;审计日志防篡改;密钥加密;越权事件 0 容忍 |
| 可观测 | 按 OTel GenAI 语义约定输出:run=`invoke_agent`、模型调用=`chat`(token 用量/结束原因)、工具调用=`execute_tool`、宿主 MCP=MCP client span(`mcp.client.operation.duration` 指标);`gen_ai.conversation.id`=真实会话 ID;消息内容属性默认关闭(对齐审计脱敏);runId 作 trace 关联 ID,经薄封装层防约定演进返工 |
| 兼容 | SDK 支持 Chrome/Edge/Safari 近两年主版本;微前端不污染宿主全局样式(shadow 隔离或命名空间);SDK 与服务端 API 保持 N-1 版本兼容,宿主不被迫同步升级;SDK 文案语言可配(首版中/英),对话组件满足键盘可达与读屏基线 |
| 部署 | Docker Compose 单机起步,支持前后端分离与独立扩展;配置全部环境变量化 |

---

## 9. 数据与埋点

### 9.1 核心事件

| 事件 | 关键属性 |
| --- | --- |
| agent_init | appKey、视图类型、主题 |
| conversation_created | agentId、入口页面 |
| run_started / run_finished | runId、耗时、终止原因(成功/失败/取消/超时) |
| tool_called | 工具名、来源(应用/三方/内置)、风险级、结果(成功/失败/被拒)、耗时 |
| confirmation_shown / confirmed / rejected | 工具名、响应时长、决策 |
| reconnect_success | 断开时长、回放事件数 |
| kb_hit | 知识库 ID、命中分段数、引用展示与否 |
| skill_activated | Skill 名、来源(内置/导入) |
| feedback_received | 消息 ID、反馈类型(👍/👎/已解决/未解决) |
| run_continued | runId、续跑前终止原因 |
| tool_timeout | 工具名、超时时长、是否重试 |
| scope_resolved | 结果(命中/降级——宿主未实现 resolve_scope)、耗时 |

### 9.2 指标看板

北极星:任务完成率。过程指标:工具调用成功率、确认通过率、平均确认响应时长、断线重连成功率、人均会话数、token 成本/应用。

---

## 10. 风险与开放问题

| # | 风险/问题 | 影响 | 应对 | 状态 |
| --- | --- | --- | --- | --- |
| R1 | 宿主 MCP 工具设计质量参差(描述不清、粒度不当)导致 Agent 答非所问 | 任务完成率不达标 | 《工具设计规范》纳入 M1 接入文档包;后台工具体检为 P1 功能项(§6.8) | 已落档 |
| R2 | 前端上下文上报不可信(伪造 objectId) | 越权风险 | 身份由宿主后端签发 token 绑定;宿主工具侧二次校验;审计可追 | 设计中 |
| R3 | 高危操作(改密码、删数据)确认疲劳,用户无脑点允许 | 误操作 | 高危二次确认(输入对象标识);后台可按工具强制"每次必确认" | 开放 |
| R4 | mini 知识库无向量检索的召回上限 | 长尾问题答不好 | 明确规模上限并写进边界;命中率指标监控,超阈值引导升级方案 | 已接受 |
| R5 | 三方 MCP 的数据外泄(把 A 系统数据发给 B 系统) | 合规风险 | 三方工具默认确认流 + 审计明示数据流向;应用管理员可禁用跨源组合 | 开放 |
| R6 | 模型成本失控 | 成本 | 用量看板 + Agent/用户级限额(P1);超限降级提示 | 规划 |
| R7 | 用户输入或模型输出包含违禁内容;海外模型协议用于国内商用 | 合规风险 | 审核接入点可配(N12、§6.9);国内生产推荐 DashScope/Ollama(§6.3);内容合规责任由宿主最终承担 | 开放 |
| Q1 | ~~配置页以微前端提供给应用管理员,还是 InnerAgent 自带独立管理站?~~ | 影响部署形态 | **已拍板(2026-09-20):双支持**——用户级配置走微前端,应用级/平台级管理走独立管理站 | 已拍板 → ADR-7 |
| Q2 | ~~SDK 技术形态:Web Component 还是 iframe?~~ | 影响样式隔离与主题联动 | **已拍板(2026-09-20):双支持**——默认 Web Component,iframe 兜底受限宿主环境 | 已拍板 → ADR-8 |
| Q3 | ~~会话数据的保留期限默认值(审计要求 vs 用户隐私)~~ | 合规 | **已拍板(2026-09-20):默认 180 天,应用级可配置** | 已拍板 → ADR-9 |

---

## 11. 里程碑规划

| 里程碑 | 范围 | 验收标准 |
| --- | --- | --- |
| M0 · 内核验证(W1–W4) | 引擎骨架:会话/运行/SSE/重连 + MCP 工具中枢 + starter + DEFAULT 权限(对应《03-开发计划》P0–P1) | 用例 U5(只读直通)与 U1(写操作 + 确认 + 审计)全流程走通 |
| M1 · MVP(W5–W6) | M1/M2(除三方 MCP)/M3/M7(基础主题)/M8(Agent、工具、模型、审计、紧急停用、终态 Webhook、工具体检 v1)/M9 隔离(对应 P2) | 验收锚点 U1–U3、U5 全部通过;接入演练 ≤ 2 人日;交付《工具设计规范》接入文档包(内容框架按《04-调研》§3.5.4 五条) |
| R1 · 融光反向替代灰度(W7–W12) | 融光 starter 接入、Agent 定义导出、flag 双跑、等价性对照、灰度切流(PRD 外新增试点路径) | 100% 切流且稳定 ≥ 2 周 |
| M2 · 增强(W13–W16) | 三方 MCP、Skill 体系、mini 知识库、子 Agent(含权限继承)、用量统计、性能压测、融光删除批次①② | U4 与场景 C(跨系统)、场景 D(文档生成)走通 |
| M3 · 平台化(P2 启动后) | 结算对接、租户体系对齐、(评估)受控记忆、渠道扩展 | 按 03-租户方案完成对接验收 |

---

## 12. 关键设计决策记录(ADR)

| # | 决策 | 理由 | 放弃的方案 |
| --- | --- | --- | --- |
| ADR-1 | 以 MCP 作为工具统一协议(应用工具、三方工具同为 MCP) | 接入标准化,宿主只需实现协议即可接入;生态工具可直接复用 | 自研 RPC 工具 SDK(绑定语言、接入成本高) |
| ADR-2 | 事件日志为事实源,消息表仅投影 | 支撑断线重放、续跑、审计与可复现(参考实现已验证该架构) | 消息表即事实源(无法回放与接管) |
| ADR-3 | 权限判定归宿主应用,InnerAgent 只传递身份与范围 | 业务语义(谁能改什么)只有宿主知道;中台集中裁决必然失准且责任不清 | InnerAgent 集中权限模型 |
| ADR-4 | mini 知识库无向量,设规模上限 | 部署轻、成本零、规模内够用;与"极简接入"定位一致 | 引入向量库(部署与成本翻倍) |
| ADR-5 | 运行级内核快照(定义+工具 schema 哈希) | 配置热更不撕裂进行中的对话;审计可复现 | 实时读取最新配置(运行中行为漂移) |
| ADR-6 | 首版关闭长期记忆与 Skill 脚本执行 | 安全边界未验证前不放入内核;先把"操控行"做稳 | 全量对齐参考实现 |
| ADR-7 | 管理形态**双支持**:用户级配置页 = 微前端(对话页同源嵌入),应用级/平台级管理 = InnerAgent 独立管理站(2026-09-20 拍板) | 终端用户配置必须内嵌;管理员配置低频、需要全局视图与跨应用管理 | 只做其一(内嵌管理页体验受损 / 独立站割裂用户配置入口) |
| ADR-8 | SDK **双形态**:默认 Web Component,提供 iframe 模式,两者 API 一致(2026-09-20 拍板) | Web Component 样式隔离好、主题可与宿主 CSS 变量联动;iframe 兜底老旧框架、严格 CSP 等受限宿主环境 | 单一形态(放弃部分宿主环境) |
| ADR-9 | 会话/消息/运行数据**默认保留 180 天,应用级可配置**;审计日志独立保留 ≥ 180 天且仅追加(2026-09-20 拍板) | 不同应用合规与审计要求不同;审计与业务数据保留期解耦,清理互不影响 | 全局固定保留期 |
| ADR-10 | 首版**单应用私有化部署**(一应用一套);多应用共享部署为 P2,对齐《03-租户体系设计》后再启动 | 隔离模型与责任边界最简单,接入与运维成本最低;多应用的真实需求与计费模型验证后再平台化 | 首版即做多应用共享(隔离、计费、命名空间复杂度前置,拖慢首个宿主接入) |

---

## 附录 A · 参考实现能力对照

参考实现关键资产(mmagix-minicuts/ai-fusion-video,`com.stonewu.fusion.service.ai`):

- 运行引擎:`AgentRunCoordinator`(准入事务、lease/epoch、状态会话 fencing)、`AgentEventJournal` + Outbox 发布、`AgentRunReplayService`(回放+实时衔接)、消息投影服务。
- 内核:`AgentScopeHarnessFactory`(Toolkit 并行、状态存储、权限上下文、按上下文窗口压缩、禁用内置文件系统/Shell/记忆/子代理后按需显式启用)、内核 LRU 缓存与指纹失效。
- 工具:`ToolExecutor` SPI(名称/描述/schema/只读/并发安全/风险)自动注册、白名单交集、schema sha256 清单锁定、资源归属校验。
- 确认流:确认等待状态(超时可配)、批量确认、超时协调器。
- MCP:系统级(配置)与用户级(库表 + LRU 缓存、防遮蔽)、连通性测试。
- Skill:仓库优先级合并、zip 导入预览校验、激活上限。
- SSE:持久化事件 ID(`runId:seq`)、Last-Event-ID 重连、事件整形(合并/背压)。
- 底座:多租户行级隔离贯穿工具执行上下文、模型用量按租户结算、状态存储 fail-closed、状态保留清理策略。

## 附录 B · 术语速查

| 缩写 | 全称 |
| --- | --- |
| MCP | Model Context Protocol |
| SSE | Server-Sent Events |
| SDK | Software Development Kit |
| KB | Knowledge Base(本产品特指无向量 mini 知识库) |
| ADR | Architecture Decision Record |
