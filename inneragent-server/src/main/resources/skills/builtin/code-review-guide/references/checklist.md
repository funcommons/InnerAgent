# 自检清单明细

## 正确性
- 单元测试是否覆盖新分支；不覆盖时说明原因；
- 迁移/配置变更是否兼容滚动发布（新旧实例并存窗口）。

## 边界条件
- 数值边界：0、负数、溢出、精度；
- 文本边界：空串、BOM、不可见字符、超长截断。

## 并发与资源
- 锁的获取顺序是否一致；超时是否有兜底；
- 缓存失效路径是否与写入路径同事务语义。

## 可读性
- 公共 API 变更是否更新了文档与注释；
- 日志是否包含可定位的上下文（ID、键值），且无敏感信息。
