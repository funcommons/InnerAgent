# InnerAgent 开发计划

> **版本 V1.1 · 2026-09-20** · 依据《01-PRD V1.3》《02-技术方案 V1.1》《04-对标调研》修订:W3 增授权管线/审计落库/确认不变式/FQN、W4 增附件上传、W5 增管理站认证与 OTel 工厂、P0 增 lease 行为核实、P4 增接管重投断言、风险表增补三条。

## 目录

- 1. 计划总览
- 2. 总体测试策略
- 3. P0 · 移植基座
- 4. P1 · 去业务化与工具中枢
- 5. P2 · 管理运维与 MVP 验收
- 6. P3 · 反向替代灰度(融光试点)
- 7. P4 · 增强、性能与替代收尾
- 8. P5 · 稳定期与收尾
- 9. 风险与应急
- 10. 度量与报告机制

---

## 1. 计划总览

### 1.1 计划依据与范围

- 依据:《01-产品需求文档-PRD》V1.2、《02-技术方案-内核移植与反向替代》。
- 范围:InnerAgent 微服务 + JS SDK + 宿主 starter + 管理站的全量开发;含融光(mmagix-minicuts)反向替代试点。
- 每阶段出口 = **验收标准全过 + 测试方案全绿 + 缺陷满足出口准则(§2.4)**,三者缺一不进入下一阶段。

### 1.2 阶段里程碑总表

| 阶段 | 周期 | 核心内容 | 出口里程碑 |
| --- | --- | --- | --- |
| P0 移植基座 | W1 | 仓库骨架 + 内核整包移植编译通过 + `ia_` schema | 引擎本机跑通单轮对话 |
| P1 去业务化与工具中枢 | W2–W4 | 身份双层、双级令牌、MCP 工具中枢、starter、SDK core+chat | demo-host 上 U5/U1 全流程(=PRD M0) |
| P2 管理运维与 MVP | W5–W6 | 管理站、审计、熔断、Webhook、工具体检 | PRD M1 验收(U1–U3、U5 + 接入 ≤2 人日) |
| P3 反向替代灰度 | W7–W8 开发,W9–W12 灰度 | 融光 starter 接入、定义导出、双跑对照、灰度切流 | 100% 切流稳定 ≥2 周 |
| P4 增强与替代收尾 | W13–W16 | 三方 MCP、Skill、mini KB、子 Agent、性能压测、删除批次①② | PRD M2 验收 + 容量报告 |
| P5 稳定期 | W17 起 | 监控强化、缺陷修复、删除批次③④、复盘 | 稳定运行 2 周,项目转运营 |

### 1.3 与 PRD / 技术方案映射

| 阶段 | PRD 里程碑 | 技术方案章节 |
| --- | --- | --- |
| P0 | M0 前半 | §4.1 包映射、§5.1 schema |
| P1 | M0 后半 + M1 主体 | §4.2/4.3/4.4、§6 凭据、§8.4 starter |
| P2 | M1 | §4.7 资源上限、§7.3 Webhook、§8.3 管理站 |
| P3 | PRD 外新增(试点关键路径) | §9 反向替代 |
| P4 | M2 | §4.4 KB/Skill/子 Agent、§9.5 删除清单 |

### 1.4 人力

| 角色 | 人数 | 投入 |
| --- | --- | --- |
| 后端(InnerAgent) | 2 | P0–P4 全程 |
| 前端(SDK/管理站) | 1 | P0 起全程 |
| 后端(融光侧) | 1 | P3–P4 |
| QA | 0.5 | P2 起介入,P3 灰度期全职 |

---

## 2. 总体测试策略

### 2.1 测试金字塔与工具链

| 层 | 工具 | 门禁时机 |
| --- | --- | --- |
| 单元测试 | JUnit5(server/starter)、Vitest(sdk/web) | 每次 PR |
| 集成测试 | Testcontainers(PG17+Redis,server/starter/demo-host 同网络) | 每次 PR |
| 契约测试 | JSON schema 快照(SSE 事件、MCP 工具清单、管理 API) | 每次 PR |
| E2E | Playwright(驱动 demo-host 页面与 SDK) | 每日 nightly + 发版前 |
| 等价性对照 | 自研 golden-diff(两侧事件序列归一化 diff) | P3 每次灰度升档前 |
| 性能 | k6(SSE 长连接场景)+ PG/Redis 监控 | P2 基线、P4 全量 |
| 混沌/故障注入 | 脚本化(kill/停容器/断网) | P2 子集、P4 专项周 |
| 安全 | 用例矩阵(越权/令牌/注入/脱敏)+ Skill zip 攻击面 | P2、P4 |

### 2.2 测试环境矩阵

| 环境 | 形态 | 用途 |
| --- | --- | --- |
| local | docker compose(pg/redis/server/web/demo-host) | 开发自测 |
| ci | CI runner + testcontainers | PR 门禁 |
| staging | 与融光试点同构部署 | E2E、性能、混沌、灰度预演 |
| pilot | 融光生产,flag 灰度 | P3 双跑与切流 |

### 2.3 CI/CD 门禁

- **PR 必须**:`./mvnw test`(server+starter)、`pnpm verify`(sdk/web:类型+lint+单测)、契约快照无未审变更。
- **nightly**:E2E 全量 + smoke 脚本(§3.4)+ 混沌子集(实例 kill 接管)。
- **发版**:tag → staging 全量(含性能抽样)→ 生产;生产发布单独立审批。
- 移植代码提交规范:复制类 commit 统一 `[port]` 前缀,改造类 `[adapt]`,保证 §3.4 差异审计可执行。

### 2.4 缺陷分级与出口准则

| 级别 | 定义 | 阶段出口准则 |
| --- | --- | --- |
| P0 | 数据错乱/越权/安全漏洞/引擎死锁 | = 0 |
| P1 | 主流程不可用(对话、确认、重连失败)且无 workaround | ≤ 2 且均有 workaround 与修复排期 |
| P2 | 体验/次要功能缺陷 | 不阻断,全部登记 |

### 2.5 通用 DoD(每个工作项)

代码评审通过;对应测试层有用例且绿;文档同步(接入指南/API 契约变更记录);无未解释的 `[port]` 差异。

### 2.6 公共测试资产(随阶段建设)

| 资产 | 说明 | 建成于 |
| --- | --- | --- |
| `examples/demo-host` | 空白 Spring Boot 宿主:starter + 若干工具(user 查询/改密、商品简介读写、流程模板复制、登录记录查询)+ resolve_scope | P1 建,P2 扩 |
| `golden-diff` | 归一化(去 runId/seq/时间戳/RESOURCE_LIMIT)后 diff 两侧 SSE 事件序列 | P3 |
| `scripts/smoke-sse.sh` | 建会话→流→断开→Last-Event-ID 重连续流 | P0 |
| 灰度大盘 | 双侧并报:工具成功率、确认率、重连率、任务完成率代理指标 | P3 |

---

## 3. P0 · 移植基座(W1)

### 3.1 任务分解

| 日 | 任务 |
| --- | --- |
| D1–D2 | 仓库与模块初始化(server/web/sdk-js/starter/compose);Maven 与融光同栈(Spring Boot 3.5.14/Java 21/AgentScope 2.0.0 锁定) |
| D2–D4 | `[port]` 整包复制:run(75)、agentscope(71)、provider(13,去 GoogleFlow)、根 Service、entity、controller;包名 `com.stonewu.fusion→com.inneragent` |
| D3–D4 | `ia_` schema:从融光 5 个迁移文件改造(V1 起链,前缀改写 + `app_id` 列) |
| D4–D5 | 内置工具(时间、文件解析)走本地 ToolExecutor 路径;hardcoded 演示 Agent(定义暂为代码) |
| D5 | smoke 脚本 + compose 健康检查 |

### 3.2 交付物

可运行的 inneragent-server(compose 一键起)、`ia_` 初版 schema、移植差异审计报告、smoke 脚本。

### 3.3 验收标准

1. `./mvnw test` 全绿;随包迁移的测试中 `@Disabled` ≤ 10 条且逐条登记原因。
2. curl 发起会话 → SSE 收到 CONTENT…DONE;`ia_agent_run/event/message` 三表落库,`runId:seq` 连续。
3. 断开重连(Last-Event-ID)补齐缺失事件,无丢失无重复。
4. compose up 后健康检查 200,日志无未处理异常。

### 3.4 测试方案

- **移植差异审计**(本阶段核心):脚本 diff 融光原包 vs 移植包,除 import/包名/身份对象替换外零差异;差异报告附评审记录。这是「行为等价」的第一道证据。**专项核实:融光「等待确认」是否占用运行 lease**——PRD 规则为不占用,若融光实现不同,登记为 P1 定向改造点(不阻塞主线)。
- 集成(Testcontainers):run 状态机遍历(成功/失败/取消终态齐备)、事件 journal 顺序性与 outbox 投递。
- 冒烟:smoke 脚本进 CI(P0 起即有最小回归网)。

---

## 4. P1 · 去业务化与工具中枢(W2–W4)

### 4.1 任务分解

| 周 | 任务 |
| --- | --- |
| W2 | 身份双层(AppContext/UserContext,ThreadLocal + MyBatis 双列拦截);embed token(宿主 RSA 签)与 act token(InnerAgent JWKS,claims 按 RFC 8693 建模);`ia_app` 与应用注册 |
| W3 | McpToolCatalog/McpToolAdapter(工具 FQN 命名、注解缓存、`tools/list_changed` 活刷新分诊);工具注册管理 API(schema 指纹、风险级默认+人工覆盖、管理员策略、resumeSafe);`ia_tool_grant` 授权管线(四档确认、自动失效、级联清理);`ia_audit_log` 落库最小版(含 decision_source、脱敏);`resolve_scope` 协议与降级;确认端点幂等+确认者绑定+决策先日志化;starter 四职责(MCP Server 桥、act 验签 Filter 与上下文重建、token 签发、resolve_scope 占位) |
| W4 | SDK core(移植 api/store,改 baseURL 与 tokenGetter);chat Web Component(移植 13 个 Vue 组件,去业务依赖);附件上传(SDK + `/ia/api/v1/attachments`,落工作区后端);demo-host 建成 |

### 4.2 交付物

starter 0.1、SDK 0.1(WC chat 视图)、demo-host、附件端点。(开放问题 Q1 已前置关闭:官方 MCP Java SDK v2.0.x,W3 集成验证为 starter 首任务。)

### 4.3 验收标准(= PRD M0 出口)

1. demo-host(全新空白应用)仅依赖 starter + yaml + 2 个 ToolExecutor,注册后 **U5 只读直通、U1 写+确认+审计** 全流程通过。
2. 伪造前端 objectId 越权调用 → 宿主侧 403(starter 重建用户上下文后由 Guard 拦截),审计留痕。
3. act token 过期(60s)拒绝;JWKS 轮换后旧 key 在宽限期内仍可通过。
4. 工具 schema 变更分诊:纯增量差异自动接受并留审计;安全相关差异(必填参数新增、readOnlyHint 翻转)强制重新确认后生效;运行快照 pinning 不受任何变更影响。
5. resolve_scope 未实现的宿主 → 降级行为(PRD §6.1.4)可复现。
6. WC 在纯 HTML 页挂载,Shadow DOM 隔离(宿主样式与 SDK 互不污染);`SCOPE_RESOLVED` 事件到达前端。

### 4.4 测试方案

- 单元:令牌矩阵(有效/过期/篡改/错 appKey/重放);双列拦截器(跨 app、跨租户用例);McpToolAdapter 错误映射与超时;降级逻辑。
- 集成:testcontainers 同网络起 demo-host(真实 MCP server)——`tools/call` 身份头透传断言、只读重试 2 次/非幂等不重试、资源上限触发 `RESOURCE_LIMIT`。
- 契约:MCP 工具清单快照;SSE 事件类型快照 = 融光全集 + 2 新事件。
- SDK(Vitest):tokenGetter 懒加载与过期重取;时间线 reducer 直接复用融光 assistant store 的既有测试用例断言行为一致。
- E2E 初版(Playwright + demo-host):U5、U1(允许/拒绝两条路径)。
- 附:一次非正式接入演练(熟悉卡点,不计入验收)。

---

## 5. P2 · 管理运维与 MVP 验收(W5–W6)

### 5.1 任务分解

| 周 | 任务 |
| --- | --- |
| W5 | inneragent-web(管理员账号认证[Argon2+失败锁定+登录审计]、应用注册、Agent 定义导入导出与提示词编辑、工具注册与体检 v1、模型配置、审计检索[decision_source 过滤]、授权列表页);终态 Webhook(签名+重试);OTel GenAI span 工厂(invoke_agent/chat/execute_tool/MCP client span,内容属性默认关) |
| W6 | 审计敏感参数脱敏;内容安全接入点(过滤链+可配回调);紧急停用(应用总开关+单运行终止,经 Redis 通道);《接入指南》《工具设计规范》;demo-host 扩充(商品简介、流程模板工具,支撑 U2/U3) |

### 5.2 交付物

管理站 1.0、接入文档包、正式接入演练报告。

### 5.3 验收标准(= PRD M1)

1. U1–U3、U5 的 Playwright 自动化全绿。
2. **正式接入计时**:未参与开发的工程师从零接入 demo-host ≤ 2 人日,输出工时与卡点清单。
3. 紧急停用触发 → ≤5s 内前端收到取消事件。
4. 拒绝的调用留审计;`password/token/secret` 类参数在审计与确认卡片均打码。
5. Webhook:终态回调签名可验;模拟宿主 5xx 时按 5 次指数退避重试。
6. 管理站无 embed/M2M 令牌不可访问;管理操作留痕。
7. 夜间批跑(100 次随机对话含取消/重连)无事件丢洞。

### 5.4 测试方案

- 集成:Webhook 签名与重试;熔断通道到运行实例;审计仅追加(UPDATE/DELETE 被拒)。
- 安全专项:越权矩阵 12 例(跨 app/租户/用户 × 读/写/确认/历史);SSE 中 token 过期行为(运行继续、重鉴权无缝恢复);Markdown 渲染 XSS 注入被 sanitize;CORS 白名单。
- E2E 全量:U1 三路径(允许/拒绝/超时——超时用可配缩短窗口)、U2、U3、U5;重连用例用 `context.setOffline()` 模拟断网恢复,断言事件不丢不重。
- 混沌子集:运行中 kill -9 server → 重启 → lease 接管或标失败 ≤30s,用户可续跑。
- 管理站:组件测试 + lint + build 进 PR 门禁。

---

## 6. P3 · 反向替代灰度(W7–W8 开发,W9–W12 灰度)

### 6.1 任务分解

| 周 | 任务 |
| --- | --- |
| W7 | 融光侧:引入 starter(yaml 配置 + 工具暴露开关);embed-token 签发端点;Agent 定义导出脚本(17 定义 + 22 提示词 → JSON → InnerAgent 导入) |
| W8 | 融光前端 flag `ASSISTANT_BACKEND` + WC 挂载 + `assistantPageContext` 对接 SDK 上下文;golden-diff 工具;融光化对照用例集;双侧指标上报与灰度大盘 |
| W9–W12 | 灰度 5%→25%→50%→100%(每档 ≥3 天);每档升档前跑等价性对照;执行一次真实回滚演练 |

### 6.2 交付物

融光试点环境、灰度大盘、golden-diff 工具与首批差异白名单、回滚演练记录。

### 6.3 验收标准

1. 融光 17 个 Agent 定义在 InnerAgent 可用,子 Agent 级联取消正确。
2. 融光 30+ 业务工具经 starter 桥**零改写**可用,工具清单快照(名称/schema)与内置引擎一致。
3. `generate_image/generate_video` 经桥可调,长任务等待态不阻塞 SSE;确认等待跨天行为正确(CI 用分钟级窗口验证同逻辑)。
4. 等价性:对照用例集事件序列 golden-diff = 0(白名单差异除外,白名单需评审)。
5. flag 双向切换即时生效;各侧会话历史保留可查、不跨侧合并(builtin 侧经归档入口访问)。
6. 灰度出口:100% 切流后 ≥2 周无 P0/P1;指标不劣于内置(工具成功率差 ≤1pp、重连成功率 ≥99%、任务完成率代理指标差 ≤2pp)。
7. 回滚演练 RTO ≤5min。

### 6.4 测试方案

- 等价性对照(核心):融光既有 agent 集成测试子集 + PRD 锚点的融光化版本,在 builtin/inner 两侧各跑一遍,golden-diff 出报告。
- 多租户回归:融光多租户下工具调用 tenant 归属(复用融光 20260914 租户归属修复的回归用例)。
- 性能对照:MCP 桥环回 vs 进程内直调,P95 额外延迟 ≤50ms;融光真实负载脱敏采样回放。
- 灰度运维:告警阈值(错误率、重连失败、工具失败率突增自动降档回 builtin 的预案)。

---

## 7. P4 · 增强、性能与替代收尾(W13–W16)

### 7.1 任务分解

| 周 | 任务 |
| --- | --- |
| W13 | 三方 MCP(应用级+用户级,LRU 缓存、防遮蔽);Skill 体系(zip 导入预览校验) |
| W14 | mini KB(tsvector + zhparser,分段、引用溯源);子 Agent(定义/调起/级联) |
| W15 | 用量统计;SDK 配置视图与 iframe 模式(postMessage 协议);用户反馈(👍/👎) |
| W16 | 性能压测与容量校准;融光删除批次①(引擎与控制器)②(实体与迁移归档)+ 归档恢复演练 |

### 7.2 交付物

PRD M2 功能全量、容量校准报告、融光删除批次①②合并 PR。

### 7.3 验收标准(= PRD M2 + 容量)

1. U4 与场景 C(demo 第二应用充当三方)、场景 D 走通。
2. Skill:恶意 zip(路径穿越/symlink/超限/夹带脚本声明)被预览校验拦截;激活上限 8 生效。
3. KB:命中片段带来源展示;无命中不注入;1000 文档上限触发提示。
4. 子 Agent:父取消级联;子 deadline ≤ 父;前端父子层级渲染。
5. iframe 模式在严格 CSP demo 宿主可用;token 不入 URL。
6. 用户反馈事件入库,北极星看板出数(任务完成率代理指标)。
7. 性能(30 分钟持续):100 并发运行 + 500 events/s,错误率 <0.1%;首字 P95 ≤3s(不含模型)、事件透传 P95 ≤200ms;1000 空闲会话内存稳定。容量表回填 PRD §8。
8. 融光删除批次①②合入,归档恢复演练通过。

### 7.4 测试方案

- 性能:k6 SSE 长连接 ramping 场景;PG 慢查询审查;Redis 内存与连接数监控。
- 混沌专项周:Redis 停机 → 降级直连(功能可用+告警);PG 停机 → fail-closed(新运行拒绝、无半状态);MCP 宿主停机 → 错误回灌模型可续跑;outbox 投递失败 → 重试最终一致;确认等待中实例崩溃 → 恢复后仍可确认或正确超时拒绝,**并断言确认事件经回放重投(`EXTERNAL_EXECUTION_REQUIRED` 重达,覆盖 lease 接管路径)**。
- 安全:Skill zip 攻击面全集;KB 提示注入样例集(检索内容以引用标记注入,不改变系统提示词);令牌重放与 JWKS 撤销。
- E2E:U4、场景 C/D;用户级三方 MCP 隔离(他人不可见);多标签页只读跟随。

---

## 8. P5 · 稳定期与收尾(W17 起)

- 上线后 2 周:值班响应、P0/P1 缺陷清零、大盘巡检日报。
- 删除批次③(融光前端 assistant 域)④(配置与文档清理),执行前各做一次回滚镜像可用性检查(保留最后含引擎的镜像 tag ≥90 天)。
- 项目复盘:实际工期 vs 计划、缺陷分布、等价性白名单复盘、接入工时复盘(更新 PRD 接入承诺)。
- 交接运营:值班手册、告警处置手册、容量与成本基线。

**验收**:稳定运行 2 周无 P0/P1;删除批次完成且融光回归测试全绿;复盘与交接文档齐备。

---

## 9. 风险与应急

| 风险 | 触发阶段 | 应急 |
| --- | --- | --- |
| 移植差异审计发现大量隐性耦合(工具层反向依赖引擎) | P0 | 冻结新增,先补接口隔离层;必要时该子包延后到 P1,不阻塞主线 |
| MCP Java SDK 选型不满足桥接需求(Q1) | P1 初 | 备选 Spring AI MCP starter;仍不可行则 starter 内自实现最小 JSON-RPC 子集(范围可控,协议面小) |
| 等价性 diff 无法归零且原因不明 | P3 | 白名单逐条评审 + 定向修复;某类差异超 5% 即暂停升档,回滚当前档位 |
| 灰度指标劣化超阈值 | W9+ | 自动降档回 builtin,按大盘归因(桥延迟/工具失败/模型差异)后重试 |
| 性能不达标(事件吞吐/延迟) | P4 | 优先内核调优(合并窗口、投影批写);仍不达标则容量承诺降档并同步 PRD |
| zhparser 不可用(Q4) | P4 | 降级 trigram,中文召回损失以同义词表缓解,PRD R4 风险登记更新 |
| PRD 新规则与融光引擎行为差异(确认等待/lease、四档授权、24h 超时)扩大移植面 | P0–P1 | P0 差异审计专项核实;差异项白名单化逐条评审,改造或放宽 PRD |
| MCP 规范演进(2026-07-28 无状态化)快于 Java SDK 跟进 | P1–P4 | `/ia-mcp` 无状态实现兼容两代客户端;跟踪 SDK 发布节奏(技术方案 Q7) |
| 工具体检 eval 化缺真实误用样本 | P4 | P3 灰度期即积累 tool_called/confirmation 事件样本 |

---

## 10. 度量与报告机制

| 机制 | 频率 | 内容 |
| --- | --- | --- |
| 站会/周报 | 每周 | 任务完成率、阻塞项、缺陷新增/关闭 |
| 阶段评审 | 每阶段末 | 验收清单逐条过、测试报告、差异审计/等价性报告签字 |
| 灰度日报 | W9–W12 | 双侧指标对比、diff 白名单增量、用户反馈 |
| 质量大盘 | 持续 | 测试通过率、缺陷按级分布、flake 率(flake >2% 的用例强制治理) |
