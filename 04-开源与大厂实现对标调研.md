# InnerAgent 开源与大厂实现对标调研

## 0. 文档信息

| 项 | 内容 |
| --- | --- |
| 版本 | V1.0 · 2026-09-20 |
| 目的 | 以知名开源与大厂的**源码级实现**校验 InnerAgent 三份文档(PRD/技术方案/开发计划)的关键设计,输出采纳/调整/拒绝裁定 |
| 方法 | 6 路并行深研:①嵌入 SDK(CopilotKit/Vercel AI SDK/assistant-ui/AG-UI)②持久化运行(LangGraph/OpenAI Agents SDK/Google ADK/MS Agent Framework/Pydantic AI/Temporal/Restate/DBOS)③工具权限与人工确认(Claude Code/Cline/OpenHands/Cursor/Windsurf/Copilot/Goose)④MCP 规范与官方 SDK ⑤生产实践(Anthropic/OpenAI 官方指南、GitHub Copilot/Notion/Linear/Sentry Seer/Cloudflare Agents、OTel GenAI)⑥平台对比(Dify/Coze/eino/Spring AI,**因限流未完成,见 §5**) |
| 证据标准 | 优先仓库源码(标注文件路径/类名/枚举值),其次官方文档与工程博客;全部来源见 §6 |

---

## 1. 执行摘要

**总评:InnerAgent 的架构主干(事件日志+投影、runId:seq 重连、确认等待不占 lease、内核快照、PG+Redis 分工、宿主侧权限终裁)与业界最成熟的实现(Google ADK、LangGraph Platform、OpenHands、MS Agent Framework)同构,且在身份令牌、授权失效、确认超时三处领先——不需要架构级返工。** 需要修订的是 12 处战术设计,集中在:**权限模式语义(ALWAYS_ASK)、授权存储与作用域、schema 变更策略、MCP 协议版本演进、工具注解的采纳方式**。

裁定分布:**16 项 confirmed(维持)/ 14 项 adjust(调整)/ 3 项 reject(拒绝)**。

---

## 2. 总裁定表

| # | InnerAgent 设计(出处) | 业界证据 | 裁定 |
| --- | --- | --- | --- |
| V1 | 事件日志为事实源+消息投影(ADR-2) | Google ADK `events` 表+state 投影;OpenHands 每事件一文件、重放重建;LangGraph OSS 快照**无法重放流**,Platform 专门另建事件日志;DBOS `operation_outputs` 同构 | ✅ 维持。快照式(checkpointer)更简单但放弃无损重连与逐事件审计,两者均为 PRD 硬指标 |
| V2 | SSE `runId:seq` + Last-Event-ID 重连 | LangGraph Platform 线程流原生支持 Last-Event-ID(`'-'` 全量重放);Vercel 用 TTL Redis 缓冲(过期即失);AG-UI 无序列号无重放 | ✅ 维持——已是对裔中最强方案 |
| V3 | 确认等待不占 lease + 待确认持久化 + 24h 超时 | MS AF `IDLE_WITH_PENDING_REQUESTS`;Restate awakeable 挂起不驻留;Cloudflare `waitForApproval` 持久门;**各框架普遍不内置确认超时,只有重引擎有** | ✅ 维持,且超时+通知是差异化补强 |
| V4 | 自研引擎,不用 Temporal/Restate | Temporal Cloud ~$50/百万 action,百级并发运行月成本 $1–2k+,且需 2–4 个新有状态组件,违背「Compose 单机起步」;Codex/Replit 用 Temporal 前提是百万级执行量 | ✅ 维持(ADR-T1);**重访条件:运行量 ×100、跨服务编排、不设上限长任务** |
| V5 | 内核快照(定义+schema sha256) | MS AF `graph_signature_hash`:恢复时图签名不符即拒绝——同一教训 | ✅ 维持 |
| V6 | PG 事实源 + Redis 仅信号/流分发 | LangGraph Platform:「PostgreSQL 默认存 runs/checkpoints;Redis 只做 signaling/cancellation/streaming pub/sub,无用户与运行数据」 | ✅ 维持(与 Platform 逐字对齐) |
| V7 | DEFAULT=读自动/写确认 | 五个编码工具全部如此(Claude Code Manual、Cursor、VS Code、Windsurf、Goose Auto) | ✅ 维持 |
| V8 | 强制确认策略 > 用户「总是允许」 | VS Code `chat.tools.eligibleForAutoApproval:false` 强制手动并**移除 always-allow 按钮**;Claude Code 组织级 force-ask 连 bypassPermissions 也拦 | ✅ 维持(有直接先例) |
| V9 | 拒绝原因回灌模型 | Claude Code 拒绝评论→模型继续;Cline/OpenHands `UserRejectObservation` | ✅ 维持;**补 Cline 细节:拒绝文案必须陈述后果**(「文件未被修改」),否则模型把拒绝当迭代信号而漂移 |
| V10 | 审计敏感参数打码、仅追加 | OpenHands 写入前 secret 脱敏;Claude Code OTel 默认 `<REDACTED>` | ✅ 维持 |
| V11 | MCP 为宿主工具唯一通道(ADR-1) | 各平台双轨(内置+MCP)是**一方工具模式**;InnerAgent 业务工具全在宿主侧,双轨将重新制造本要删除的耦合 | ✅ 维持;LocalToolAdapter 保持极小并走同一确认管线 |
| V12 | X-IA-Act act token(自定义头) | MCP 规范**禁止 token 透传**、要求 audience 绑定——act token 形态合规;业界端到端身份传播无标准,普遍自定义头;Notion MCP「继承授权用户权限」同理 | ✅ 维持+**调整**:claims 按 RFC 8693 建模(`sub`=用户,`act.sub`=run,`aud`=宿主 MCP URI),便于未来迁移标准 Token Exchange;三方 MCP 服务器走规范 OAuth(CIMD/DCR),**绝不发 act token** |
| V13 | Streamable HTTP 作为宿主 MCP 传输 | 旧 SSE 传输已于 2025-03-26 弃用;**2026-07-28 规范进一步删除 session/GET 流/Last-Event-ID,全面无状态化**;官方 Java SDK v2.0.x(2026-06 GA) | ✅ 采纳+**调整**:`/ia-mcp` 桥按**无状态**实现(不依赖 Mcp-Session-Id/GET 流),同时服务新旧两代客户端;技术方案开放问题 Q1 关闭→官方 Java SDK |
| V14 | 运行级快照拒绝 schema 变更即拒绝新运行 | 规范只定义 `list_changed` 通知与 rug-pull 风险,**无 pinning 标准**;宿主迭代 schema 是常态,全量拒绝会处处碰壁 | ⚠️ **调整**:运行快照 pinning 保持绝对;注册表层改为**活刷新+差异分诊**——纯增量自动接受并审计,仅安全相关差异(工具名集合、新增必填参数、readOnlyHint true→false)强制重新确认 |
| V15 | 工具风险等级=注册时人工声明 | MCP `readOnlyHint/destructiveHint/idempotentHint/openWorldHint` 注解(规范明示 hints 不可信);五大工具全部叠加结构性风险(built-in 只读集、protected paths、默认 deny 列表) | ⚠️ **调整**:可信宿主的注解作为**策略软输入**——readOnlyHint→自动执行、idempotentHint→自动重试门槛(替代手工声明)、openWorldHint→确认卡出口警示;自有 risk 字段保留为覆盖值,注册时由注解生成默认值;工具体检 v2 增加按描述/schema **建议**风险级(LLM 预判,管理员确认),运行时 LLM judge 列 P2 |
| V16 | 子 Agent 权限继承 | Goose 的已知漏洞:子代理硬编码 Auto(「需要确认的模式会挂死」)——**唯一找到的现成反例** | ⚠️ **必须显式定义**:子运行经快照继承父模式与强制策略;确认卡片浮到父会话 UI;子 Agent 自身需确认的调用=由父确认预授或阻止(P6 前写入 PRD M6) |
| V17 | 确认卡「本次/总是」两档 | 共识三–四档:VS Code 单次/会话/工作区/永久;Claude Code 文件编辑**仅会话级且从不持久化**;OTel 区分 `user_temporary/user_permanent` | ⚠️ **调整**:增「本会话内允许」中间档;确认卡四选一:本次/本会话/总是/拒绝 |
| V18 | 「总是允许」无 TTL | 五工具全部**无过期**,撤销靠规则列表 UI;Claude Code `/permissions` 按来源展示;Goose 卸载扩展时**清空其授权** | ⚠️ **调整**:不做 TTL;但须补齐**授权的存储/列表/撤销/清理**:新表 `ia_tool_grant`(user, tool FQN, scope, 授予时风险级, schema sha, source, created_at)+ SDK 配置页列表/撤销 + 工具注销时级联清除 |
| V19 | ALWAYS_ASK=「所有工具调用均需确认」 | **任何工具都未对只读逐次询问**(Claude Code 读类在所有模式豁免;OpenHands 最严只门命令类);与 PRD 自身 R3(确认疲劳)矛盾 | ❌ **拒绝现语义**:重定义为「所有**写**操作必确认且不可被任何授权绕过」(= DEFAULT 减去授权通道),或从模式集移除 |
| V20 | FULL_ACCESS 仅平台管理员可开 | Claude Code bypassPermissions 仍要:关键路径继续弹窗、root 拒绝、一次性留痕接受、项目级配置不可激活 | ⚠️ **调整**:开启时审计记录一次性确认;宿主侧强制校验在该模式下**依然生效**(写入 PRD 为模式属性);禁止应用/Agent 配置自激活 |
| V21 | 批量确认(多待确认合并一卡) | **无任何工具合并多张待确认卡**;OpenHands 控制器物理上单 `_pending_action`;业界「批量」=计划批准/模式预批 | ⚠️ **调整或降级**:仅限同轮并行调用、逐项列明(名称/参数/风险)、允许全部/拒绝全部/逐项过、批量内禁用「总是允许」、拒绝按项审计;否则维持顺序单卡 |
| V22 | 审计仅记决策结果 | Claude Code OTel 决策**来源**分类:`config/hook/user_permanent/user_temporary/user_reject/user_abort` | ⚠️ **调整**:`ia_audit_log` 增 `decision_source` 字段——「高危 100% 确认」从日志**可证明**(由强制策略/模式默认/用户授权/实弹确认哪条路径放行) |
| V23 | 上下文三键 {user,page,object} 每轮携带 | CopilotKit `useAgentContext`:声明式客户端 store、按 run 附带、可 `available:enabled/disabled`、带 Inspector 检视 | ✅ 维持+**调整**:上下文必须**可检视、可关停**(SDK 提供 context 面板与 per-agent 作用域);仅在 run 开始时附带,不流中变更 |
| V24 | WC+iframe 双形态(ADR-8) | **无一家发布 Web Component**;iframe 仅用于不可信三方 UI(CopilotKit MCP Apps);白标共识=无头核心+薄样式层(CSS 变量/slot/复制式组件) | ⚠️ **保持但重排**:WC(经框架无关核心包装)为第一形态;iframe 降级为「受限宿主」兜底且需 postMessage 工具/上下文桥(真实工程量,排期时计入);主题按 CopilotKit v2 模式:语义令牌+slot+headless |
| V25 | 工具名应用内唯一 | MCP 规范:聚合多服务器时客户端**应加服务器前缀**消歧(Claude Code `mcp__<server>__<tool>`、Gemini `mcp_{server}_{tool}`) | ⚠️ **调整**:模型可见名用确定性 FQN(`mcp__<serverKey>__<tool>`),`ia_tool_registry` 存原始 server+tool 对;serverKey 字符集预留(避免下划线,Gemini 踩坑) |
| V26 | 采证接入点(N12)作为过滤器链 | OpenAI guardrail 三分类:input(仅首 agent)/output(仅末 agent)/tool guardrails(包裹每次工具调用),tripwire 触发拦截;廉价分类模型前置筛查 | ⚠️ **采纳其形态**:N12 按 input/output/tool 三类 tripwire 建模,命中动作=拦截/改写/放行,审计记录 |
| V27 | OTel 全链路 trace(方向性) | OTel GenAI 语义约定(Development)已成收敛点:Langfuse 原生、LangSmith/Arize 支持中;span 树 `invoke_agent→chat→execute_tool→MCP client span`;内容默认 opt-in | ⚠️ **采纳具体约定**(见 §3.5.3) |
| V28 | 《工具设计规范》内容(待写) | Anthropic「Writing tools for agents」:任务型合并优于 CRUD 罗列、命名纪律、`response_format` 枚举、上下文化结果+可操作错误、eval 驱动迭代、poka-yoke | ⚠️ **按 §3.5.4 五条起草**,工具体检从静态 lint 升级为接入 eval |
| V29 | 确认幂等与确认者绑定 | LangGraph resume 写入 upsert;Pydantic AI 明示「**UI 提交的批准不是授权边界**」 | ⚠️ **补**:confirm 端点幂等(unique(run_id, confirmation_id, decision) + epoch 守卫);确认者必须=embed token 中的 userId(会话属主) |
| V30 | 确认决策先落日志再恢复执行 | MS AF response-entry checkpoint:先日志化人的答案,再重放消费它的超步——HITL 重放精确性的关键 | ⚠️ **补**:确认状态机不变式——决策事件先于恢复写入 journal |
| V31 | 事件 schema 版本化 | OpenAI RunState 显式 `$schemaVersion` 且前向兼容故意 fail-fast | ⚠️ **补**:`ia_agent_event` 增 `schemaVersion` 列;API 已有 `IA-Protocol-Version`,补持久层 |
| V32 | 接管后重发确认事件 | MS AF 恢复时重发未应答 request_info(重启后重新提示人) | ⚠️ **补**:P4 混沌用例加断言——lease 接管/重启后 `EXTERNAL_EXECUTION_REQUIRED` 重投 |
| V33 | 双发策略(reject) | LangGraph Platform 默认 **enqueue**(入队),提供 reject/interrupt/rollback | ✅ v1 维持 reject(匹配「单会话单运行」);**enqueue 列 P2 评估**(聊天 Agent 第一大 UX 抱怨点) |
| V34 | 续跑幂等契约 | ADK:resume 至少一次语义,工具必须幂等 | ⚠️ **补**:工具注册表加 `resumeSafe`(可并入 idempotentHint);PRD「不重复扣模型调用」扩为书面契约:哪些工具可能被 continue 重执行 |

---

## 3. 分领域发现(源码级证据摘要)

### 3.1 持久化运行与断线恢复

- **LangGraph(Postgres checkpointer)**:`checkpoints`(每超步小头)+`checkpoint_blobs`(按 channel-version 内容寻址去重)+`checkpoint_writes`(含中断/恢复/错误保留通道:`INTERRUPT=-3`、`RESUME=-4`)。**HITL 协议就是 checkpoint_writes 里的行**——这是它崩溃后可恢复的原因。OSS 流模式(values/updates/custom)不可重放,Platform 为此**另建事件日志**,并以 Last-Event-ID(`'-'`=全量)支持重连。
- **Google ADK(与 InnerAgent 最接近的公开同构)**:`sessions(app_name,user_id,id,state)` + `events(event_data JSON,FK 级联)`;`EventActions` 载荷携带 state_delta/artifact/确认请求/压缩;state 是**投影**,原始日志永不改写(压缩与回退本身也是新事件)。多租户=state 前缀(`app:`/`user:`/`temp:`);并发=会话锁+`SELECT FOR UPDATE`+存储版本号乐观并发(`StaleSessionError`),无 lease 概念。
- **MS Agent Framework**:三种 checkpoint(入口/超步/**应答入口**)使 HITL 完全可重放;`graph_signature_hash` 拒绝图定义变更后的恢复;未应答请求在 checkpoint 中随行,重启后**重新提示**。
- **OpenHands(v0.62.0 Python)**:每事件一个 JSON 文件、锁下单调 id、**写入前递归脱敏**、附加即不可变(带 id 再入流直接 ValueError);确认中动作仍在流中(带 `awaiting_confirmation` 态)但永不执行;`UserRejectObservation` 作为工具结果回灌 LLM。风险门:`UNKNOWN/LOW/MEDIUM/HIGH` + `RISK_SEVERITY` 阈值;v1 策略对象 `AlwaysConfirm/NeverConfirm/ConfirmRisky(threshold)`。
- **DBOS(Postgres-only 持久执行,与 InnerAgent 表结构几乎同构)**:`workflow_status(executor_id, recovery_attempts, deadline)`+`operation_outputs`(按步日志)+`notifications`/`streams`;**无心跳列**,靠 reaper 按 executor 重指派——佐证「纯 PG 进程内持久执行」是成立的目标架构(无 Java SDK,仅观摩)。
- **Temporal/Restate/Inngest**:为 InnerAgent 不采纳(见 V4);移植其**模式**:journal 即决策记录、批准即另一个持久步骤、step 记忆化(continue 不重执行 journal 已有完成事件的工具)。

### 3.2 工具权限与人工确认(编码工具)

- **Claude Code 三层模型**:会话模式(6 种)→ 规则(allow/ask/deny,固定求值序 deny→ask→allow,首中即胜)→ 提示语改写规则。**per-parameter allow 被明确拒绝**(「单参数白名单不能证明整体安全」;主内容字段上的参数规则被忽略并启动警告——可绕过)。永久授权存 `.claude/settings.local.json`,**文件类编辑授权仅会话级、从不持久化**;受保护路径/关键路径为**凌驾于规则之上的熔断线**,bypassPermissions 也拦。auto 模式=分类器替代人(大维护块清单),进入时**丢弃宽 allow 规则**、退出恢复。**对我们的启示:授权粒度=精确工具名,绝不引入参数模式匹配;我们的 resolve_scope+宿主侧逐调用鉴权在架构上强于它们的客户端近似**。
- **Cline**:自动批准=按类别布尔+模型自报 `requires_approval`;拒绝文案若不含后果陈述,模型会基于未落盘内容继续迭代(实测教训);MCP 信任=每工具 `autoApprove` 开关,新服务器默认 ask。
- **VS Code/Copilot**:per-tool allowlist + `eligibleForAutoApproval:false` 强制手动并隐藏 always-allow;「管理工具批准」按服务器分组信任+一键重置;终局兜底是 `PreToolUse` hook(文档自认规则匹配「尽力而为,非安全边界」)。
- **Goose**:模式(Auto/Approve/SmartApprove/Chat)与应答(AlwaysAllow/AllowOnce/Cancel/DenyOnce/AlwaysDeny)分离;`permission.yaml` 每工具三态;**卸载扩展清空其授权**;LLM 对抗检查器(AdversaryInspector)拦外传/越权/提权;子代理硬编码 Auto 的前车之鉴(V16)。

### 3.3 流式协议与嵌入 SDK

- **事件词汇已收敛**:run 生命周期(start/finish/error/step 边界)+ 块寻址 text/reasoning start/delta/end + 工具 start/args-delta/available/output + 审批对(`tool-approval-request/response`,Vercel 含 `approvalId/approvalDescriptor/reason/isAutomatic/signature` 与终态 `tool-output-denied`)+ data-*/custom 逃生舱 + 状态同步(快照+JSON-Patch)。**InnerAgent §7.2 事件集方向正确,建议吸收三点**:①自动决策仍下发并标 `isAutomatic`(审计可见);②拒绝是独立终态事件;③所有 delta 按 messageId/toolCallId 自标识(重放安全)。
- **无人使用原生 SSE id/Last-Event-ID**(自定义重连端点或 TTL 缓冲)——InnerAgent 的持久 journal+原生 Last-Event-ID 是对它们临造机制的标准化(V2)。
- **前端工具注册**(`RunAgentInput.tools` 随 run 携带)+ 未注册工具优雅兜底(CopilotKit `"*"`、assistant-ui ToolFallback、AI SDK dynamic-tool part)——SDK 需要同类兜底渲染。
- **上下文注入**:CopilotKit 每组件声明、客户端 store、run 开始整批附带、可禁用、带 Inspector——**自动附加上下文的可检视性是信任设计的一部分**(V23)。
- **白标**:无头核心+CSS 变量令牌+slot+headless;无一家做 WC;iframe 只用于不可信 UI(V24)。

### 3.4 MCP 规范与生态(关键:版本演进)

- **规范已迭代到 2026-07-28(破坏性)**:删除 session/initialize 握手/GET SSE 流/Last-Event-ID 可恢复性,转为**无状态 POST + 多轮请求(MRTR:`InputRequiredResult`→携带 `inputResponses` 重试)**;`subscriptions/listen` 替代变更通知;Roots/Sampling/Logging **弃用**;DCR 弃用改 Client ID Metadata Document。**设计含义:`/ia-mcp` 桥与 MCP 客户端必须按无状态形态实现**(V12/V13),InnerAgent 自身的 run 级 SSE 重连与 MCP 层解耦,不受影响。
- **注解(hints)不可信原则**:规范原文「clients MUST consider tool annotations to be untrusted unless from trusted servers」——对应用级宿主服务器可作软策略输入,三方服务器一律确认(V15)。
- **授权**:MCP 服务器=OAuth 2.1 resource server(RFC 9728 元数据、8707 audience 绑定、**禁 token 透传**)——act token 形态合规(V12);三方接入走规范 OAuth/静态头,act token 限于内环。
- **Elicitation**(server→用户结构化输入,accept/decline/cancel):与确认卡互补不替代——映射为确认管线的输入通道,复用 UI/脱敏/审计;2025-06-18 下会阻塞请求,不适配 24h 等待,2026-07-28 MRTR 形态才契合。
- **官方 Java SDK**:`io.modelcontextprotocol.sdk:mcp` v2.0.x(2026-06 GA,规范 2025-11-25,Spring 传输移入 Spring AI 2.0)——**技术方案开放问题 Q1 就此关闭**。
- **工具名消歧**:规范明示聚合方应加服务器前缀;registry(mcp.gov 预览)仅作 P4 三方发现的可选目录。

### 3.5 生产实践与可观测

1. **Temporal/Restate/Inngest 裁定见 V4**;OpenAI Codex/Replit Agent 3 建于 Temporal 的前提是百万级执行量。
2. **Anthropic《Writing tools for agents》→《工具设计规范》五条**(V28):①任务型合并(`schedule_event` 而非三件套 CRUD;1:1 端点包装是反模式,工具体检标记它)②命名纪律(服务-资源-动作;`user_id` 不写 `user`)③描述=给新人的入职文档+注解标记破坏性 ④结果上下文化(返回名称非 uuid)+错误可操作(改什么,不是栈)⑤eval 驱动迭代(按宿主建任务集跑对照;冗余调用→分页/描述问题,非法参数→schema 问题——**工具体检接真实误用信号,不止静态检查**)。
3. **OTel GenAI 具体采纳**(V27):run=`invoke_agent {gen_ai.agent.id/name}`(用稳定定义 ID);每次模型调用=`chat {model}` span(`gen_ai.provider.name/usage.input_tokens/usage.output_tokens/…`,`gen_ai.conversation.id`=真实 conversationId);每工具调用=`execute_tool`(gen_ai.tool.name/call.id,可选 `gen_ai.tool.definitions` 供快照取证);宿主 MCP 调用=MCP client span(`mcp.method.name`)+`mcp.client.operation.duration` 指标;**内容属性默认关**(对齐审计脱敏);runId 作 trace 关联 ID。经薄内部 span 工厂封装,防约定演进返工。
4. **Sentry Seer 自主分级**:分析→方案→执行的分段自主权旋钮;不可逆变更必落人类可审工件;确认拒绝率当调优信号而非失败指标。
5. **Cloudflare Agents 细节**:超时不静默——升级通知(webhook/管理员)+提醒调度;不可变 `approval_audit` 表;多审批人累计(暂不需要,记录模式)。
6. **Notion/Linear MCP**:身份域=继承授权用户权限(与 act token 同理);**Linear 提供只读端点/只读 scope**——低风险集成可免确认流的注册 profile,记入工具注册表设计。

---

## 4. 对三份文档的修订清单(按文档归位)

### PRD(V1.3 候选)

| 落点 | 修订 |
| --- | --- |
| §6.2.2 模式表 | ALWAYS_ASK 重定义(V19);FULL_ACCESS 补三条护栏(V20);确认卡四档:本次/本会话/总是/拒绝(V17) |
| §6.2.2 确认流规则 | 批量确认限同轮并行+逐项+禁 always-allow(V21);拒绝回灌文案须陈述后果(V9) |
| §6.2.2/新 6.2.4 | 授权管理:ia_tool_grant 存储、配置页列表/撤销、工具注销级联清除、无 TTL(V18) |
| §6.2.1 | 工具 schema 变更策略改为「活刷新+安全相关差异重确认」(V14);风险级默认取注解、人工覆盖(V15);注册表增加 admin per-tool policy(force-ask/allow/deny)与 `resumeSafe` 字段(V34) |
| §6.6 子 Agent | 权限继承三条(V16) |
| §6.9 审计 | 增 decision_source 字段(V22) |
| §7.1/N12 | 审核接入点按 input/output/tool tripwire 建模(V26) |
| §8 可观测 | 按 OTel GenAI 约定细化(V27) |
| §11 M2/M6 | 「工具设计规范」按五条起草(V28);M6 前完成子代理权限定义 |

### 技术方案(V1.1 候选)

| 落点 | 修订 |
| --- | --- |
| §4.3 | 工具 FQN 命名(V25);schema 活刷新分诊(V14);注解→策略输入(V15) |
| §4.5/§7.2 | SSE 事件吸收三点(自动决策标记/拒绝终态/delta 自标识)(§3.3) |
| §5.1 | 新增 `ia_tool_grant`;`ia_audit_log` +decision_source;`ia_agent_event` +schemaVersion;`ia_tool_registry` +admin_policy/resume_safe/annotations 列 |
| §6.1/6.2 | act token claims 按 RFC 8693 建模;`/ia-mcp` 无状态化(不依赖 session/GET 流);三方 MCP 走规范 OAuth(V12/V13) |
| §6.3 | Q1 关闭:官方 Java SDK v2.0.x;补「确认端点幂等+确认者绑定」「决策先日志化」两条不变式(V29/V30) |
| §7.1 API | confirm 契约明确幂等语义 |
| §10 安全清单 | S 系列对应增补(授权生命周期、注解不可信、三方 OAuth) |
| §14 | 关闭 Q1;新增开放问题:2026-07-28 无状态规范迁移时间表 |

### 开发计划(V1.1 候选)

| 落点 | 修订 |
| --- | --- |
| W3(P1) | 增:ia_tool_grant 与授权管线;确认幂等/绑定;FQN 命名 |
| W5(P2) | 审计 +decision_source;工具注册表策略字段;OTel span 工厂 |
| P4 验收 | 混沌用例加「接管后确认事件重投」断言(V32);Skill/KB 不变 |
| §9 风险 | 增:MCP 规范演进(2026-07-28 无状态化)导致 SDK 跟进压力;工具体检 eval 化的样本依赖 |

---

## 5. 研究局限

- **Dify / Coze Studio / eino / Spring AI Alibaba 平台对比未完成**(调研代理遭遇 API 限流中断);其关注点(模型凭据加密、工具注册表缓存、租户行级隔离的工程细节)已部分由 MCP 调研(凭据静态头/OAuth 实践)与权限调研覆盖,但**平台级工具市场的治理模式、Dify 租户模型源码细节**仍属空白,如反向替代进入平台化(PRD M3)前建议补一轮。
- Cursor/Windsurf/Copilot 基于公开文档而非源码(闭源);OpenHands 以 0.62.0(Python 引擎末版)+software-agent-sdk 交叉验证。
- MCP 2026-07-28 规范较新,Java SDK(v2.0.x)尚未跟进该版本;结论按「无状态兼容」策略消化,已列入开放问题。

## 6. 主要参考

LangGraph checkpoint-postgres/base.py、pregel/_algo.py;Google ADK sessions/schemas/v1.py、base_session_service.py、database_session_service.py;MS agent-framework _workflow.py/_checkpoint.py;OpenHands 0.62.0 agent_controller.py、events/stream.py、storage/locations.py;Cline AutoApprovalSettings.ts、ToolExecutor.ts(v3.17.16)、toggleToolAutoApprove.ts;CopilotKit use-human-in-the-loop.tsx、context-store.ts、interrupt-state.ts;Vercel ai process-ui-message-stream.ts、ui-message-chunks.ts、generate-tool-approval-generic.ts;assistant-ui toolbox.ts、AssistantTransport.ts;AG-UI types.ts;MCP 规范 2025-06-18/2025-11-25/2026-07-28(Tools/Elicitation/Authorization/Transports/changelog)、java-sdk v2.0.x;Temporal AI 文档与定价、Restate HITL 模式、Inngest agent-kit;Anthropic Building effective agents / Writing tools for agents / Claude Code best-practices 与 permissions 文档;OpenAI agents guide/guardrails;VS Code approvals 文档;Notion/Linear MCP 文档;Sentry Seer 博客;Cloudflare Agents HITL;OTel GenAI semconv(gen-ai-spans/mcp);DBOS system-tables;Goose permission.rs/permission_inspector.rs。
