# 业务线 8 · 断流重连(R2 复测)

- 被测:SDK 断流自动重连(Last-Event-ID 续传)+ DEF-06 修复面(轮询判完成 → 补偿回填)
- 用例文件:`e2e/specs/09-stream-reconnect.spec.ts`(2 用例)
- 结论:**2 通过,0 失败,0 REGRESSED**。DEF-06 关闭:断流窗口覆盖 run 终态时,UI 不刷新即回填至全文,与持久层一致;不存在「已完成但截断」收敛路径。
- 手段:`e2e/reconnect-host.mjs`(18085)`POST /__break?seconds=N` 物理断链;R2 起该中转为**纯透传**(无任何 URL/请求体改写),宿主页以 SDK 默认 baseURL 接入。

## 用例明细

| # | 用例 | R2 实际 | 判定 | 证据(assets/) |
| --- | --- | --- | --- | --- |
| 1 | 流式中途断流 → 自动恢复 → 收敛与数据完整 | 断流 8s 覆盖 mock run 终态;收敛 **已完成**;UI 含收尾段全文(「不允许截断终态」无条件断言通过);库层事件 seq 连续、助手消息全文;路径 A:重连游标 Last-Event-ID 有效且 < DONE seq,重放覆盖游标后全部可见事件、直达 DONE | ✅ | `L8-04-路径A-自动重连续流至DONE(全文+已完成).png`、`L8-psql-事件序列.txt`、`L8-reconnect-host-断流重连日志.txt`、`L8-ui-sse-id与库对齐.json` |
| 2 | **DEF-06 回归**:长断流覆盖 run 终态 → 补偿回填 | 断流(allow=running 白名单)覆盖终态;run 已完成;**uiMissingFooter=false** —— UI 未刷新即经 /events 从本地游标回填至全文,UI 与库层内容一致 | ✅ **缺陷关闭** | `L8-08-DEF06回归-取证.json`(uiMissingFooter=false / dbHasFooter=true)、`L8-08-DEF06回归-已完成且内容完整.png` |

## DEF-06 闭环

- R1 发现:断流窗口覆盖终态 → 轮询先判「已完成」即放弃重连,UI 保留截断投影(P2)。
- 修复提交:**c4f9a50**(判终态且本地投影落后时,先经 `/runs/{id}/events` 从本地游标补偿回填,交付根终态后再终态化)。
- R2 复测:L8-06 断言反转为「不刷新即全文」后通过;L8-01 追加的无条件「续流/回填至全文」断言(R1 交接 §7-2 要求同步复核)同轮通过。**判定:关闭**。
