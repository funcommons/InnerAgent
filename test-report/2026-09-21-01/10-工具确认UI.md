# 10 · 工具确认 UI 线(L10,核心)

- 业务线:写工具确认卡(批准/拒绝)、SCOPE_RESOLVED CustomEvent、scope 降级 chip 边界、只读直通对照
- spec:`e2e/specs/11-tool-confirm.spec.ts`(4 用例,4 通过)
- 编排:WC 宿主页(demo-spring-host 六工具经 admin API 注册,endpointUrl=`http://localhost:18091/ia-mcp`)+ mock 模型脚本驱动(`ia_ai_model.config.mockScript`,psql 设置/finally 复位)+ 宿主 `/ia-demo/state` 与 `ia_audit_log` 双侧断言。

> 重要背景:注册的桥接工具在纯 UI 会话中默认**不可达**(SDK 默认发送 `"enabledMcpTools":[]`,服务端按显式空白名单过滤)→ **DEF-07(P1)**。本线经 e2e 可断中转的网关层把空数组从 `/runs` 请求体剔除(测试补偿,日志 `[rewrite] /runs 请求体剔除 enabledMcpTools 空数组`)后完成 UI 侧验证;缺陷本体由分册记录。

## L10-01 · 写工具确认·批准:确认卡 → 允许 → DONE + 宿主简介版本 +1

- 步骤:mockScript 驱动 `update_product_brief`(product 88,唯一新文案);发送后断言行内确认卡;捕获 SCOPE_RESOLVED;点击「允许」;等终态;宿主 `/ia-demo/state` 前后对比;psql 审计断言。
- 期望:确认卡渲染(工具名/等待确认/允许/拒绝);批准后恢复执行至 DONE;宿主简介更新、版本 +1、调用计数 +1;恰 1 条 `allowed(live-confirm)` 审计。
- 实际:全部符合 —— 确认卡含 `update_product_brief` 工具名与「等待确认」倒计时;批准后续流至「已完成」+ 全文;宿主 `brief=批准文案`、`briefVersion N→N+1`、`invocations.update_product_brief +1`;审计 `allowed/live-confirm` 恰 1 条。
- 判定:✅ 通过
- 证据:`L10-02-写工具确认卡(工具名+等待确认+允许拒绝).png`、`L10-05-批准后续流至DONE(工具行已完成).png`、`L10-01-宿主状态-前/后.json`、`L10-06-psql-审计行.txt`

## L10-07 · 写工具确认·拒绝:拒绝 → 终态 + 宿主零执行

- 步骤:mockScript 驱动 `copy_flow_template`(唯一新模板名);确认卡出现后点「拒绝」;断言终态、工具行「已拒绝」、宿主零执行、审计。
- 实际:终态「已完成」(模型对拒绝结果收尾作答);工具行「已拒绝」;`flowTemplateCount` 不变、无新模板名、`invocations.copy_flow_template` 不变(**确认在宿主之前拦截**);恰 1 条 `denied/live-confirm` 审计。
- 判定:✅ 通过
- 证据:`L10-08-拒绝路径-确认卡渲染.png`、`L10-09-拒绝后终态(工具行已拒绝).png`、`L10-07-宿主状态-前/后.json`、`L10-10-psql-审计行.txt`

## L10-11 · 两轮脚本(读直通 + 写确认)与 scope chip 渲染边界

- 步骤:mockScript 两轮 [`get_product_brief`(readOnlyHint=true)→ `update_product_brief`];验证只读第 0 轮直通(无确认)、写第 1 轮出确认卡;检查单工具确认场景的 scope chip 渲染;批准收尾。
- 实际:只读工具直通完成(行内无「等待确认」),确认卡仅属于写工具;**单工具行内确认卡无 scope 渲染位**(`assistant-confirm-scope` 不渲染,批量确认条 `assistant-batch-approval` 也不渲染 —— 该条仅在 ≥2 工具同批时出现,而 mock 模型单轮单工具,UI 自然路径无法构造同批多工具)→ 记录观察项 F13:scope 数据已随事件下发(SCOPE_RESOLVED detail 可证),但最常见「单工具确认」场景 UI 无 scope 呈现,PRD §6.1.4 可检视性缺口。
- 判定:✅ 通过(F13 记录)
- 证据:`L10-11-只读直通观察.json`、`L10-12-第二轮写工具确认卡.png`、`L10-12-scope-chip两轮观察.json`、`L10-13-两轮脚本完成(读直通+写批准).png`

## L10-14 · 只读工具直通对照:U5 list_login_records 无确认卡

- 步骤:mockScript 驱动只读 `list_login_records`;运行中轮询断言确认卡始终不出现;终态/宿主计数/审计断言。
- 实际:全程无确认卡、无 SCOPE_RESOLVED 派发;宿主 `invocations.list_login_records +1`;该 run 无 `ia_audit_log` 行(与写确认对照)。
- 判定:✅ 通过
- 证据:`L10-15-只读工具直通至DONE(无确认卡).png`、`L10-16-只读直通-SCOPE_RESOLVED.json`、`L10-16-psql-审计行数.txt`、`L10-14-宿主状态-前/后.json`

## SCOPE_RESOLVED CustomEvent 证据(L10-01 捕获,`assets/L10-03-SCOPE_RESOLVED-detail.json`)

```json
{
  "at": "2026-09-20T20:43:00.367Z",
  "conversationId": "55928808-…", "runId": "cd6c9bb0-…", "replyId": "c133a021-…",
  "tools": [{
    "toolCallId": "mock-call-9228",
    "toolName": "mcp__demo-spring-host__update_product_brief",
    "scope": { "resolved": false, "degraded": true,
               "summary": "宿主未实现约束范围反查(resolve_scope),本次运行无约束范围上下文" }
  }]
}
```

- 宿主元素 `addEventListener('SCOPE_RESOLVED')` 可收(bubbles+composed 穿透 Shadow 边界)✅;同确认批恰派发一次 ✅;detail 携带 conversationId/runId/replyId/tools[].toolCallId/toolName/scope ✅;平台当前恒为降级形态(`degraded=true` + 稳定原因文案)✅ 与服务端 `AgentExecutionFactory.scopeNode` 一致。

## 本线结论

| # | 用例 | 判定 |
| --- | --- | --- |
| L10-01 | 写确认·批准(宿主状态变化+审计) | ✅ |
| L10-07 | 写确认·拒绝(宿主零执行+审计) | ✅ |
| L10-11 | 读直通+写确认、scope chip 边界 | ✅(F13) |
| L10-14 | 只读直通对照 | ✅ |

- 修复意见(F13,详见分册):为单工具行内确认卡补 scope 呈现(degraded 弱提示/summary),或允许行内确认也挂 chip;否则 PRD §6.1.4 的「InnerAgent 传递与呈现 scope」在最常见场景不可检视。
- 修复意见(DEF-07,详见分册):SDK 空引用时不要下发 `enabledMcpTools:[]`;或服务端将空数组视作未指定。
