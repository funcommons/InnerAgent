# R3 · 验证分册(第 3 轮 · 修 P0~P3 批次 26 条优化验证)

- 轮次:/loop 测试循环 第 3 轮(R1 发现→修复→R2 复测→**26 条优化批次落地后的全量验证**)
- 日期:2026-09-21 · 执行:E2E 验证 agent(只测不改产品代码;e2e/** 与 test-report/2026-09-21-03/** 可写)
- 结论:**回归 64 用例 63✅ / 1❌;新增 18 用例 18✅(合计 81/82)**;新能力(状态灯/即输即查/历史抽屉/断流静默提示/附件气泡/scope chip 等)全部可用。
- **发现缺陷 2 条(DEF-08 P1 服务端回归、DEF-09 P2),按口径不修、不出收官文档** → 明细见 `00-缺陷清单.md`;本分册承担验证明细与 26 条逐条矩阵。

---

## 1. 环境搭建结论

| 项 | 结果 |
| --- | --- |
| **server jar** | ⚠️ **强制重建**:现场 05:34 的旧 jar **不含** dfcecb5(08:04),`mvn package -DskipTests`(JDK21)重建后 V14 熔断/Webhook 类与迁移在位。**沿用旧 jar 将把「熔断域手验」整体误判为端点不存在** |
| 数据卷 | R2 保留卷直接增量升级:Flyway V14 `circuit breaker and webhook config` 增量应用成功(无卷重建);`ia_circuit_event` 表就绪 |
| web dist | env.sh 判定「已最新」经核实成立(08:14,含 15 提交全部 P3 修复;env-badge/CircuitView 等新组件在 dist 实证存在) |
| sdk-js | `pnpm build` 重建(08:20,608.9 kB) |
| compose 错峰 | 35432/36379;server 18090(IA_ADMIN_KEY=test-key + bootstrap admin/Admin#12345) |
| web 真服务模式 | gateway.mjs 18081(静态 dist + /ia 反代) |
| 辅助进程 | demo-host vite 5180(**已声明 agentType:'demo'**,beaa822)、demo-spring-host 18091(JDK21,mvn spring-boot:run)、reconnect-host 18085;**本轮新增** web vite dev 5177(msw 演示后端,专供状态灯「演示数据」态取证) |
| 六工具注册 | ✅ 经 admin API 重注册 demo-spring-host 六工具(readOnlyHint 注解齐备,200×6,`R3-00-工具注册表.txt`;R2 收官清理曾清零 ia_tool_registry,属每轮环境前置) |

## 2. 熔断域新端点手验(服务端,任务口径全链)

| 步骤 | 结果 | 证据(assets/) |
| --- | --- | --- |
| GET /admin/circuit-breaker | ✅ 200,含 emergencyStopped/stoppedAt/stopReason/**limits(7 项)**/**activeRuns**/**recentEvents** | `R3-01-GET-state.json` |
| PUT /limits(部分合并) | ❌ **500 → DEF-08** | `R3-02-PUT-limits.json` |
| POST /emergency-stop | ❌ **500 → DEF-08**(同根因);改经 psql 直设总开关后状态端点正确回显 emergencyStopped=true+原因+时刻 | `R3-04-GET-state-停用中.json` |
| 停用中起 run | ✅ **403 「应用已紧急停用:{reason},新运行已被拒绝,请等待管理员恢复后再试」**(守卫挂在 run 发起唯一入口,SDK 对话链全覆盖语义成立) | `R3-05-停用中起run-响应.txt` |
| POST /resume | ❌ **500 → DEF-08**(psql 复位) | `R3-03-POST-resume-500.json` |
| POST /terminate-run(运行中 run,mockScript 4 轮工具拉长存活期) | ✅ 200,事件 type=run-terminated 入 `ia_circuit_event`,审计 `run-terminated/forced-policy` 落库,run → CANCELLED | `R3-06-*.txt/json` |
| POST /terminate-run(已终态 run) | ✅ **409 「运行已处于终态,无需强制终止: {runId}」**(偏差清单抽查 2) | `R3-07-terminate-run-二次-409文案.txt` |

**判定**:读路径/守卫/终止与审计链全通;三写端点全灭(DEF-08)。「紧急停用不批量取消进行中 run(逐个 terminate-run)」与任务书已知残留一致。

## 3. 服务端手验(其余两条)

| 项 | 结果 | 证据 |
| --- | --- | --- |
| 413 文案带实际上限 | ✅ 25MB 上传 → `413 「附件大小不能超过 20MB,请压缩或拆分文件后重新上传」`(上限数值+行动指引) | `R3-11-413-25MB文案.txt` |
| ILIKE 子串检索 UI 生效 | ✅ UI 以 **18 字符子串**(未输全 FQN)过滤,granted+revoked 两行审计均命中(e2e A13) | `A13-*.png/txt` |
| deliveries status 过滤(偏差清单抽查 1) | ✅ 四态逐一命中:PENDING→[6]、FAILED→[5]、SUCCESS→[1,7]、EXHAUSTED→[2,3,4];手动重投 EXHAUSTED→PENDING+尝试清零。**无 status 形态 500 = DEF-09** | `R3-08-*.json`、`R3-09-*.txt`、`R3-10-redeliver-重投.json` |

## 4. 回归(64 用例)

| 轮次 | 结果 | 说明 |
| --- | --- | --- |
| 第一遍(原样重跑) | 60✅ / 4❌ | 4 个失败逐一核验:**3 条为「产品行为正确+断言过时」**(#6/#12/#2 修复后旧取证断言失效),1 条(02 轮换)为 DEF-08 真回归被旧断言遮蔽(旧断言在 500 之前先行失败) |
| 断言更新后全量 | **63✅ / 1❌**(3.1m,与 18 条新用例同轮) | 唯一失败 = spec 02 公钥轮换 PUT → 500(DEF-08,非断言问题);07/11/05/02 更新后全过 |

**断言更新清单(全部在 e2e/specs,产品代码零改动;均带 [R3 断言更新] 注释)**:

| spec | 用例 | 更新 | 依据 |
| --- | --- | --- | --- |
| 02-apps | L2-06 轮换对话框 | 「宽限期语义未实现」取证断言 → 「旧公钥保留 72 小时验证宽限」正向断言 | #6 修复(0e518c1) |
| 05-audit | L5-05 decision_source 值域 | 「下拉缺 expired」发现项断言 → 字典驱动含 expired 正向断言 | #12 修复(9585a4a) |
| 07-mockdomains | L7-01/L7-02 | 「熔断/Webhook 404 取证」→ 真端点 200 + 页面渲染 + 状态过滤正向回归(文件头注明残留 DEF-09) | #2 修复(dfcecb5/5e8feb7/51de916) |
| 11-tool-confirm | L10-11/L10-12 scope chip | 「单工具确认卡无 scope chip」→ chip 可见正向断言(批量条仍不出现) | #11 修复(6b57c1a) |

任务书预期之外的漂移面(07/11/02)已同样按「产品行为正确+断言过时」口径处理并留痕;03-tools(#11 历史抽屉为**新增**抽屉入口,未改既有断言面)与 05 的其余用例零漂移。

## 5. 新用例(18 条,`13-p3-admin.spec.ts` + `14-p3-sdk.spec.ts`,18/18 ✅)

**管理站 13 条(A01~A13)**:

| # | 用例 | 对应优化(#=99-优化建议.md) | 关键证据 |
| --- | --- | --- | --- |
| A01 | 环境状态灯三态(已连接绿/演示数据黄/服务不可达红) | #1 | `A01-状态灯三态.json` + 两态截图(黄态经 web vite dev msw 取证) |
| A02 | 审计时间即输即查 + 已应用范围 chip + 无 Z ISO | #7 | `A02-即输即查-请求响应.json` |
| A03 | 导出 CSV(download 事件+文件头)+ 列设置隐藏/恢复列 | #18 | `A03-导出CSV-文件头.txt` |
| A04 | decision_source 字典驱动下拉含 expired + expired 审计可检索(fixture 行) | #10 | `A04-*.json/png` |
| A05 | deliveries 状态过滤(重试耗尽)+ 手动重投(待投递/尝试清零) | #2/18b | `A05-重投后库层状态.txt` |
| A06 | 密钥指纹列(前 12 位+复制+完整指纹)+ 轮换对话框 72h 文案+当前指纹(不执行轮换) | #8/#6 | `A06-指纹短形与完整.txt` |
| A07 | schema 历史抽屉:applied/silent_refresh 时间线 + 重开即刷新 + 指纹短形 | #9 | `A07-历史抽屉文本.txt` |
| A08 | 熔断页:无「≤5s」残留 + 活跃运行 N + 逐个终止指引 + limits 表单 | #2 | `A08-熔断页文案全文.txt` |
| A09 | textProtocol 显式选择随 payload 下发(mock)且落库 | #14 | `A09-创建payload与响应.json` |
| A10 | 窄屏(<1280)收描述/指纹等次要列,1440 恢复 | #26 | `A10-窄屏1100(次要列收起).png` |
| A11 | IaTime 相对时间 + tooltip 绝对时间 + `<time datetime>` | #24 | `A11-IaTime观察.json` |
| A12 | config 端点不可达 → 「服务端能力未开通」占位 + 重新检测 CTA | #2/#16 | `A12-旧版404占位*.png` |
| A13 | toolFqn ILIKE 子串检索 UI 生效(子串命中 granted/revoked) | #13 | `A13-ILIKE子串检索命中行数.txt` |

**SDK 5 条(B01~B05)**:

| # | 用例 | 对应优化 | 关键证据 |
| --- | --- | --- | --- |
| B01 | 断流静默提示:重连横幅可见 + 原始错误/手动重试被抑制 + 恢复后清除并收敛已完成 | #5 | `B01-断流中-静默提示观察.json` |
| B02 | 单工具确认卡 scope chip 降级形态(约束范围降级文案/is-degraded/summary title) | #11 | `B02-单工具scope-chip.json` |
| B03 | 消息区附件气泡:img[src$="/attachments/{id}"] + 历史回放 src 一致 + naturalWidth>0 | #12 | `B03-气泡附件img.json` |
| B04 | 15MB 附件 url 回退提示(「大文件将以 URL 引用传输」+ 库层 transport=url) | #15 | `B04-回退提示文案.txt` |
| B05 | demo-host 请求体断言:POST /runs 携带 agentType='demo' 且无 enabledMcpTools | #20 | `B05-demo-host-runs请求体.json` |

口径说明:管理站交接「expired 档含 id 39」在本环境以**自建 fixture 审计行(id 随建随录,`A04-expired-审计fixture行.txt`)**替代演示,下拉含 expired + 过滤命中的验证语义不变。

## 6. 26 条优化逐条验证矩阵(条目→落点提交→验证证据→判定)

> 编号 = `99-优化建议.md` 条目;判定:✅ 验证通过 / ⚠️ 部分或留观察 / ❌ 被 DEF-08 打挂。

| # | 条目 | 落点提交 | 验证证据(R3) | 判定 |
| --- | --- | --- | --- | --- |
| 1 | 「mock 后端」徽标→环境状态灯 | 9bbc7f7 | A01 三态 | ✅ |
| 2 | mock 域(熔断/Webhook)补齐 | dfcecb5(服务端)+ 5e8feb7/51de916(web) | spec 07 重写 + A08/A12 + 手验 §2;读/守卫/终止全通;**写端点 500** | ⚠️(DEF-08/09) |
| 3 | zh-cn locale 全量接入 | 80bc75f | spec 01/02 回归;确认框中文按钮(确认轮换/重投)实证 | ✅ |
| 4 | 上传超限 413+可读文案 | dfcecb5 | §3 413 手验 | ✅ |
| 5 | 断流静默提示 | 8cffbeb | B01;spec 09 回归兼容(断言零改动) | ✅ |
| 6 | 轮换「宽限期未实现」文案纠正 | 0e518c1 | spec 02(断言更新)+ A06 对话框 72h 文案 | ✅(轮换执行被 DEF-08 阻断,文案/展示面无涉) |
| 7 | 审计时间筛选即输即查 | 898e4e7 | A02 + spec 05 L5 时间过滤回归 | ✅ |
| 8 | 指纹/轮换展示位 | d805626 | A06 | ✅ |
| 9 | schema 历史 UI 入口 | 4cb9470 | A07 | ✅ |
| 10 | decision_source 下拉补 expired | 9585a4a | A04 + spec 05 断言反转 | ✅ |
| 11 | 单工具确认卡 scope chip | 6b57c1a | B02 + spec 11 断言反转 | ✅ |
| 12 | 消息区附件渲染 | 84a40ed | B03(首发+回放) | ✅ |
| 13 | toolFqn 检索语义对齐(ILIKE) | dfcecb5 | A13 | ✅ |
| 14 | 模型表单 textProtocol | b36d08d | A09 | ✅ |
| 15 | 附件 url 回退提示(README 半:d8cf6e1) | 2b3e060 | B04;README 章节静态核验 | ✅ |
| 16 | 空态设计 IaEmpty | 21fc753 | A12 占位 + A07/审计空态渲染;六域接入由 web 单测覆盖 | ✅ |
| 17 | 错误码语义表 | dfcecb5 | 413(R3-11)/409(R3-07)/统一错误体(R3-02)抽样 | ✅(DEF-08 的 500 兜底文案亦为统一形态) |
| 18 | 列表页三件套 IaListPage | 0275013 | A03 | ✅ |
| 19 | SDK README import map 集成说明 | 460f958 | 文档面,静态核验 sdk-js/README.md | ✅(文档面) |
| 20 | demo-host 声明 agentType | beaa822 | B05 请求体断言;R2 环境差异(spec 08 中转页依赖)就此具备清理条件 | ✅ |
| 21 | webhookSecret 明文类型清理 | 0e518c1 | web 单测/typecheck 面(typecheck 随 pnpm build 通过);掩码展示回归 | ✅(单测面) |
| 22 | 面包屑 + IaPageContainer | 0d5a726 | 各 spec 截图页头(面包屑/副标题/页级操作右置) | ✅(展示面) |
| 23 | 分页器 IaPagination | 6f94510 | spec 05 分页用例回归(总数/翻页语义不变) | ✅(回归面) |
| 24 | IaTime 相对时间 | 5bb251f | A11 | ✅ |
| 25 | 过时契约注释 CI 守卫 | d12615f | 静态;**OBS-R3-2:CircuitView 头注释仍有「≤5s」漏网** | ⚠️ |
| 26 | 窄屏收次要列 | 9f979ee | A10 | ✅ |

矩阵小结:✅ 22 · ⚠️ 3(#2/#25 + #6 附注)· ❌ 0(直接验证面);#2 的服务端写半与 #6 的执行端被 **DEF-08** 打挂。

## 7. 已知残留(移交下批,非本轮缺陷)

1. **DEF-08/DEF-09**(见 `00-缺陷清单.md`)。
2. limits 内核执行层仅落库未强制(下批接线,任务书已知残留);紧急停用不批量取消进行中 run(逐个 terminate-run,本轮已验证该语义)。
3. 状态灯「演示数据」态的 e2e 取证依赖 web vite dev(5177)常驻——若后续嫌重,可改为 mockServiceWorker 注入式单测;本轮已入 spec A01(环境无该端口时 A01 失败,属环境前置而非产品缺陷)。

## 8. 可复跑性说明

- 跑法:`cd e2e && npx playwright test`(本机注意 **cwd 复位坑**:须在 e2e 目录内执行,否则 @playwright/test 双实例加载冲突);workers=1 串行,证据落 `assets/`。
- 前置顺序:`jar 重建(若 inneragent-server 有新提交)→ source e2e/env.sh up → pnpm -C sdk-js build → demo-host 5180 → demo-spring-host 18091 → reconnect-host 18085 →(A01 需要)web vite dev 5177 → 六工具重注册(R3-00-工具注册表.txt 有完整 payload)`。
- 每用例固定标识符 + 查库自建前置 + finally 物理清理;本轮 18 条新用例与 64 条回归同轮混跑无串扰。

## 9. 清理确认(证据 `assets/R3-99-清理确认.txt`)

- 辅助进程先停(vite×2/reconnect-host/demo-spring-host,含 mvn 父进程),再 `source e2e/env.sh down`:gateway/server 已停。
- **8 端口全部无监听**(18090/18081/18085/5180/18091/5177/35432/36379);inneragent 容器 0;compose down(volume 保留,V1..V14 完整迁移态,惯例同 R2)。
- 数据:18 条新用例自建前置全部 finally 物理清理;手验残留(mockScript 复位、演示用户 97770~97772 行、delivery fixtures)已清;`ia_circuit_event` 保留 1 条 run-terminated 手验留痕、六工具注册留待下轮复用或清理(两可,注册 payload 已存档)。
- git:仅 `e2e/**`(helpers/support.ts 证据目录指向 03 + 4 个 spec 断言更新 + 2 个新 spec),提交前缀 `test(e2e):`,**未 push**。

## 10. 判定

**R3 验证轮不能收官**:新能力验证面 18/18 全绿、26 条矩阵 22 条全绿,但 **DEF-08 属本批次引入的服务端回归(R2 全绿的公钥轮换被打挂)且熔断/Webhook 写端点全灭**——按「发现缺陷只出分册+缺陷清单」口径,不出 `00-R3验证报告.md`;待服务端修复后做 **R4 复测轮(范围:spec 02 轮换 + 熔断域三写端点 + webhook config 保存 + deliveries 无 status 形态,预计 <10 用例)** 即可补收官文档。
